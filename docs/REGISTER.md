# Findings register

Per `porting-directive.md` §5: a finding is recorded when noticed, before it is repaired and
before it is understood. Status moves **found → recorded → decided**; a recorded item is not a
resolved one. This register was empty until 2026-08-14 despite substantial prior work — several of
the entries below were found earlier this session and only reached durable-doc form (let alone
register form) today. That gap is itself the reason this file exists: session memory is not a
register.

**Columns**: found (date) · what · where · status · decision (if any).

---

| Found | What | Where | Status | Decision |
|---|---|---|---|---|
| 2026-08-10 | CTRL: invented `ctrl_coef` weighting not in the paper (plain sum `L_CTRL = L_clust + L_pred`, no scalar) | `rlgen/algos/onpolicy_ext.py` `CTRLLearner`; `FAITHFULNESS.md` §ctrl point 0 | decided | Fixed — coefficient removed |
| 2026-08-10 | CTRL: `L_clust` (Sinkhorn-Knopp clustering loss) missing entirely — only `L_pred` survives; EMA k-means centroids have no gradient | `onpolicy_ext.py` `CTRLLearner.update`; `FAITHFULNESS.md` §ctrl point 1 | recorded | Not decided — "small to change... left as a decision rather than made silently" |
| 2026-08-10 | CTRL: positives drawn from same partition, paper specifies neighbouring partitions | `onpolicy_ext.py:355-361`; `FAITHFULNESS.md` §ctrl point 2 | recorded | Not decided |
| 2026-08-10 | CTRL: SSL step shares the policy optimizer; paper source (`pseudocode.tex:59,64`) runs two optimizers over disjoint parameter sets, policy params not in the SSL update at all | `onpolicy_ext.py` `CTRLLearner.update`; `FAITHFULNESS.md` §ctrl point 3 | recorded | Not decided |
| 2026-08-13 | CTRL: `_embed_windows` has no episode-boundary guard — flattened rollout windowed by raw index, splices frames across episodes into one embedding | `rlgen/algos/onpolicy_ext.py::CTRLLearner._embed_windows`; `FAITHFULNESS.md` §ctrl point 4 | **corrected 2026-08-14** | **Not a divergence.** Found once 2026-08-13, lost across a context compaction, rediscovered 2026-08-14 and *mischaracterized as a bug relative to the reference* on that same recovery pass. Directly checked against `ext/ctrl_public/algo.py:90-95` (`extract_windows_vectorized`) while reading `algo.py` for the hermetic module: the reference itself takes no `done`/episode signal and windows by raw index too — identical property, not a gap relative to it. Matching this (unguarded) is the faithful default; adding a guard would be a declared improvement beyond the reference, an open branch point, not a fix. See corrected `FAITHFULNESS.md` §ctrl point 4 for the full primary-source citation. This is itself the second instance of the exact failure this register exists to catch — a claim repeated across a compaction without re-verification (`porting-directive.md` §6: argument-shaped claims are never inherited as settled) |
| 2026-08-14 | PPG: exactly one shared value head (`cfg.algo="ppo"` → `value_net` stays `None`); paper needs a genuinely separate, more-frequently-updated value network the auxiliary phase distills from | `rlgen/algos/onpolicy_ext.py::PPGLearner`; `rlgen/algos/idaac/algo.py::Learner.__init__`; `FAITHFULNESS.md` §ppg "Pipeline audit" | recorded | Not decided — "flagging for a decision rather than building it unprompted" |
| — (undated, predates this session's dating discipline) | PPG: official code has zero gradient clipping and two internally-inconsistent discount factors (reward normalizer defaults γ=0.99, GAE/value path uses γ=0.999); this repo's shared `Learner.update()` imposes grad clipping on PPG via inheritance, undeclared as an accepted-or-rejected decision | `ext/phasic-policy-gradient/.../train.py`, `reward_normalizer.py`; `rlgen/algos/idaac/algo.py:147`; `FAITHFULNESS.md` §ppg | recorded, upgraded from bare fact to register entry 2026-08-14 | Not decided. Distinguish from the paragraph above it in `FAITHFULNESS.md`: that text records a *fact about the reference*, not a stated accept/reject — recording is not justifying (`porting-directive.md` §6) |
| 2026-08-13 | IBAC-SNI: value head computed from the SNI-mixed/noisy feature instead of the deterministic pass; three independent references (TF CoinRun, authors' own torch port, DZ's Procgen port) all route the value head through the deterministic pass specifically | `rlgen/algos/onpolicy_ext.py::IBACSNILearner`; `FAITHFULNESS.md` §ibac_sni | decided | Fixed — `value_from_feat` substitutes the deterministic pass |
| 2026-08-14 | The shared-PPO-core premise itself (`onpolicy_ext.py` inheriting `idaac/algo.py:Learner` for ppg/ibac_sni/ctrl) was never tested at the architecture level — every fix above operates *inside* the premise; none tested whether the premise should hold. CTRL's own reference is JAX, never had a code relationship with IDAAC's PyTorch core at all | This conversation, turns on "are you really quite sure... justified" and "did you take ours IDAAC port or some official one" | decided | Superseded — default flipped to hermetic-per-baseline (`porting-directive.md` §1); see `jolly-spinning-valiant.md` |
| 2026-08-14 | RAD has no genuine local clone; the vendored stand-in (`ext/dmcontrol-generalization-benchmark/src/algorithms/rad.py`) is a 13-line empty `SAC` subclass, augmentation applied elsewhere; official `MishaLaskin/rad` README confirms the same architecture (agent and augmentation selected independently via flags) but the exact augmentation parameters were not diffed | `ORIGINAL_LOCATIONS.md` §rad | recorded | Not decided — cheap to close (fetch `data_augs.py`, diff), not yet done |
| 2026-08-14 | No PyTorch reimplementation or independent reproduction of CTRL exists anywhere (two independently-run `agy` searches, different methods: fork/profile enumeration, then citation-graph analysis over all 30 citing papers via Semantic Scholar — same negative result) | `ORIGINAL_LOCATIONS.md` §ctrl; `agy/ctrl_pytorch_result.md` | recorded | Not decided — queued as a background `agy` succeed-or-fail attempt, `jolly-spinning-valiant.md` §CTRL framework |
| 2026-08-14 | `svea`/`curl`/`drq` as shipped by `RL-ViGen-upstream` are backbone-ported onto DrQ-v2-style structure (hydra, `RandomShiftsAug`), not transcriptions of each method's own original backbone (SVEA is DrQ-based; CURL and DrQ predate DrQ-v2 and are SAC-based in their own official repos) | `RL-ViGen-upstream/algos/{svea,curl,drq}.py`; classification pass, this entry | recorded | Not decided — classified as Adaptation (RL-ViGen's own, inherited as-is), not Transcription; see classification table in `ORIGINAL_LOCATIONS.md` |
| 2026-08-14 | `rlgen/algos/soda.py` and `sac.py` carried no citation/provenance comment at all, unlike every other file in `rlgen/algos/` — no stated source for the SAC implementation or the SODA mechanism's specific parameters | `rlgen/algos/soda.py`, `rlgen/algos/sac.py` | **decided** | **Checked directly the same day, not left pending**: diffed both files against `ext/dmcontrol-generalization-benchmark/src/algorithms/{sac,soda}.py` — near line-for-line match (only `.cuda()`→`.to(self.device)` and import-path differences). Both were Transcriptions missing their citation, not Construction. Citations added to both files; `soda.py`'s inherited-and-fixed bug (`hasattr(self, 'soda_predictor')`, a name never set) documented in the new docstring |
| 2026-08-14 | Self-correction: Stage 0's own classification pass called `rad`/`soda` Construction from `_sac_family`'s caller-side docstring ("recovered... rather than rewritten") without reading `rad.py`/`soda.py`/`sac.py` themselves. `rad.py` actually has a full citation and a well-declared branch point already (render-resolution crop-vs-shift substitution); `soda.py`/`sac.py` are near-verbatim transcriptions missing only a citation comment | `docs/ORIGINAL_LOCATIONS.md` classification table, corrected same-day | decided | Reclassified: `rad` → Adaptation (already well-declared), `soda`/`sac.py` → Transcription. Corrected count: 3 Constructions (`ppg`, `ibac_sni`, `ctrl`), not 5 — and now every remaining Construction has a known defect, every corrected baseline does not. Kept in the register rather than silently fixed, per `porting-directive.md` §6: verification attaches to a claim, and this session's own prior-turn claim was the thing that needed re-verifying |

| 2026-08-14 | `sgqn`'s classification was left "unconfirmed" in Stage 0 (which backbone SGQN's own paper uses wasn't checked) | `docs/ORIGINAL_LOCATIONS.md` §sgqn, first pass | decided | Checked same day: `RL-ViGen-upstream/algos/sgqn.py` imports `from drqv2 import DrQV2Agent, Actor, Critic` directly; SGQN's own official repo builds its actual method on its own local `sac.py`. Same backbone-port pattern as `svea`/`curl`/`drq` — reclassified Adaptation, not Transcription |
| 2026-08-14 | IBAC-SNI: SNI is implemented at a different mathematical level than DZ's own reference. This repo (`rlgen/algos/onpolicy_ext.py::encode_with_vib`, line 198): `z_mixed = sni_lambda*z_clean + (1-sni_lambda)*z_noisy`, ONE mixed feature fed through a single forward pass of the rest of the network (dist_head, value_from_feat). DZ's reference (`~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo_ibac.py::optimize`, lines 51-98): TWO complete separate forward passes (`sample=False` → `dist_d, value_d`; `sample=True` → `dist_s, value_s, ib_kl`), then `pi_loss = sni_lambda*loss(dist_d) + (1-sni_lambda)*loss(dist_s)`, and — not previously noticed — `value_batch = sni_lambda*value_d + (1-sni_lambda)*value_s` too, mixed the same way as the policy. Linear interpolation of a feature before a nonlinear network is not equivalent to linear interpolation of that network's two separate outputs. This is a different, previously-unflagged divergence from the value-routing bug already fixed (that fix concerned which SINGLE pass the value head reads from within one forward call, not this two-pass mixing question, which sits one level up and was never checked because the earlier verification only opened `policy_ibac.py` — the network file — not `ppo_ibac.py` — the training-loop file, where the actual SNI mixing happens) | `rlgen/algos/onpolicy_ext.py:161-222`; `~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo_ibac.py:51-119` | recorded | Not decided — found while investigating the shared-core premise for the hermetic rewrite, not yet repaired or fully understood (per §5: recorded before either) |
| 2026-08-14 | IBAC-SNI: DZ's own reference PPO base (`~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo.py`) uses gradient accumulation — `.backward()` every minibatch, `optimizer.zero_grad()`/`.step()` only every `grad_accumulation_steps`-th one — and has **no KL-early-stop mechanism at all**, running a fixed `self.epoch` count unconditionally. `idaac/algo.py::Learner.update()` (inherited by `IBACSNILearner` via `onpolicy_ext.py`) does the opposite on both counts: optimizer step every minibatch, and a `kl_early_stop`/`target_kl` per-epoch break IBAC-SNI's own reference never has. The clipped-value-loss formula does match exactly (`0.5 * max((v-ret)^2, (v_clip-ret)^2)`), and DZ's reference also defaults `normalize_rew=True`, reinforcing the reward-normalizer finding from earlier this session | `~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo.py:63-113`; `rlgen/algos/idaac/algo.py:78-256` | recorded | Not decided — this is concrete, first-hand evidence for the shared-core-premise concern (register entry "2026-08-14... shared-PPO-core premise itself... never tested at the architecture level"), now with a specific mechanism named rather than a general worry |

| 2026-08-14 | First hermetic module built: `rlgen/algos/ibac_sni/model.py` (`IBACSNIPolicy`) — network only, not yet wired into the registry/trainer. Verified, not assumed: every parameter reachable by gradient, `deterministic=True` confirmed pure (repeat calls under `no_grad` produce identical output), `kl` confirmed invariant to the `sample` flag (only `z` should differ). Encoder init independently confirmed to match the reference (`xavier_uniform_`/`orthogonal_(sqrt(2))`), not assumed from `idaac/model.py`'s presence. Old baseline's `ibac_sni/__init__.py` docstring claim ("re-deriving would be strictly worse") is directly contradicted by this module's own existence: re-deriving is what surfaced the SNI-output-mixing and gradient-accumulation divergences in the first place | `rlgen/algos/ibac_sni/{__init__,model}.py`; `tests/test_ibac_sni_hermetic.py` (5 tests, all passing) | decided | Built, tested, not yet integrated — `onpolicy_ext.py::IBACSNILearner` is still what the registry actually dispatches to. Next: `rlgen/algos/ibac_sni/algo.py` (the training loop — gradient accumulation, no KL-early-stop, output-level SNI mixing including the value estimate) and its own storage, then the registry switch |

| 2026-08-14 | `idaac/storage.py`'s own docstring cites verification "against a closed-form two-segment GAE in tests/test_storage.py" — that file does not exist anywhere in the current test suite (checked directly: `find`/`grep` across `tests/`, no match) | `rlgen/algos/idaac/storage.py` module docstring | recorded | Not decided — a stale citation in pre-existing code, out of scope for the IBAC-SNI hermetic-module work this citation was noticed during. Not fixed here |
| 2026-08-14 | Own bug caught before it shipped: first draft of `rlgen/algos/ibac_sni/algo.py` called `storage.feed_forward_generator(adv, cfg.num_mini_batch)` — `cfg.num_mini_batch` is a COUNT (32), but this module's own `storage.py` deliberately takes a SIZE, not a count (stated in its own docstring). Caught by re-reading algo.py against storage.py before running either, not by a failing test | `rlgen/algos/ibac_sni/algo.py`, first draft | decided | Fixed same-session: `mb_size = (storage.T * storage.N) // cfg.num_mini_batch` computed explicitly before the epoch loop |
| 2026-08-14 | Own bug caught by running, not by reasoning: first end-to-end smoke test of `ibac_sni/algo.py::Learner.update()` raised "Trying to backward through the graph a second time" — traced to the TEST script, not the module: rollout collection wasn't wrapped in `torch.no_grad()`, so `.act()`/`value_of()` calls during collection built live graphs that got copied into storage and re-entangled with `update()`'s own backward pass later | test script (now `tests/test_ibac_sni_algo.py::_rollout`) | decided | Fixed in the test fixture; `algo.py` itself was never the source of the bug. Kept as an explicit code comment in the fixture so the fix stays exercised, not just fixed once |
| 2026-08-14 | Second hermetic module piece built: `rlgen/algos/ibac_sni/{storage,algo}.py`. Storage: GAE-with-truncation-folded-reward verified against a hand-computed closed form (not just run and eyeballed). Algo: end-to-end update verified — all 41 parameters move after one `update()` call, gradient-accumulation flush path tested explicitly (accum_steps=100 still produces a step via the end-of-update flush), `sni=False` path (the reference's other configuration, not hypothetical) also tested | `rlgen/algos/ibac_sni/{storage,algo}.py`; `tests/test_ibac_sni_{storage,algo}.py` (7 tests) | decided | Built, tested. Not yet wired into `registry.py` — `onpolicy_ext.py::IBACSNILearner` is still what actually runs. That's the next and last step to make this baseline's disaggregation complete |

| 2026-08-14 | IBAC-SNI's hermetic disaggregation wired in: `registry.py`'s `ibac_sni` entry now builds `rlgen/algos/ibac_sni/algo.py::Learner` via a new `_ibac_sni_hermetic_build`, not `_onpolicy_ext("IBACSNILearner")`. Own hermetic `config.py` written too (not `idaac.config.Config`) — found one real, small divergence while sourcing it: DZ's reference uses `lr=2.5e-4`, `idaac.config.Config` uses `3e-4` (IDAAC's own DMC-tuned value) — used the reference's own number, not the borrowed one | `rlgen/registry.py::_ibac_sni_hermetic_build`; `rlgen/algos/ibac_sni/config.py` | decided | Verified against the PRE-EXISTING test suite, not just new tests: `tests/test_onpolicy.py`'s own `test_training_actually_moves_the_parameters[ibac_sni]` (a test this session didn't write) passes through the new module unmodified. `onpolicy_ext.py::IBACSNILearner` is now unreachable, marked as such in its own docstring, kept (not removed) until `ppg`/`ctrl` are also disaggregated — deliberate, not an oversight |

| 2026-08-14 | Full-suite run after wiring caught two real things none of this session's own new tests did: (1) `baselines/ibac_sni/README.md` went stale (it's generated from the registry notes, which the wiring commit changed) — `tools/gen_baselines.py --check` caught it precisely; (2) a PRE-EXISTING test, `test_onpolicy.py::test_ibac_value_head_never_sees_the_noisy_or_sni_mixed_pass`, reached into `learner.vib`/`learner.policy.encode()`/`value_from_feat()` — all specific to the OLD `onpolicy_ext.py::IBACSNILearner`'s monkeypatch structure, which the new hermetic module correctly does not have (one combined `forward(obs, sample)`, no separate encode/value_from_feat two-step) | `tests/test_contract.py::test_generated_baseline_files_are_current`; `tests/test_onpolicy.py:184-200` | decided | (1) Regenerated via `tools/gen_baselines.py`, 1-line diff. (2) Test updated to check the exact same property (gradient reaches VIB's parameters through the deterministic value path) via the new module's actual structure (`learner.policy.vib`, `learner.policy(obs, sample=False)`) rather than weakened or deleted — this is the "verify the tool has teeth" moment for the full-suite-run step itself: it found two real things this session's own targeted tests missed |

| 2026-08-14 | PPG's aux phase is more involved than this repo's existing docstring ("distils the value function into the policy network's auxiliary head") captures. Read `ext/phasic-policy-gradient/phasic_policy_gradient/ppg.py` directly (not previously read in full this session — only `reward_normalizer.py` had been). `arch="dual"` (the confirmed default) builds a genuinely separate `vf_enc`+`vf_vhead` network producing `vpredtrue`, AND a small `aux_vf_head` attached to the POLICY's own encoder producing `vpredaux` — two distinct value predictions, not one. The aux phase's loss (`aux_train`, `ppg.py:178-201`) has THREE terms trained simultaneously: `pol_distance` (KL-clone, already known), `vf_aux` (train the policy-encoder aux head), AND `vf_true` (the aux phase ALSO continues training the separate value network itself — not previously captured). The aux phase uses its OWN separate Adam optimizer over `model.parameters()` at `aux_lr` (`ppg.py:239`), distinct from the policy phase's own PPO optimizer — currently this repo's `PPGLearner._auxiliary_phase` uses `self.policy_opt` for everything. `make_minibatches` (`ppg.py:163-175`) calls `th.randperm` fresh every call, and is called once per aux epoch inside `aux_train`'s own loop — CONFIRMS, from the actual reference rather than inference, that the official aux phase DOES reshuffle every epoch. `docs/FAITHFULNESS.md` §ppg had this as "a plausible divergence... not a confirmed paper contradiction" (no reference to check against at the time) — now confirmed as a real divergence, this repo's current no-reshuffle behavior is wrong, not just unverified | `ext/phasic-policy-gradient/phasic_policy_gradient/ppg.py` (full file, all line numbers above real); `docs/FAITHFULNESS.md` §ppg (to be updated to reflect this is now CONFIRMED, not plausible) | recorded | Not decided — this reshapes the PPG hermetic module's scope: dual network was already known; separate aux optimizer, the `vf_true` retraining term, and confirmed-not-plausible reshuffling are new. Task #18 (PPG hermetic module) not yet built past this reading |

| 2026-08-14 | PPG's own reference runs exactly ONE epoch of minibatch SGD per policy-phase update (`n_epoch_pi=1`, `n_epoch_vf=1`), not ten. Confirmed at both levels, not a generic fallback: `ppo.py::learn`'s own function-signature default AND `train.py::train_fn`'s default AND its CLI argument default (`--n_epoch_pi`, default `1`) all agree. This project's shared `Learner` currently applies `cfg.epochs_policy=10` (IDAAC's own `[E]` DMC-tuned value) to every on-policy baseline including PPG via inheritance — a third real, previously-undetected hyperparameter divergence for PPG specifically, after `lr` (2.5e-4 vs this project's inherited 3e-4 — wait, that's IBAC-SNI's; PPG's own is `5e-4` per `[P]`, already known to diverge from this project's `1e-4`) and grad-clip (already known, PPG's reference has none). Also newly found: a `vf_true_weight` coefficient (`train.py:29`, default `1.0`) explicitly weights the aux phase's value-network-retraining term (`name2coef = {"pol_distance": beta_clone, "vf_true": vf_true_weight}`, `train.py:54`) — not previously documented as a separate, named hyperparameter | `ext/phasic-policy-gradient/phasic_policy_gradient/ppo.py:126-129`; `train.py:17-18,83-84,29,54` | recorded | Not decided — `epochs_policy` needs its own hermetic, PPG-sourced value (1, not 10) in the eventual `ppg/config.py`, same pattern as `ibac_sni/config.py`'s `lr` correction |

| 2026-08-14 | PPG's own init scheme checked directly (`impala_cnn.py`/`torch_util.py`) rather than assumed from the xavier/orthogonal convention already used for `idaac`/`ibac_sni` — genuinely different: PyTorch default init rescaled to a target L2 norm per output unit (`NormedLinear`/`NormedConv2d`), every head at `scale=0.1`, conv stacks at a `1/sqrt(3)` → `/sqrt(2)` → `sqrt(...)` cascade. Implemented faithfully in `rlgen/algos/ppg/model.py`, not left as the wrong-but-flagged xavier/orthogonal duplication a first draft had. One piece not independently confirmed: the final flatten→hidden projection's own exact scale (not located in the portion of `impala_cnn.py` read) — `scale=1.0` used, named as unconfirmed rather than silently assumed correct | `ext/phasic-policy-gradient/phasic_policy_gradient/{impala_cnn,torch_util}.py`; `rlgen/algos/ppg/model.py` module docstring | decided | Implemented and verified: 5 tests (`tests/test_ppg_hermetic.py`) confirm shapes, full gradient reachability, `net.value()`'s fast path matches `forward()`'s `vpred_true` exactly, deterministic `act()` is pure, and `pi_enc`/`vf_enc` share zero parameter tensors (the actual property `arch="dual"` requires) |

| 2026-08-14 | PPG's own reference has no learning-rate decay mechanism at all — checked precisely (`grep` for `adjust_lr`/`lr_decay`/`scheduler`/direct `param_groups` mutation across `ppg.py`, `ppo.py`, `train.py`: zero matches). A fourth real, previously-uncaptured divergence for PPG specifically, alongside `lr` (5e-4 vs. this project's inherited 1e-4), grad-clip (none vs. inherited `max_grad_norm`), and `epochs_policy` (1 vs. inherited 10, all already recorded above). The shared core's `set_lr(frac_done)` linearly decays LR for every on-policy baseline via inheritance; PPG's own reference runs at a constant LR throughout | `ext/phasic-policy-gradient/phasic_policy_gradient/{ppg,ppo,train}.py` (grep, zero matches) | recorded | Not decided — `ppg/config.py`'s eventual `linear_lr_decay` should default `False` for this method, matching the reference, not `True` |
| 2026-08-14 | PPG hermetic storage built: `rlgen/algos/ppg/storage.py`, a duplicate of `ibac_sni/storage.py`'s per-iteration rollout buffer (identical GAE-with-truncation-folding logic, same environment-contract reasoning) — handles one policy-phase iteration only, not the aux-phase segment stash, which is `Learner`-level state matching the shape `onpolicy_ext.py::PPGLearner._aux_obs`/`_aux_ret` already used | `rlgen/algos/ppg/storage.py`; `tests/test_ppg_storage.py` (3 tests, same closed-form GAE check as `ibac_sni`'s) | decided | Built, tested. `algo.py` (the two-level N_pi/aux-phase loop, 3-way aux loss, separate optimizer, confirmed reshuffling) is the substantial remaining piece — deliberately not rushed into the same pass as this entry |

| 2026-08-14 | PPG's own reference uses plain, UNCLIPPED MSE for the value loss (`ppo.py::compute_losses`, `losses["vf"] = vfcoef * ((vpred - vtarg) ** 2).mean()`) — no clip-around-the-rollout-time-prediction at all, unlike `idaac/algo.py::_value_loss`'s clipped branch (which `ibac_sni` correctly inherited-then-duplicated since IBAC-SNI's OWN reference genuinely does use the clipped formula — confirmed identical formula, `docs/REGISTER.md` earlier entry). A fifth real, previously-uncaptured divergence for PPG specifically, and the first case this session where two of the three on-policy Constructions actually need DIFFERENT value-loss formulas from each other, not just different constants — direct evidence against treating "the value loss" as one shared, method-independent piece of infrastructure | `ext/phasic-policy-gradient/phasic_policy_gradient/ppo.py:107` | recorded | Not decided — `ppg/algo.py`'s value loss must be plain MSE, not a duplicate of `ibac_sni/algo.py::_value_loss`'s clipped formula |

| 2026-08-14 | **MAJOR, self-correcting.** `trainer_onpolicy.py` hardcoded `from .algos.idaac.storage import RolloutStorage` at module scope for EVERY on-policy baseline, regardless of which `Learner` was actually constructed. This means `ibac_sni/storage.py` and `ppg/storage.py` were never reached by the real trainer loop at all — only by their own unit tests, which construct them directly and never exercised this path. Caught only because wiring `ppg` into the registry made it CRASH (`ppg/algo.py` called `storage.feed_forward_generator(adv, mini_batch_size=...)` as a keyword, and `idaac.storage.RolloutStorage`'s own method has no parameter by that name — `TypeError`, loud). `ibac_sni/algo.py` made the SAME underlying call as a POSITIONAL argument, which does not crash against `idaac.storage`'s different-but-compatible-by-position signature — it silently computes the WRONG minibatch count. Worked through concretely: `cfg.num_mini_batch=32`, `B=2048` → `ibac_sni/algo.py` computes `mb_size = B // 32 = 64` (correct SIZE), passes it positionally into `idaac.storage.feed_forward_generator(advantages, num_mini_batch)`, which reads it as a COUNT and computes `mb = B // 64 = 32` internally — so the run actually used 64 minibatches of size 32, not the intended 32 minibatches of size 64. Training still happened (parameters still moved, the earlier `test_training_actually_moves_the_parameters[ibac_sni]` pass is NOT invalidated as "no learning occurred") — but `num_mini_batch=32`, a value this session sourced and cited carefully, was silently not what actually ran. This is exactly the class of thing "verification attaches to a claim... a claim that changes earns no credit from prior verification" (`porting-directive.md` §6) exists to catch — my own prior-turn "IBAC-SNI complete and verified, 232 passed" claim was true about the tests that ran, false about what the real trainer loop actually did with it | `rlgen/trainer_onpolicy.py:82` (before fix); `rlgen/algos/{ibac_sni,ppg}/algo.py`'s `feed_forward_generator` calls | decided | Fixed at the root: every hermetic `Learner` (`idaac`, `ibac_sni`, `ppg`) now declares its own `storage_cls`; `trainer_onpolicy.py` reads `agent.learner.storage_cls` instead of importing one class unconditionally (`ctrl`, still on the shared core, inherits `idaac`'s `storage_cls` correctly and unchanged). `ibac_sni/algo.py`'s call changed to a keyword argument too, matching `ppg`'s, so a future mismatch crashes loudly instead of silently coercing. Full re-verification required for BOTH baselines, not just `ppg` — in progress |

| 2026-08-14 | Two more pre-existing tests broke on the same architectural shift, found by the full-suite run after the `storage_cls` fix (not contamination — reproduced identically on a fresh, clean rerun): `test_on_policy_lr_anneals_linearly_and_clamps_at_both_ends` built `"ppg"` and asserted linear LR annealing — wrong now, PPG's own reference genuinely has no LR decay (already recorded). `test_the_trainer_feeds_set_lr_a_monotone_fraction_of_the_budget` spied on `idaac_algo.Learner.set_lr` while training `"ppg"` — never called once PPG stopped inheriting from that class at all, so `seen` stayed empty | `tests/test_sweep_gaps.py:622-680` | decided | Both switched to test `"idaac"` instead of `"ppg"` — `idaac` genuinely anneals and genuinely owns `idaac_algo.Learner`, so the property each test actually checks (does linear annealing work; does the trainer's `frames/total_frames` reach `set_lr` monotonically) is preserved exactly, just on a baseline where it's still true. PPG's own no-op `set_lr` already has its own dedicated test (`test_ppg_algo.py`). Neither test was weakened or deleted — the assumption "any on-policy baseline exercises this" was the thing that broke, not the property |

| 2026-08-14 | CTRL's JAX-vs-PyTorch decision (queued since the porting-directive planning session) resolved. A background `agy` job attempted a rigorous, failure-biased PyTorch port under `porting-directive.md`'s own T1-T4 tier framework, reading `ext/ctrl_public/{algo,models,buffer,train_ppo}.py` directly. Reported T1 (forward) and T2 (single-step gradient) parity reached, T3 declared honestly unreached (would need a real Procgen training run). **Independently spot-checked, not trusted from the report alone**: (1) the two claimed "upstream JAX bugs" (`ext/ctrl_public/algo.py:203,205`, commented-out `scores_w_target`/`stop_gradient` lines) confirmed by direct file inspection — `scores_w_target` appears exactly twice in the whole file, the commented-out definition and a real use with no other definition anywhere, which would genuinely crash real JAX with `UnboundLocalError`; (2) the parity test itself (`test_parity.py`) actually RUN, by me, independently, not just read — jax/flax were already available in `barannikov-work/.venv` (0.10.2/0.12.8), and all 6 checks passed with real near-machine-precision differences (1e-5 to 1e-8 range) that differed slightly from the report's own numbers run-to-run — consistent with genuinely-executed, non-seed-fixed test inputs, not a canned/fabricated log | `/private/tmp/.../scratchpad/agy/ctrl_pytorch_attempt/{REPORT.md,test_parity.py}`; `ext/ctrl_public/algo.py:203,205` (independently verified) | decided | **Pursue the PyTorch port for CTRL's hermetic module**, matching `ibac_sni`/`ppg`'s pattern. Not yet built into `rlgen/algos/ctrl/` — the agy output is scratch-directory reference material (models_torch.py, algo_torch.py, buffer_torch.py) to build FROM, with the same citation/testing discipline as the other two modules, not to copy in directly. Continuous-action adaptation (Gaussian head) still needed, same as every other on-policy baseline |

| 2026-08-14 | Reward-normalizer status checked directly against each of the 12 baselines' own reference files (not assumed by family resemblance) — porting-directive.md §2: "Its reward normalizer is part of the algorithm and stays," §1: stateful normalizers are never lifted into shared utility code. **PPO/Procgen-lineage (4, confirmed present in the reference): idaac** — `idaac/config.py` already declares `normalize_reward=True`/`reward_clip=10.0` and `storage.py` already carries a dead `rewards_raw` field (`# never touched by the normalizer`) sitting next to the live `rewards` field (`# normalized, + boot value`) — plumbing built, normalizer never wired (`FAITHFULNESS.md` line 585, prior finding). **ctrl** — `ext/ctrl_public/train_ppo.py:91,99,106` construct every env with `normalize_rewards=True`, routing through `vec_env.py`'s `VecNormalize`/`RunningMeanStd` (Welford-tracked `ret_rms`, `rews = clip(rews / sqrt(ret_rms.var + eps), -c, c)`); `evaluate_ppo.py:38` explicitly sets `normalize_rewards=False` for evaluation — the reference's own code independently confirms the train/measurement split `porting-directive.md` §2 requires, not something invented here. **ppg, ibac_sni** — already-recorded prior findings stand: PPG's own `reward_normalizer.py` (register entries above, `ppg.py` reading) and DZ's IBAC-SNI reference defaulting `normalize_rew=True` (`ppo.py`, register entry above). None of the 4 has a wired hermetic implementation yet. **SAC-family (8, checked and genuinely absent): alda** — already correctly decided, `normalize_reward=False`, cited `[AC]`, with an explicit stated reason (SAC's auto-tuned temperature targets a fixed entropy against the reward scale; normalizing would move that target under the agent during training) — `rlgen/algos/alda/config.py:56-64`, the model case for how this should look per baseline. **drqv2, svea, sgqn, curl, drq** — grepped `RL-ViGen-upstream/algos/{drqv2,svea,sgqn,curl,drq}.py` directly for `normalize`/`reward_scale`/`RunningMeanStd`/`VecNormalize`/`ret_rms`: zero matches in any of the five. **rad, soda** (+ `sac.py`, their shared backbone) — same grep against `rlgen/algos/{rad,soda,sac}.py` and `ext/dmcontrol-generalization-benchmark/src/algorithms/`: zero matches (the only hits anywhere under that vendored tree are in unrelated `dm_control` locomotion task files, not the SAC/RAD/SODA algorithm code) | `rlgen/algos/idaac/{config,storage}.py`; `ext/ctrl_public/{train_ppo,vec_env,evaluate_ppo}.py`; `rlgen/algos/alda/config.py:56-64`; `RL-ViGen-upstream/algos/*.py`; `rlgen/algos/{rad,soda,sac}.py` | recorded | Not decided (mechanism), partially decided (scope): 4 of 12 need a real per-baseline hermetic implementation (`idaac`, `ppg`, `ibac_sni`, `ctrl`), 8 of 12 are genuinely `NotApplicable` per §2 (1 already decided and cited — `alda`; the other 7 newly checked and recorded here, reasons given per family). Placement mechanism for the 4: see next entry |
| 2026-08-14 | Reward-normalizer placement decision. Per §1 ("stateful objects... are not lifted out... identical code under different parameters is a different object") a single shared `RunningMeanStd`/Welford utility class imported by idaac/ppg/ibac_sni/ctrl is the wrong shape even though the math rhymes across all four — same reasoning already applied to `ppg/storage.py` vs. `ibac_sni/storage.py` (register entry above: "a duplicate... identical GAE-with-truncation-folding logic... same environment-contract reasoning" — duplicated, not shared, on purpose). Per §2, the harness must still see the true raw reward for measurement — `idaac/storage.py`'s existing `rewards_raw` field is exactly this, already correctly named and already unpopulated correctly (nothing writes it wrong; nothing writes it at all). **Decision**: a `normalize_reward(self, raw_reward) -> reward` method on each Learner; no-op identity on whichever base/default path a bare Learner uses; each of the 4 confirmed-present baselines overrides it with a normalizer independently transcribed from *its own* reference file (gen-rebuttal's `_RewardBookkeeping`/`RunningReturnStd` for idaac, `ext/ctrl_public/vec_env.py::RunningMeanStd`/`VecNormalize.step_wait` for ctrl, PPG's own `reward_normalizer.py` for ppg, DZ's `ppo_ibac.py` reward path for ibac_sni) — four separate pieces of code, not one parametrized. Call site: `trainer_onpolicy.py`, between `env.step()`'s raw reward and `storage.insert()` — `reward_raw` keeps the untouched value (already logged/accumulated into `ep_ret` before this point, unaffected), `reward` becomes `learner.normalize_reward(reward)`'s output. `trainer.py` (off-policy) has no equivalent split at all (`replay.add(..., reward, ...)` straight from `env.step()`) — moot for now since all 8 SAC-family baselines are `NotApplicable`, but the gap is real if that ever changes and is not silently assumed safe. Sequencing, per §5 ("Step N dictates step N+1"): one baseline at a time, same discipline as the ppg/ibac_sni build (read reference → transcribe → test → wire → verify against full suite → commit) — not a single mechanical edit touching all four algo.py files plus the shared trainer at once. `idaac` first: the storage-level plumbing already exists and needs only the normalizer itself plus the trainer call site; `ppg`/`ibac_sni`/`ctrl` follow, each needing their own hermetic config field and normalizer file too | `rlgen/trainer_onpolicy.py:144-150`; `rlgen/algos/idaac/storage.py:40-41`; `porting-directive.md` §1-2 | decided (mechanism and sequencing) | Not yet implemented for any of the 4. Next action: `idaac` |

| 2026-08-14 | CTRL reference (`ext/ctrl_public/algo.py`, all 618 lines) now fully read for the hermetic module (task #19), not just grepped. Five new findings, none previously recorded. (1) **The two already-confirmed commented-out lines (`scores_w_target`, `stop_gradient`, `algo.py:203,205`) break more than previously scoped**: `cluster_idx = jnp.argmax(scores_w_target, 1)` (line 211) reads the undefined name, and EVERYTHING downstream of it — `X_grouped`, `group_by`'s scan, `cluster_membership_list`, `nearby_vec_idx`, and therefore the entire MYOW neighbor-selection mechanism that picks CTRL's positive pairs — is unrunnable in real JAX as shipped, not just the stop-gradient on the proto-loss target. The immediately-preceding commented line (`# scores_w_target = w_clust_target @ protos`) shows what the live definition would be character-for-character — a literal adjacent artifact, not an inferred guess (`porting-directive.md` §3 distinguishes reading what's there from filling a gap by inference; this is the former). (2) **A `v`/`w` dual-embedding split never previously documented**: `cluster()` returns FOUR embeddings (`v_clust, w_clust, v_pred, w_pred`), not one — `v_clust`/`protos` drive the Sinkhorn-Knopp cluster-assignment score, `w_clust_target` (via the broken path above) would drive neighbor selection, `v_pred`/`w_pred_target` are what the MYOW cosine loss actually runs on. This repo's current construction (`onpolicy_ext.py::CTRLLearner._embed_windows`) produces exactly ONE embedding per window — structurally simpler than the reference in a way no prior note flagged. (3) **Three separate Optax optimizers confirmed by the executable code, not just the pseudocode already cited for DEPARTS(3)**: `TrainState.tx` is a `Sequence`, and `apply_gradients(..., opt_idx=N)` is called with `opt_idx=0` (`update_ppo`'s combined actor+critic loss), `opt_idx=1` (`update_cluster`'s SSL loss, `algo.py:525`), `opt_idx=2` (`update_daac`'s separate value-only pass, `algo.py:467`) — doubly confirms the policy parameters are never in the SSL update, now from the actual training-step code rather than the paper's pseudocode alone. (4) **`update_cluster`'s own comment contradicts its own code**: `algo.py:490`, `"CTRL has large memory footprint bc sliding window so take 1/8 of them"`, immediately followed by `algo.py:491`, `size_batch = num_envs * (n_steps-cluster_len+1) // 2` — computes a HALF, not an eighth. Separately, `idxes_cluster` is a permutation over `[0, size_batch)` where `size_batch` is already restricted to the first half of the possible window range before the permutation runs — meaning the actual mechanism only ever draws minibatches from the FIRST half of each rollout's windows, never the second half, which is a different (and more consequential) property than "randomly subsample half of all windows" and not what the comment describes either way. Lower confidence than (1)-(3): read directly, not executed, so stated as an observation of the code's literal indexing rather than a fully traced runtime claim. (5) **`reward_mlp` (`models.py:124`) is declared as a submodule but never invoked in `cluster()`'s live forward pass** (`models.py:184-186`, the FiLM-from-reward branch is commented out) — this REFINES rather than contradicts the existing docstring's "rewards are never used": true in the forward computation, but as dead initialized parameters sitting in the model rather than a clean omission, a third instance of the same commented-out-mid-development-code pattern as (1) | `ext/ctrl_public/algo.py:170-240` (`loss_cluster`), `:478-534` (`update_cluster`), `:330-475` (`update_ppo`/`update_daac`, `opt_idx` sites); `ext/ctrl_public/models.py:124,163-186` | recorded | Not decided. Reshapes what CTRL's hermetic module needs to declare: the MYOW neighbor-selection mechanism the paper intended cannot be recovered from this file alone (§3: "where the trace will not close... name the gap, do not fill it by inference") — this repo's existing same-partition-sampling simplification (DEPARTS(2), already recorded) is now the ONLY runnable option evidenced by primary source, not merely a simplification of something that does run. The half-vs-eighth and first-half-only window selection ((4) above) is a candidate additional divergence for the hermetic module to either match or deliberately deviate from, not yet decided either way |

| 2026-08-14 | CTRL reference reading pass completed: `buffer.py` (86 lines) and `train_ppo.py` (289 lines) now fully read, plus `vec_env.py`'s wrapping order traced precisely (not just grepped for keyword hits, per the earlier reward-normalizer entries). Four findings. (1) **`info['episode']['r']` (what `train_ppo.py`'s own in-loop ID/OOD eval logs) is confirmed genuinely raw, by wrapping order, not by a flag**: `vec_env.py:38` wraps `VecMonitor` first (innermost, tracks cumulative episode reward before any transform), `:44` wraps `VecNormalize` around it; `VecNormalize.step_wait()` (`:271-279`) returns `infos` untouched from the inner `venv.step_wait()` call — so even though `train_ppo.py`'s `env_test_ID`/`env_test_OOD` are constructed with `normalize_rewards=True` (`:91,99,106`, already recorded), the episode returns it logs are still pre-normalization. This is a SECOND, independent mechanism achieving the same train/measurement split as `evaluate_ppo.py:38`'s `normalize_rewards=False` (previously the only cited evidence) — strengthens rather than contradicts that finding, and structurally the same pattern gen-rebuttal's `_RewardBookkeeping` independently converged on (raw return tracked separately from the normalized value returned for training). (2) **`buffer.py`'s `calculate_gae` has no truncation/timeout bootstrap distinction at all** — `value_diff = discount * value[t+1] * (1 - done[t]) - value[t]`, one `done` flag, no separate `truncated`. Confirms this is genuinely a Procgen-family property (episodes there don't need the terminated-vs-truncated split this project's robosuite adaptation requires for Door/Lift), not something CTRL specifically chose against — the existing truncation-fold-into-reward machinery already built for `idaac`/`ppg`/`ibac_sni`'s storage is adaptation infrastructure this repo already owns, not something to source from `buffer.py`. (3) **The target network (`train_state_target`, used for `w_clust_target`/`v_clust_target`/`v_pred_target`/`w_pred_target`/the Sinkhorn target) is initialized from the SAME `key` as the online network** (`train_ppo.py:146,150`, both call `.init(key, ...)` with the identical `key` variable, before any `jax.random.split`) — online and target start with identical weights, then diverge only via the `state_update` EMA (`tau=0.95`) after each `update_cluster` call. `train_state_target`'s own optimizer (`tx_target`, `:162-163,172`) is passed to `TrainState.create` but never reached by any `.apply_gradients()` call in the file — the target network is never trained by gradient descent, only EMA'd; `tx_target` is dataclass-required plumbing, not live machinery. A fourth, minor instance of the file's general "declared but not exercised" character (alongside the three commented-out branches already recorded). (4) **`train_ppo.py:69` hardcodes a W&B API key in plaintext** (`flags.DEFINE_string("wandb_key", "02e3820b...", ...)`) — the reference authors' own key, committed in their public repo, not this project's; noted here because it's the kind of thing worth a heads-up given this workspace's own W&B-key discipline, but it is third-party material inside a read-only `ext/` vendor copy and there is nothing to fix in this repo | `ext/ctrl_public/buffer.py` (full, 86 lines); `ext/ctrl_public/train_ppo.py` (full, 289 lines); `ext/ctrl_public/vec_env.py:38,44,248-279,330-369` | recorded | Not decided (mechanism-wise, doesn't change the already-decided reward-normalizer placement — item (1) confirms the existing plan's raw/normalized split is correct, doesn't revise it). Reference-reading for CTRL's hermetic module is now at the same depth as `ibac_sni`/`ppg` before their builds started: `algo.py`, `buffer.py`, `train_ppo.py` fully read; `models.py` fully read earlier this session; `vec_env.py` read at the sections load-bearing for reward normalization and episode-info reporting, not cover-to-cover |

| 2026-08-14 | Self-correction, re-reading `models.py` directly rather than trusting this session's own earlier characterization (`porting-directive.md` §6: argument-shaped claims, including this session's own prior output, are never inherited as settled). The register entry above describes `v_clust/w_clust`, `v_pred/w_pred` as "FOUR embeddings" from a "dual-embedding split" — imprecise. Re-read `models.py:192-198` directly: `w_clust = self.w_clust_mlp(v_clust)` and `w_pred = self.w_pred_mlp(v_pred)` — `w_clust`/`w_pred` are NOT independent branches from `z`, they are further MLPs applied ON TOP of `v_clust`/`v_pred` respectively. The chain is `z -> v_clust -> w_clust` and `z -> v_pred -> w_pred`, two chains not four independent heads. The ROLE division already recorded (v_clust/protos for cluster scoring, v_pred/w_pred_target for the MYOW loss) stands; only the "independent split" framing was wrong. Also newly confirmed while re-reading for the model build: `default_conv_init = nn.initializers.glorot_uniform()` (`models.py:14`) is mathematically identical to PyTorch's `xavier_uniform_` (Glorot and Xavier name the same method) — CTRL's own reference conv init already, independently, matches the convention `idaac/model.py` uses, not something borrowed from it. **New adaptation branch point** (`porting-directive.md` §4): `action_mlp`'s input (`models.py:176`, `jax.nn.one_hot(action, n_actions)`) is a one-hot discrete-action encoding, which has no direct analog for this project's continuous 7-D robosuite action space. Structural property the mechanism depends on: `action_mlp` needs SOME fixed-size vector representation of the action to produce the FiLM `(gamma_a, beta_a)` parameters — one-hot is Procgen's way of supplying that, not an assumption about discreteness baked into the FiLM math itself (the FiLM modulation formula `(1+gamma_a)*z_state + beta_a` doesn't care how `gamma_a`/`beta_a` were produced). Options considered: (a) feed the raw continuous action vector directly into `action_mlp` in place of the one-hot vector; (b) discretize/bin the continuous action before embedding, preserving one-hot; (c) drop the action-conditioning FiLM term entirely for the continuous case. Choice: **(a)**, raw action vector as `action_mlp`'s input — the mechanism's requirement is "a fixed-size vector," a continuous vector satisfies that as directly as a one-hot one, and (b)/(c) each destroy real information (binning) or remove a term the paper's own mechanism depends on (dropping it) for no stated reason. Falsifiable outcome: if this destabilizes the cluster-assignment loss (e.g., clusters collapse or embeddings stop separating by action) in a way traceable to the raw-vector action embedding specifically, that would show the choice was wrong and (b) becomes the fallback | `ext/ctrl_public/models.py:14,176-198` | recorded | Decided: (a), raw continuous action vector into `action_mlp`. Implemented in `rlgen/algos/ctrl/model.py` |

| 2026-08-14 | Re-read `algo.py:21-167` (helper functions) precisely before writing `algo.py`, since the earlier summary of this section predates this window and is exactly the kind of argument-shaped claim `porting-directive.md` §6 says to re-derive when load-bearing. Two findings, both orthogonal to the already-established "MYOW neighbor-selection is unrunnable" conclusion (don't change that plan, recorded for completeness). (1) **`compute_distance` computes cosine SIMILARITY (higher = more alike), not a distance** despite its name and the calling code's own variable name (`dist = compute_distance(...)`, `algo.py:209`) — confirmed by the formula (`cosine.T * inv_mag`, standard normalized dot product). The calling code then does `jax.lax.top_k(-dist, myow_k+1)` (`algo.py:210`) — the standard "negate-then-top-k" idiom for k-NEAREST-neighbor selection, which is only correct if `dist` were a real distance (smaller = closer). Since it's actually a similarity, `top_k(-dist,...)` selects the k prototypes LEAST similar (farthest) to each given prototype, the opposite of "neighbouring partitions." Moot for the hermetic module either way -- this feeds into the same `cluster_idx`/neighbor-selection path already established as unrunnable (undefined `scores_w_target`), so it doesn't change the decided same-partition-sampling simplification, but it's a second, independent piece of evidence that this section of the reference was never actually exercised end-to-end. (2) **`state_update`'s EMA direction, precisely**: `target_update(online, target, tau)` computes `new_target = tau*online + (1-tau)*target` — at the launch default `tau=ema_ctrl=0.95` (`train_ppo.py:60`), the target network moves 95% of the way to the online network EVERY update, not 5% -- nearly a hard copy with light smoothing, the OPPOSITE of the typical DQN/SAC target-network intuition ("small tau = slow-moving target"). Confirmed directly from the formula, not assumed from the variable name `tau` alone -- an easy sign/direction error to introduce when transcribing to PyTorch (`new_target.data = tau*online.data + (1-tau)*target.data`, not the reverse) | `ext/ctrl_public/algo.py:21-37` (`compute_distance`), `:98-126` (`state_update`/`target_update`), `:151-167` (`sinkhorn`) | recorded | Not decided (doesn't change any existing plan) — (1) is additional evidence for the already-decided same-partition MYOW simplification, not a new branch point; (2) is a transcription-precision note for `ctrl/algo.py`'s own `state_update`, not a divergence to resolve |

| 2026-08-14 | While designing `ctrl/algo.py`'s cluster-scoring computation: `algo.py:187-189` extracts `protos` via `apply_fn_embedding(params, jnp.eye(hidden), method=protos_fn)` -- feeding an identity matrix through the `protos` Dense layer to get its weight matrix, a standard JAX-functional-API trick since Flax modules have no direct weight-tensor handle outside `.apply()`. But `nn.Dense` has a trainable bias by default (`models.py:137-139` doesn't pass `use_bias=False`), and identity-through-a-biased-Dense computes `W + broadcast(bias)`, not `W` alone. `scores_v = v_clust @ protos` (`algo.py:198`) then uses this bias-contaminated matrix as a plain matrix multiply -- mathematically different from a normal forward call `protos_fn(v_clust) = v_clust@W + bias` (the contaminated version effectively adds `sum(v_clust) * bias` instead of a clean `+ bias`). Likely an unintended side effect of the extraction trick rather than a deliberate mechanism -- no stated reason for wanting sum-scaled bias contamination, and the trick's purpose (`compute_distance`/neighbor-selection, `algo.py:209`) doesn't need scores_v/scores_v_target to be affected by it at all. Not chased further (already deep past this module's reasonable budget on secondary details) — the hermetic module uses a clean `protos_fn(v_clust)` forward call for scoring, which is what a careful integrator reading the algorithm's intent (not the JAX-API workaround) would write, and is simpler to implement correctly than reproducing the artifact | `ext/ctrl_public/algo.py:187-189,198,202`; `models.py:137-139` | recorded | Decided: clean forward-call scoring in `ctrl/algo.py`, not the identity-trick's bias-contaminated matrix. Low-priority given the reference's own bias starts at zero-init and the effect scales with how far it moves — a small, plausible, not-chased-further divergence |

| 2026-08-14 | Task #22 (PPG's own reward normalizer) built. `reward_normalizer.py::RewardNormalizer`/`RunningMeanStd` is a BATCHED reference (arbitrary `(num_envs, nstep)` per call), while this project's trainer calls `normalize_reward` once per single step — the two shapes needed reconciling, not just transcribing formula-for-formula. Verified the reduction algebraically before writing code: substituting a batch of size 1 into `update_mean_var_count_from_moments` (`batch_mean=x`, `batch_var=0` since one point has zero variance, `batch_count=1`) collapses it to plain incremental Welford — algebraically IDENTICAL to `idaac/algo.py::_RunningReturnStd.update`'s formula, confirmed by direct substitution, not assumed from the two looking similar. Similarly, `backward_discounted_sum`'s `first`-gated formula (`ret[t] = reward[t] + (1-first[t])*gamma*prevret`) is equivalent to `idaac`'s own done-gated post-step reset when state is threaded consistently across steps — unrolled both by hand across an episode boundary to confirm the sequences match, not inferred from a resemblance. **Independently verified beyond the algebra**: ran the reference's own `RewardNormalizer`/`backward_discounted_sum` directly (a dependency-free verbatim copy of `reward_normalizer.py`, since its package `__init__.py` pulls in `mpi4py` — an unrelated dependency chain, not reflective of the math being checked) side by side with this project's transcription, 7 steps across 2 episode boundaries — exact match to floating-point precision. This is the first reward-normalizer verification this session that ran the actual reference code rather than only a hand-derived closed form (idaac's own test is closed-form only) | `ext/phasic-policy-gradient/phasic_policy_gradient/reward_normalizer.py`; `rlgen/algos/ppg/algo.py::_RunningMeanStd`/`Learner.normalize_reward` | decided | Built, red-green tested (mutated the normalizer to bypass itself, confirmed the closed-form test fails, restored, confirmed green), verified through the real trainer (`tests/test_onpolicy.py`'s pre-existing ppg-parametrized tests, all 6, unmodified). The one-time reference-run verification is NOT kept as a permanent test (it depended on a session-scratch file path, not durable across sessions/CI) — the hand-derived closed-form test is what stays in the suite, matching idaac's own pattern; this register entry is where the stronger, one-time verification is recorded instead of silently dropped |

| 2026-08-14 | Task #23 (IBAC-SNI's own reward normalizer) built. `common/env/procgen_wrappers.py::VecNormalize`/`RunningMeanStd` (`:274-334`) read directly, not recalled from the earlier "defaults normalize_rew=True" finding alone — same mechanism shape as `idaac`'s (`ret = ret*gamma + rews`, Welford update, clip-and-normalize, reset on done), but a DIFFERENT reference file than idaac's gen-rebuttal lineage, transcribed independently per `porting-directive.md` §1. Confirmed the reference's own train/measurement split a second way, not just by reading `train.py`'s `normalize_rew` flag: `common/storage.py:110-114` separately reads a raw `env_reward` the wrapper stashes into `infos` (`procgen_wrappers.py:327`) specifically for reporting — a third independent instance of this pattern (after `idaac`/`ctrl`), reinforcing rather than merely repeating the earlier finding. **Verified by actually running the reference**: a dependency-free extract of `procgen_wrappers.py`'s `RunningMeanStd`/`VecNormalize.step_wait` (isolating the reward-normalization slice from the unrelated `VecEnvWrapper` base class) run side by side with this transcription across an episode boundary — exact match to floating-point precision, same verification strength as `ppg`'s (not just a hand-derived closed form, actual reference code executed) | `~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/common/env/procgen_wrappers.py:274-334`; `common/storage.py:110-114`; `rlgen/algos/ibac_sni/algo.py::_RunningMeanStd`/`Learner.normalize_reward` | decided | Built, red-green tested (mutated to bypass, confirmed the closed-form test fails, restored, confirmed green), verified through the real trainer (`tests/test_onpolicy.py`'s pre-existing ibac_sni-parametrized tests, all 7, unmodified and passing). Same as `ppg`: the one-time reference-run verification is not kept as a permanent test (session-scratch path, not durable across sessions/CI) — the hand-derived closed-form test stays in the suite. Only `ctrl` (task #21) remains before all four on-policy baselines' reward normalizers are real |

| 2026-08-14 | Task #21 (CTRL's own reward normalizer) built — the last of the four on-policy baselines. `ext/ctrl_public/vec_env.py::VecNormalize`/`RunningMeanStd` (`:248-327`) re-read precisely before transcribing, not assumed from the earlier reward-normalizer audit's summary of it. Byte-identical formula shape to `ibac_sni`'s own reference (`ret = ret*gamma + rews`, Welford update, clip-and-normalize, `ret[news]=0`) — not a coincidence needing further chasing: DZ's `procgen_wrappers.py` and this file's `vec_env.py` both visibly descend from the same OpenAI Baselines `VecNormalize` original (same class name, same docstring, same variable names `ob_rms`/`ret_rms`/`clipob`/`cliprew`), so the shared shape is lineage, not independent convergence — still transcribed as two separate pieces of code per `porting-directive.md` §1, each cited against its own file. The train/measurement split confirmed a THIRD independent way for this baseline specifically (after `evaluate_ppo.py`'s `normalize_rewards=False` and `VecMonitor`-inside-`VecNormalize` wrapping order, both already recorded): this entry re-confirms both of those still hold, no new mechanism found. **Verified by actually running the reference**: a dependency-free extract of `vec_env.py`'s `RunningMeanStd`/`VecNormalize.step_wait` run side by side with the transcription across an episode boundary — exact match to floating-point precision, same verification strength as `ppg`/`ibac_sni`'s | `ext/ctrl_public/vec_env.py:248-327`; `rlgen/algos/ctrl/algo.py::_RunningMeanStd`/`Learner.normalize_reward`; `rlgen/algos/ctrl/config.py` | decided | Built, red-green tested (mutated to bypass, confirmed the closed-form test fails, restored, confirmed green), verified through the real trainer (`tests/test_onpolicy.py`'s pre-existing ctrl-parametrized tests, all 5, unmodified and passing). **All four on-policy baselines (idaac, ppg, ibac_sni, ctrl) now have real, independently-transcribed, reference-verified reward normalizers** — tasks #20-23 all complete. `onpolicy_ext.py::CTRLLearner`'s own guard (the dead, unreachable class) is untouched and still tested separately (`test_onpolicy_ext_ctrl_learners_own_guard_is_still_a_no_op`) since it's a genuinely different object from the live `ctrl/algo.py::Learner`; task #24 tracks that whole file's eventual deletion |

| 2026-08-15 | Stage 3 scoping (`porting-directive.md` §3, reference-execution diffing): before diffing, checked whether `RL-ViGen-upstream/train.py`'s own native robosuite path shares any code with this project's `rlgen/envs.py`. It does, exactly: both call the identical `wrappers.robo_wrapper.robo_make` function (`envs.py:157`, `train.py:74`). This means the env-CONSTRUCTION seam is single-provenance, not independently ported — so a naive per-step diff there would only re-surface `docs/PREMISES.md` P1 (upstream's own `train.py` never passes `mode`/`scene_id` to `robo_make` at all, silently building a `train`-regime env regardless of what scene was requested), which is already found, patched, and pinned by `test_eval_modes_actually_look_different_from_train` — re-deriving it via diffing would be redundant work, not a new finding. **Redirected the diff to the seam P1-P5 don't already cover**: for the 5 baselines that import `RL-ViGen-upstream/algos/*.py` directly (drqv2/svea/sgqn/curl/drq — literally the same Python file, not a port, so no algorithm-code divergence is even possible), the open question is whether `rlgen/replay.py` feeds that shared algorithm code the same conventions `RL-ViGen-upstream/replay_buffer.py` does. Read both files in full (190 lines each). Found the seam immediately: `RL-ViGen-upstream/wrappers/robo_wrapper.py::Gym2DMC.step` (`:35-40`) sets `discount=0.0` unconditionally whenever `done` is true, no branch for termination-vs-timeout — and since Door/Lift only ever end by time limit, this fires every episode, and flows straight into `RL-ViGen-upstream/replay_buffer.py::ReplayBuffer._sample`'s n-step accumulation (`:159`), zeroing the bootstrap at every episode boundary. `rlgen/replay.py::FrameReplay.sample` deliberately does NOT zero discount on `done` (already correctly reasoned in its own comment: "Door/Lift never terminate early... treating a time limit as a terminal state biases every value estimate downward") — but `docs/PREMISES.md` P4's existing text credited this fix only against "the earlier single-file script" (a sibling project's draft), not against RL-ViGen-upstream's own shipped code, which has the identical bug. `wrappers/dmc.py::ExtendedTimeStepWrapper` (`:46-54`) just propagates whatever `discount` the underlying env reports — confirmed NOT itself the source, `Gym2DMC.step` is | `RL-ViGen-upstream/wrappers/robo_wrapper.py:35-40`; `RL-ViGen-upstream/replay_buffer.py:142-160`; `RL-ViGen-upstream/wrappers/dmc.py:46-54`; `rlgen/replay.py::FrameReplay.sample` | decided | `docs/PREMISES.md` P4 corrected in place: `time_limit_handling: "truncate_with_bootstrap"` is a deliberate, first-class `porting-directive.md` §4 ADAPTATION from what RL-ViGen-upstream's own code (and any published numbers produced under that exact commit) actually does, not merely a repair of an inferior third-party draft. The reasoning for making the deviation is unchanged and still correct; only the claim about what it diverges from was wrong, per §6's own rule that a changed claim earns no credit from prior verification. No code changed — this is a correction to the record, verified by reading the actual reference files, not by running a diff harness (which was scoped out as redundant with already-fixed P1 for the env-construction seam specifically) |

| 2026-08-15 | Stage 3, `alda`'s own runnable reference (`ext/ALDA_Official`, separate lineage from RL-ViGen-upstream — `registry.py`'s own note: "Algorithm files copied from ../gen-rebuttal/vigen-idaac"). `curl`/`drq` in this project both use `_drqv2_family`, i.e. RL-ViGen-upstream's own backbone-ported agents on the SAME `rlgen/replay.py`/`rlgen/envs.py` seam just checked above — already covered by the prior entry, no separate check needed. `alda` is the one baseline on a genuinely different, not-yet-checked reference. Traced its truncation handling precisely: `alda_trainer.py:616`, `done_bool = 0 if episode_step + 1 == self.env.env._max_episode_steps else float(done)` — computed AT THE TRAINER LEVEL from the step counter (not trusted from the env's own `truncated` flag, which the `obs, reward, done, _, _ = env.step(action)` unpacking at `:615` discards entirely) — then presumably passed into the buffer's `add()`. Traced the buffer itself (`dmcontrol_generalization_benchmark/src/utils.py::ReplayBuffer.add`, `:113-121`): stores `not done` directly with no further logic of its own — confirming the truncation-awareness lives entirely at the trainer call site, already read in full. **This CONFIRMS rather than contradicts** `rlgen/replay.py::SacView`'s own "not_done always 1" simplification: ALDA's own reference implements the general form (bootstrap through a time-limit cutoff specifically, respect a genuine `done` otherwise), which — given the already-established premise that Door/Lift never terminate early — reduces to exactly "always bootstrap" for these two tasks. A third, independent corroborating source for the truncation-bootstrap principle (after the sibling project's `DISCREPANCY_MATRIX.md` B31/truncation bullet already cited in P4, and RL-ViGen-upstream's own code found to have the OPPOSITE bug in the entry immediately above this one) — this time the method's own official paper reference doing the correct thing, not another project's bug report about it | `ext/ALDA_Official/trainers/alda_trainer.py:615-616,460`; `ext/ALDA_Official/dmcontrol_generalization_benchmark/src/utils.py:113-121`; `rlgen/replay.py::SacView.sample` | decided | Confirmed, no code change needed. `rlgen/replay.py::SacView`'s docstring already states the correct reasoning independently — this entry adds a verified citation to ALDA's own reference actually doing the general form, strengthening rather than establishing the claim |

| 2026-08-16 | **Surfaced by external review, not by self-audit.** `rlgen/algos/ctrl/{model,algo}.py`'s docstrings claim "transcribed from Flax to PyTorch" and cite the T1/T2 parity verification from `docs/REGISTER.md`'s 2026-08-14 entry. That verification was run against a DIFFERENT artifact — the `agy`-built scratch prototype (`.../scratchpad/agy/ctrl_pytorch_attempt/{models_torch,algo_torch,buffer_torch}.py`), independently re-run and confirmed by me at the time. The actual shipped code in `rlgen/algos/ctrl/` was written afterward, informed by that prototype and by the reference itself, but is a MATERIALLY DIFFERENT artifact (the `protos` clean-forward-call fix instead of the identity-matrix trick, at minimum — a deliberate, already-recorded divergence from the prototype). It was never itself run through `test_parity.py` or any equivalent numerical comparison against JAX. Checked directly, 2026-08-16: zero files under `rlgen/algos/ctrl/` or `tests/test_ctrl_*.py` import `jax`/`flax` — every existing CTRL test is internal self-consistency only (shapes, gradient reachability, hand-derived closed forms, red-green mutation, deterministic-purity) with no numerical reference to compare against. A concrete, structural confirmation this isn't just an unverified-but-probably-fine gap: `models_torch.py::Impala.forward` explicitly permutes NCHW→NHWC before flatten "for exact weight transplant parity" with Flax's own flatten order (a comment in the prototype says so directly); `rlgen/algos/ctrl/model.py::Impala.forward` does a plain `.flatten(1)` in PyTorch's native NCHW order, no permutation. This means the shipped module, AS WRITTEN, could not currently pass a weight-transplant T1 check even if one were run — not "unverified," actively structurally incompatible with the verification method that WAS used on the prototype | `rlgen/algos/ctrl/model.py:88-113` vs. `.../ctrl_pytorch_attempt/models_torch.py::Impala.forward` (the NHWC-permute comment); `rlgen/algos/ctrl/{model,algo}.py` docstrings (the overclaim) | recorded | Not decided/not yet repaired. Per `porting-directive.md` §0/§1: this module is a CONSTRUCTION (informed by, and partially inheriting reasoning from, a separately-verified prototype and the reference itself; NOT a transcription — JAX-to-PyTorch necessarily crosses the framework boundary §1 names as the case that "converts a faithful-transcription artifact into a constructed one" absent a reason beyond matching the local stack). The honest current tier is **T4 at best (structural accounting only) — possibly undeclared until this is fixed**, not the T1/T2 the docstrings currently imply by citing the prototype's verification as if it transferred. Per §6: "verification attaches to a claim, not to an artifact... code that predates a reframe earns no credit from having existed before it" — except here it's not even the same artifact predating a reframe, it's a genuinely different artifact inheriting credit from a different one's check. Next actions, not yet done: (1) correct the docstrings to state the actual, honest provenance and tier; (2) either fix the flatten-order divergence and build a real transplant-based T1 test against the ACTUAL shipped model, or explicitly cap the declared tier at T4 and say why T1 wasn't pursued; (3) audit `algo.py` the same way — `_sinkhorn`, `_cos_loss`, `_l2_normalize`, `state_update`'s EMA direction were checked via hand-derived closed forms and red-green (real, but not the same as numerical agreement with the JAX computation on the same input) |

| 2026-08-16 | Resolution of the entry immediately above (CTRL's `Impala` encoder now has a real, passing T1 check, `tests/test_ctrl_parity.py`) — built by fixing two real bugs, not by lowering the bar. (1) Flatten order: added an explicit NCHW→NHWC permute before flatten, matching Flax's own `reshape` on an NHWC tensor. (2) Maxpool padding VALUE, found only by running the test and debugging the failure, not by reading: an initial fix used explicit `F.pad`-based Flax "SAME" padding (correctly asymmetric per the formula) but with the default pad value 0.0 — which is right for the conv calls (zero contributes nothing to a weighted sum) but silently wrong for max-pooling, where a 0-valued pad can win the max whenever a window's genuine pre-ReLU values are all negative. The first attempt at the real test failed with a broadly-spread (not edge-localized) disagreement across the whole spatial grid — ruled out a simple asymmetry-direction bug by checking the diff pattern per row/column, isolated to the maxpool step specifically by comparing intermediate activations via Flax's own `capture_intermediates=True` (conv0's raw output matched flax to 8e-7; the divergence appeared only after the maxpool). Fixed to `value=float("-inf")` on the maxpool's own padding call, matching what Flax's SAME-padding maxpool implicitly does. Both fixes are in `rlgen/algos/ctrl/model.py`, not test-only workarounds — real correctness fixes, since a zero-padded max-pool is a genuine, if usually small, error whenever it fires, not merely a testing inconvenience | `rlgen/algos/ctrl/model.py::_same_pad_2d`, `Impala.forward`; `tests/test_ctrl_parity.py` | decided | CTRL's `Impala` encoder: real T1 (forward agreement under weight transplant), verified against the actual shipped class, not a prototype. Scope stated precisely, not overclaimed: encoder only. `CTRLPolicy`'s MLPs/`concat`/`protos`/FiLM conditioning and all of `algo.py`'s training-loop math (`_sinkhorn`, `_cos_loss`, `state_update`) remain T4 (structural accounting), not yet given an equivalent numerical check — a real, bounded next step, not claimed as done by association with the encoder's result |

| 2026-08-16 | **Provenance re-grounding**, on an instruction to discard carried-forward assumptions and return to the original implementations as they stood before any work here. Read the actual primary sources fresh rather than any of this session's own prior descriptions: `gen-rebuttal/ext/alda/Nd_ln.py` (DZ's own original, 1079 lines, never before read directly this session — only described via other files' docstrings), `~/Downloads/faithful_nd_ln.VERIFIED.py` (947 lines, a corrected reference the user produced with another AI against `Nd_ln.py`'s documented problems, living OUTSIDE both `many-gens-rl-vigen` and `gen-rebuttal` proper, importing `gen-rebuttal/vigen-idaac/vigen_alda.{agent,buffer,config}` directly). Confirmed two of `Nd_ln.py`'s cited defects myself, directly, not from description: `evaluate_policy(env, num_episodes)` (`Nd_ln.py:625`) takes ONE env object, not a scene list — genuinely single-scene; `rb.add(obs, next_obs, actions, rewards_scaled, terminations, ...)` (`:839`) passes `terminations` straight through with no truncation distinction — genuinely no bootstrap-through-timeout. Then diffed THIS project's `rlgen/algos/alda/{agent,buffer,config}.py` against the sibling's own `vigen_alda/{agent,buffer,config}.py` byte-for-byte, not trusted from any docstring's "byte-identical" claim: **`agent.py` — genuinely byte-identical, `diff` exit 0, confirmed directly.** `buffer.py` — **does not exist in `rlgen/algos/alda/` at all**: this project's ALDA does not use the sibling's own audited `vigen_alda.buffer.FrameStackReplayBuffer` (with its own `BufferVerifier` transition-alignment guarantee) — it runs through this project's own separate `rlgen/replay.py::SacView`/`FrameReplay`, a different buffer implementation entirely, already independently checked this session for the one property that matters most (truncation-bootstrap correctness, confirmed correct against `ALDA_Official`'s own `alda_trainer.py:616` separately). `config.py` — 37 lines of real diff, centered on `utd`: the sibling's own `vigen_alda/config.py` dataclass default is STILL `1.0`, marked `[AC-MISREAD, see below]` in its own comment but never corrected in the class itself; `faithful_nd_ln.VERIFIED.py`'s own CLI overrides it to the correct `0.25` at the script level (confirmed: `--utd", type=float, default=0.25`) without ever updating the underlying config class; this project's `rlgen/algos/alda/config.py` independently bakes `0.25` directly into the dataclass default, citing the sibling's own empirical finding (`STATE.md D1`, 141k-frame divergence at `utd=1.0`) | `gen-rebuttal/ext/alda/Nd_ln.py:625,839`; `~/Downloads/faithful_nd_ln.VERIFIED.py:141-176,199`; `gen-rebuttal/vigen-idaac/vigen_alda/{agent,buffer,config}.py` vs. `rlgen/algos/alda/{agent,buffer,config}.py`, direct `diff` | recorded | Not a bug in this project's alda specifically — `agent.py` is genuinely faithful (verified, not assumed), `config.py`'s divergence is a deliberate, cited, arguably-more-correct choice, and `SacView`'s truncation handling was already separately verified against `ALDA_Official` directly. What's actually new here: (1) the "byte-identical" claim was true for `agent.py` specifically and had never been checked for `buffer.py`/`config.py`, where it does NOT hold; (2) `buffer.py` never existing in this project means the sibling's own audited `BufferVerifier` guard (which "catches [transition-alignment bugs] in a run rather than in an audit," per `faithful_nd_ln.VERIFIED.py`'s own BUG-21) has no equivalent running in this project's own pipeline — worth a genuine, separate check, not yet done. Decided: this entry stands as the corrected, precise record; no code change needed unless the `BufferVerifier`-equivalent gap turns out to matter |

| 2026-08-16 | **`ibac_sni`'s base term was misdescribed in three separate places, found on first contact with the artifact — before a single line of the rebuild was written.** The rebuild was scheduled by `docs/STEP-ZERO.md`'s own handoff block, which stated: base = `~/Downloads/train-procgen-pytorch_example/`, "clean `joonleesky/train-procgen-pytorch`, remote-verified", delta = **5 files** (`agents/ppo.py`, `hyperparams/procgen/config.yml`, `train.py` modified; `agents/ppo_ibac.py`, `common/policy_ibac.py` new). `docs/ORIGINAL_LOCATIONS.md`'s `~/Downloads` inventory table says the same thing in the same words ("Clean ... — **the exact base DZ edited**. Delta to DZ's copy is 5 files"). Both are this session's own earlier output. Checked against the artifact: **(1) the tree is not clean.** `git status --porcelain` in `train-procgen-pytorch_example` reports ` M agents/ppo.py`, ` M train.py`, and an untracked `experiments/`. The remote WAS verified (`git@github.com:joonleesky/train-procgen-pytorch.git`) and the commit is real (`1678e4a9e2cb8ffc3772ecb3b589a3e0e06a2281`, 2020-09-10) — **remote-verified was silently read as working-tree-clean**, two different properties, and only the first was ever checked. **(2) The modification is not benign and not ours:** `_example/agents/ppo.py` carries an unrelated third-party experiment — a `find_principal_component` method doing an SVD of the hidden-state batch, a `noise_amplitude` parameter threaded through `optimize()`/`train()`, Russian-language comments (`# Сливаем первые два измерения`, `# Нашли главную компоненту`) — and it **does not parse**: `principal_vector = self.find_principal_component( self.storage.  ).to(self.device)` raises `SyntaxError: invalid syntax` under `ast.parse`. A file that cannot be imported was recorded as a pristine base for four days. **(3) The delta is 4 files, not 5.** `git show 1678e4a:agents/ppo.py` diffed against DZ's `agents/ppo.py` returns **exit 0, byte-identical** — DZ did not touch `ppo.py` at all. It appeared in the delta only because the diff was taken against the dirty working tree instead of against the commit. The real delta: `hyperparams/procgen/config.yml` (+45 lines: `easy-ibac`, `easy-200-ibac` blocks with `beta: 0.0001`, `sni: True`, `sni_lambda: 0.5`), `train.py` (+11/-3: `IBACPolicy` import, `algo` read hoisted above the policy branch, `ppo_ibac` dispatch with a different constructor signature) modified; `agents/ppo_ibac.py` (203 lines), `common/policy_ibac.py` (78 lines) new; plus 5 non-code launch `.sh` scripts. **(4) Ordering evidence that reverses the assumed lineage:** `ppo_ibac.py::evaluate()` (`:160-203`) is a near-verbatim copy of the *dirty* `_example/agents/ppo.py::evaluate()` — same `ProcgenEnv` construction, same wrapper stack, same `write_summary` keys — with the Russian comments translated to English. So `IBAC_SNI_torch` was derived *after*, and partly *from*, the modified `_example` tree; the inventory's "the exact base DZ edited" has the dependency arrow pointing the wrong way. **(5) The class of DZ's two new files was never established.** Their header comments cite the paper by name and section (`# Igl et al., "Generalization in RL with Selective Noise Injection and Information Bottleneck", NeurIPS 2019.`) and describe the mechanism in prose, which is the signature of a re-derivation from the paper, not a transcription of code. Meanwhile the actual author-released original **is on disk and was never consulted for this baseline**: `ext/IBAC-SNI`, `microsoft/IBAC-SNI` @ `6b3a58b`, and critically it contains the authors' **own PyTorch implementation** (`torch_rl/bottleneck.py`, `torch_rl/model.py`) alongside the TF/CoinRun path — so the framework-boundary argument that would justify treating a PyTorch re-derivation as the best available base **does not hold here**. This is `docs/STEP-ZERO.md` gate 1 (base term stated as fact) and gate 4 (no inherited claim used as settled, including my own) both catching a real error on their first live application, which is the only evidence that they do anything | `~/Downloads/train-procgen-pytorch_example` (`git status --porcelain`, `git rev-parse HEAD`, `agents/ppo.py` under `ast.parse`); `git show 1678e4a:agents/ppo.py` vs `~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo.py` (`diff`, exit 0); `~/Downloads/IBAC_SNI_torch/train-procgen-pytorch/agents/ppo_ibac.py:160-203` vs `~/Downloads/train-procgen-pytorch_example/agents/ppo.py::evaluate`; `ext/IBAC-SNI` (`git remote -v`, `6b3a58bfc23a1e8e2bbb669a4e96e24dabc32d71`), `ext/IBAC-SNI/torch_rl/{bottleneck,model}.py`; `docs/STEP-ZERO.md` handoff block; `docs/ORIGINAL_LOCATIONS.md` `~/Downloads` inventory row | recorded | Decided, and the rebuild's shape changes because of it. The base term for `ibac_sni` is **not** the `_example` working tree — it is **`joonleesky/train-procgen-pytorch` @ `1678e4a`, extracted via `git show`**, which is reachable, parses, and is genuinely pristine. DZ's `train.py`/`config.yml` edits are legitimate N-term wiring and carry over cited. DZ's `ppo_ibac.py`/`policy_ibac.py` are **NOT** promoted to reference status: the IBAC-SNI *semantics* come from `ext/IBAC-SNI` (the authors' own code, including their own PyTorch bottleneck), and every point where DZ's re-derivation diverges from it gets recorded rather than inherited. Docs corrected in place, same date, both files — not appended to |

| 2026-08-16 | **Reconnaissance sweep over every reference working tree — the step whose absence produced the `ibac_sni` base-term error earlier today.** That error was diagnosed here as staleness; `git log -S` shows both the `ORIGINAL_LOCATIONS.md` inventory row (`24b5f300`) and the `STEP-ZERO.md` handoff line (`b0c345df`) were written **2026-08-16, the same day**. So nothing decayed: the `~/Downloads` sweep verified *remotes* and never opened a *working tree*, and the claim was false on the day it was written. Different failure, different fix — a missing exploratory step, not degraded guidance. Sweep run over all 14 git-backed references in `ext/`: dirty tracked files, untracked files, and `ast.parse` on every `.py`. **Result: `ext/` is clean.** Zero dirty tracked files anywhere except `ctrl_public` (one modified `.DS_Store`, not code); zero parse failures anywhere; `idaac` has 2 untracked files, both the paper PDFs (`raileanu21a.pdf`, `raileanu21a-supp.pdf`), which are the source of Appendix E's continuous DMC hyperparameters. **Remotes confirmed first-party for all 14**, checked rather than assumed after the user raised the specific risk that an `ext/` entry might be gen-rebuttal's own adaptation rather than the authors' code: `idaac` -> `rraileanu/idaac` @ `2fe3020` (Raileanu is IDAAC's own author), `ALDA_Official` -> `SumeetBatra/ALDA_Official` @ `8dcc968`, `phasic-policy-gradient` -> `openai/...` @ `7295473`, `IBAC-SNI` -> `microsoft/...` @ `6b3a58b`, plus curl/drq/drqv2/rad/SGQN/SECANT/ctrl_public/dmcontrol-generalization-benchmark/robosuite. The material distinction this draws: **`ext/` is the curated read-only reference folder and it held up; `~/Downloads` is uncurated and did not.** The dirty, non-parsing tree was in `~/Downloads`, and the inventory that blessed it treated the two locations as interchangeable | `ext/*/.git` (`git remote -v`, `git status --porcelain`, `ast.parse` over 14 trees); `git log -S 'the exact base DZ edited' -- docs/ORIGINAL_LOCATIONS.md`; `git log -S 'train-procgen-pytorch_example' -- docs/STEP-ZERO.md` | decided | Reconnaissance becomes a precondition on `docs/STEP-ZERO.md` gate 1: a base term is not stated as fact until the working tree has been opened (`git status --porcelain`, not just `git remote -v`) and every file parsed. Recorded as an added step, explicitly NOT as evidence the standard is wrong — the two are different failures and conflating them would licence rewriting guidance that is not implicated |

| 2026-08-16 | **`[IK]` — the reference `idaac`'s continuous head cites as the thing it deviates from — is not on this disk.** `rlgen/algos/idaac/config.py:152` reads `init_log_std: float = -1.0  # [OURS] DEVIATION from [IK], which uses AddBias(zeros)`, and `rlgen/algos/idaac/model.py:84` repeats it in prose. `[IK]` is Kostrikov's `pytorch-a2c-ppo-acktr`, IDAAC's own upstream lineage. Searched: no such directory anywhere under `~`, and the only `AddBias` hits in this workspace are those two of our own comments plus one in `gen-rebuttal/ext/claude-july-29/config.py` (itself an AI-produced file in a sibling project's `ext/`, not an original). Separately confirmed by reading IDAAC's own repo: `ext/idaac/ppo_daac_idaac/distributions.py` defines **only** `FixedCategorical` and `Categorical` — no `DiagGaussian`, no `AddBias` — and `model.py:313,360` wires `Categorical` unconditionally. So IDAAC's own release contains no continuous head at all, and the claim about what the continuous head deviates *from* rests on a repo that is not present and was never read in this session. Not necessarily wrong — Kostrikov's `DiagGaussian` does use `AddBias(zeros)`, which is why `ibac_sni`'s own head cites baselines' equivalent — but it is an **argument-shaped citation to an absent artifact**, which `porting-directive.md` §6 says cannot be inherited as settled | `rlgen/algos/idaac/config.py:152`, `rlgen/algos/idaac/model.py:84`; `ext/idaac/ppo_daac_idaac/distributions.py`, `model.py:313,360`; filesystem search for `*a2c-ppo-acktr*` and for `AddBias` | recorded | Not decided. Bears directly on the planned `idaac` base-first rebuild: the base (`ext/idaac`) settles the encoder, the storage and the PPO/DAAC/IDAAC losses, but **cannot settle the continuous head**, because it does not have one. That part stays an adaptation with no first-party reference, exactly as `docs/ORIGINAL_LOCATIONS.md` already says for `idaac` overall — the new information is that the *specific* `[IK]` citation used to justify its form is uncheckable here. Either fetch `pytorch-a2c-ppo-acktr` and read it, or restate the claim without leaning on it |

| 2026-08-16 | **Process defect, self-reported: commit `b49fd26a` landed with the test suite red.** `tests/test_contract.py::test_generated_baseline_files_are_current` runs `tools/gen_baselines.py --check`, which regenerates `baselines/<name>/README.md` from `registry.py`'s notes; the `ibac_sni` registry note was rewritten in that commit and the generated README was not. The commit message said "Full suite green at the previous run", which was literally true and is exactly the wrong shape of claim — a green result carried across an edit that invalidated it. The immediate cause is that the verifying run timed out at 120s and I committed on the strength of an earlier one instead of rerunning in the background and waiting. Fixed by running `tools/gen_baselines.py`; `--check` now exits 0 | `tests/test_contract.py:390-393`; `tools/gen_baselines.py`; commit `b49fd26a` | decided | Recorded rather than quietly fixed, per `docs/STEP-ZERO.md` gate 6. The generalisable part is not "rerun tests" — it is that **a verification result is scoped to the tree that produced it**, and an edit after the run voids it no matter how small the edit looks. `registry.py`'s notes field looked like documentation; it is a generated-artifact input |

| 2026-08-16 | **`ibac_sni`'s base-first rebuild verified end-to-end before the pattern was replicated.** The rebuild replaced the entire policy API and the suite stayed green across it, which is not the same claim as the training loop working — a module can pass every unit test and fail to train. Ran `bash baselines/ibac_sni/train.sh Door 0 --smoke`: **3,000 frames, 11 updates, 378s, exit 0**, evaluated across all 10 scenes in both `train` and `eval-easy` modes, no non-finite loss, checkpoint written. The two risks flagged in advance both failed to appear: `nr_samples=12` did not make updates unaffordable (the CNN and bottleneck each run once; only the two small heads see 12x), and `softplus(rho-5.0)` starting the encoding near-deterministic did nothing degenerate over 11 updates. Deliberately run BEFORE `ppg`/`alda`/`idaac`, so a defect in the *pattern* would surface once instead of four times. **What it does not establish, stated because the numbers invite over-reading**: 3,000 frames is ~1/300th of one baseline-seed; the `train` 22.3 vs `eval-easy` 14.8 figures are noise around an untrained policy at 1 episode per scene, not a generalization gap, and reading one into them would be the no-sensitivity design error `docs/RIGOR.md` names. Algorithm tier unchanged at **T4**. Throughput not usable for budgeting: the 378s covers 3,000 training frames plus ~10,000 further env steps of evaluation, so the implied ~7.9 fps is not comparable to `docs/compute.md`'s 12.16 fps training figure | `docs/VALIDATION.md` (2026-08-16 section); `logs/rl_vigen_robosuite/Door/ibac_sni/eval-easy-seed0` | decided | The base-first pattern is verified as *runnable*, which is what gated replicating it. Proceeding to `ppg` |

| 2026-08-16 | **`ppg`'s reference cannot produce a continuous policy as shipped, and contains a dead function that looks exactly like it can.** Found during base-term reconnaissance for the `ppg` rebuild, before any file was written. `ext/phasic-policy-gradient/phasic_policy_gradient/distr_builder.py::tensor_distr_builder` routes `Discrete(2)` to Bernoulli and `Discrete` to Categorical and **raises `ValueError` on everything else** — there is no continuous branch. A `_make_normal` does exist (`distr_builder.py:11-14`) and is **dead code**: grepped the entire package, it has **no caller**, and `Real` is imported only for the *observation* type (`impala_cnn.py:9`, `REAL = Real()`), never for the action distribution. Named as a trap rather than a curiosity, because a hurried reading would seize on `_make_normal` as "PPG's own continuous head" and inherit two defects at once: it hardcodes `scale=1.0` with no learnable log-std, and the authors flagged that themselves with `warnings.warn("Using stdev=1")` — a warning that only ever fires on a path nothing reaches. Reconnaissance also confirmed the base itself is exactly what the record claimed, unlike `ibac_sni`'s: `openai/phasic-policy-gradient` @ `7295473`, **0 dirty tracked files, 0 untracked, 19/19 files parse, PyTorch throughout** | `ext/phasic-policy-gradient/phasic_policy_gradient/distr_builder.py:5,11-14,22-38`; `impala_cnn.py:8-9`; `git status --porcelain` + `ast.parse` over the tree | recorded | `ppg` joins `idaac` and `ibac_sni`: **no continuous version exists in the authors' release**, so the policy head is an authored `porting-directive.md` §4 adaptation with no first-party reference, and must be recorded as such rather than borrowing credibility from the faithful base around it. The base still settles the encoder, the PPO phase, the auxiliary phase and `beta_clone` distillation, the rollout container, and the reward normalizer. Separately noted for the rebuild: `impala_cnn.py` initialises via `tu.NormedConv2d(..., scale=s)`, a different scheme from `train-procgen-pytorch`'s xavier — **nothing transfers from `ibac_sni`'s base by analogy** |

| 2026-08-16 | **`ppg`'s encoder has the project's first real T1 against a same-framework reference**, and building it surfaced two defects in the *verification*, not the code. Both sides are PyTorch (unlike `ibac_sni`, whose reference is TF 1.x and stays T4), so a genuine weight transplant is available: `tests/test_ppg_base_parity.py` loads the vendored `ImpalaCNN` (`_upstream_7295473/impala_cnn.py`), copies its `state_dict` into ours with **no key remapping** (module names are kept identical on purpose), feeds the same pixels, and requires agreement at 1e-5 — at both 84x84x9 and the reference's native 64x64x3. Red-green confirmed against two mutations: flattening in NHWC order instead of C-major, and sharing one encoder between policy and value. **DEFECT 1, in the test harness: a blanket `except Exception: pytest.skip(...)` in the fixture turned a real import failure into a silent pass.** The vendored tree failed to import for an ordinary reason (`torch_util.py:15` does `from . import logger`, which was deliberately not vendored) and **every parity test skipped while the run reported green** — pytest prints `s`, and `s` scans as a pass at a glance. Removed the guard entirely: if the base cannot be loaded, the file has no meaning and must say so loudly. **DEFECT 2, in the tier itself: T1 is structurally blind to initialisation.** Mutating `CnnDownStack.firstconv` to take the per-stack `scale` (the reference gives it the default; only the residual blocks receive it) left the transplant test **green**, because transplanting overwrites every weight before the forward pass. Init is a real part of the method here — PPG uses normalized-fan-in (`torch_util.py:327-351`, rescale each output unit's weight vector to L2 norm `scale`), nothing like the `xavier_uniform_` its neighbour `ibac_sni` inherits — so an **init-parity** check was added: construct both under one seed, compare tensors. It catches the mutation T1 cannot see | `tests/test_ppg_base_parity.py`; `rlgen/algos/ppg/model.py`; `_upstream_7295473/{impala_cnn,torch_util}.py` | decided | `docs/STEP-ZERO.md`'s T1 definition amended in place to carry the blind spot and the pairing rule: where the reference is executable, pair T1 with init-parity; where it is not, declare "T1-forward-only" rather than letting T1 imply more than it establishes. **Overclaim by me, caught and corrected within the hour of writing it**: this entry first said the rebuild "closes PPG's dual-network gap". It does not — `git show HEAD:rlgen/algos/ppg/model.py` shows the discarded construction already built `pi_enc` and `vf_enc` as separate encoders (`:126-127`), already had the unclipped value loss, and already omitted gradient clipping with two comments citing the reference for it. The old module was better than I implied while replacing it, and I asserted otherwise without running the one command that checks. What the rebuild actually buys for `ppg`, stated without inflation: textual descent from the reference where there was none (0%); the exact `NormedConv2d`/`NormedLinear` per-stack init, which the construction approximated with its own `_normed_linear` helpers but was never checked against the reference; a real T1 transplant; and an init-parity check. Separately noted: `docs/FAITHFULNESS.md` §ppg is itself stale on this point — it still describes the repo as "not having a separate value network to train in the first place", which stopped being true at the 2026-08-14 hermetic rebuild |

| 2026-08-16 | **A new test corrupted 14 existing ones, and both files passed in isolation.** `tests/test_ppg_base_parity.py` must import the vendored PPG reference, which needs `mpi4py` (absent here) and a synthetic package for its relative imports. Both were installed straight into the global `sys.modules` at fixture time and **never removed**. Running the file alone: green. Running `test_real_env.py` alone: green. Running the suite: **14 failures/errors in `test_real_env.py`**, with tracebacks pointing at robosuite internals and nothing pointing back at the parity test. Found only because the full suite was re-run after the last edit rather than trusting the targeted run — the discipline that already caught one stale-green commit today (`b49fd26a`). Fixed by snapshotting `sys.modules` and restoring it in the fixture's teardown. **Generalisable shape:** a test that mutates global interpreter state is not a local object. Its blast radius is the whole session, and the damage surfaces as *other people's* failures, which is the hardest kind to trace — worse than a missing test, because a missing test at least fails where it is | `tests/test_ppg_base_parity.py` (`vendored` fixture); observed against `tests/test_real_env.py` | decided | Fixed and verified: the two files now pass together. Two related anti-patterns landed in the same file within minutes of each other — the blanket `pytest.skip` (entry above) and this — both of which make a red run look green. Worth noting they were introduced by the same instinct: making the harness *cope* with an awkward dependency instead of making the awkwardness visible |

| 2026-08-16 | **I broke a verified component while claiming to preserve it.** `ppg/algo.py`'s module docstring stated that `_RunningMeanStd` and `normalize_reward` were "kept verbatim" from the discarded construction, argued explicitly as a gate-1 back-merge decision on the grounds that the transcription had been **verified 2026-08-14 by running the reference's own code side by side with it**. In fact only `_RunningMeanStd` was copied; `normalize_reward` was **rewritten from its description** — and the rewrite dropped `if done: self._ret = 0.0`, so the discounted return accumulated across episode boundaries for the whole run instead of resetting per episode. Caught by `tests/test_reward_normalizer.py::test_ppg_normalize_reward_matches_a_hand_computed_closed_form`, a closed-form check that exists precisely because this class of state bug produces entirely plausible numbers. Restored verbatim from `git show`. **The failure is exact and worth stating narrowly:** "carry it forward" means *copy the code*. Re-typing a verified component from its own docstring discards the verification while keeping the sentence that claims it — the artifact changed, the claim did not, and `porting-directive.md` §6 says a changed claim earns no credit from prior verification. The docstring asserting preservation is a claim like any other and was false the moment it was written | `rlgen/algos/ppg/algo.py::normalize_reward`; `tests/test_reward_normalizer.py:166`; `git show HEAD:rlgen/algos/ppg/algo.py` | decided | Restored and green. Second self-inflicted defect of the `ppg` rebuild after the overclaim about the dual-network gap, both of the same shape: **asserting something about code I had not run the one command to check.** Neither was caught by reading; both were caught by an existing test or by `git show` |

| 2026-08-16 | **`alda`'s 0% descent flag was investigated and DOES NOT HOLD — audit before rebuild, and the audit said don't rebuild.** `scripts/state.py` measures `rlgen/algos/alda/*` at ~100% descent from the sibling `gen-rebuttal/vigen-idaac/vigen_alda/` and **0% from `ext/ALDA_Official`** (`SumeetBatra/ALDA_Official` @ `8dcc968`), which is PyTorch — so unlike `ctrl` there was no framework crossing to excuse it, and it looked like the same failure as `ibac_sni`/`ppg`. Checked the mechanism rather than the number: the reference's disentanglement lives in `disentangle/latents/{associative,quantized,continuous}.py` plus `autoencoders/`, exposing `AssociativeLatent`, `QuantizedLatent`, `ContinuousLatent`, `OuterEncoder`. Ours exposes `Continuous`, `Quantized`, `OuterEncoder`, `build_latent_model` (`nets.py`) and `config.py:131` defaults `latent_model = "associative"` — the reference's own main path (`alda_trainer.py:15,226` constructs `AssociativeLatent`) — with `config.py:151-152` carrying an `[A]`-cited variant matrix mapping ALDA / ALDA (CG) to their paper sections. **Same concepts, same main path, renamed classes and a flattened layout.** That is the `ctrl` situation (a reimplementation whose text cannot descend because the structure differs), not the `ibac_sni` situation (a re-derivation written while a copyable base sat unread). **Decision: do not rebuild `alda` base-first.** The 0% is explained; rebuilding would discard a port that already tracks the reference's own variant structure, for provenance cosmetics | `ext/ALDA_Official/disentangle/latents/*.py`, `autoencoders/*.py`, `trainers/alda_trainer.py:15,226`; `rlgen/algos/alda/nets.py`, `config.py:131,151-152`; `scripts/state.py` | decided | **This is what "0% is a flag, not a verdict" is for** — the flag was raised by a measurement, investigated by reading, and withdrawn. Recording the withdrawal matters as much as recording the flag: a flag that is never withdrawn is not a flag, it is a verdict wearing one's clothes. **What remains genuinely open for `alda`, unchanged by this**: it has never been *numerically* checked against `ext/ALDA_Official` (concept-level agreement is not T1/T2/T3), and this project has **no `alda/buffer.py`** — it runs the shared `rlgen/replay.py`, so the sibling's own `BufferVerifier` transition-alignment guard has no equivalent running here. Both stay open |

| 2026-08-16 | **`idaac`'s base vendored and read; three details recorded that would not survive being assumed from the two baselines rebuilt just before it.** Base term stated as fact after gate-0 reconnaissance: `rraileanu/idaac` @ `2fe3020` (the paper's own author, checked by `git remote -v` because the specific risk was raised that an `ext/` entry might be a sibling project's adaptation rather than the authors' code), 0 dirty tracked files, 2 untracked entries both paper PDFs, all `.py` parse, PyTorch throughout. Vendored sha256-pinned at `rlgen/algos/idaac/_upstream_2fe3020/`. **(1) `model.py:145` hardcodes `nn.Linear(2048, hidden_size)`** — `32*8*8`, correct only for Procgen's 64x64; at 84x84 the stack gives 84->42->21->11 = `32*11*11 = 3872`. Same edit shape as `ibac_sni`'s E1, and the *opposite* of `ppg`, whose reference derives its own width and needed no edit — three baselines, three different answers to the same question. **(2) `Conv2d_tf` implements TF SAME padding including the asymmetric one-sided pad when the total is odd** (`F.pad(input, [0, cols_odd, 0, rows_odd])`); a plain `nn.Conv2d(padding=1)` is not equivalent at every input size. Wart worth naming: `BasicBlock` passes `padding=(1,1)` as a tuple, which can only ever fail the `== "VALID"` test, so the value is ignored and SAME is computed regardless. **(3) Init is `apply_init_` — `xavier_uniform_` on every `Conv2d`, zero bias** — the same family as `ibac_sni`'s base and NOT `ppg`'s normalized-fan-in. Three consecutive baselines, three distinct init schemes, which is exactly why `docs/STEP-ZERO.md`'s recorded T1 blind spot (a transplant overwrites init and cannot see it wrong) means this one needs its own init-parity check rather than inheriting confidence from `ppg`'s | `ext/idaac` (`git remote -v`, `git status --porcelain`, `ast.parse`); `_upstream_2fe3020/model.py:18-31,41-86,101-172`; `PROVENANCE.md` | recorded | Base term settled. It settles the encoder, buffer, DAAC/IDAAC losses and the published hyperparameter grid — but **not the continuous policy head**: `distributions.py` defines only `FixedCategorical`/`Categorical` and `model.py:313,360` wires `Categorical` unconditionally. Head stays an authored §4 adaptation, graded NOT INTRINSIC, and must not borrow credibility from the faithful base around it. Compounding that: `config.py:152`/`model.py:84` justify the head's form as a `DEVIATION from [IK]` (Kostrikov's `pytorch-a2c-ppo-acktr`), which is not on this disk and cannot currently be checked by anything |

| 2026-08-16 | **A branch divergence I had been describing as deliberate was collapsed by a sloppy sync, and I had never checked that it was deliberate.** All session I synced `main` by cherry-pick and reported "2 files differ, deliberately branch-scoped" — `nd_ln_style_train.py` and its test. **That characterisation was an inference from the branch name (`nd-ln-architecture-transition`), never verified.** The final sync used `git checkout <branch> -- .` instead of a cherry-pick and brought both files onto `main`, collapsing the divergence. Checked afterwards, which is the wrong order: `git log` shows the file was committed 2026-08-13 on the transition branch and had never been on `main`; it is a CLI wrapper giving DZ's `Nd_ln.py`-shaped entry point over this repo's own training loop, explicitly "wraps; does not reimplement", and it carries a test that passes on `main`. **Not reverted**: two identical green branches is a simpler and more defensible state than an undocumented two-file divergence, and nothing indicates the divergence was ever intended. But the decision is being made *now*, deliberately, rather than left as the silent side effect it was — and the process point stands on its own: I repeated "deliberately branch-scoped" in several commit messages and status reports without once running `git log` on the file | `nd_ln_style_train.py`, `tests/test_nd_ln_style_train.py`; `git log --date=short -1 --` on both; `git diff main HEAD --name-only` (now empty) | decided | Same shape as the day's other four: a claim about the repo asserted repeatedly, cheaply checkable, never checked. This one differs only in being about repository state rather than code, which is why it survived longer — `scripts/state.py` reports branch divergence but says nothing about whether a divergence is *intended*, and no instrument can |

| 2026-08-16 | **`Conv2d_tf`'s asymmetric-padding branch is DEAD in IDAAC's own architecture — my docstring claimed it was live, and red-green caught it.** `rlgen/algos/idaac/encoder.py` carries the reference's `Conv2d_tf` (TF SAME padding, including the one-sided `F.pad` when total padding is odd) and its docstring asserted "a plain `nn.Conv2d(padding=1)` is *not* equivalent at every input size, and at 84x84 the odd-size branch is live". Mutation test: substituting plain `nn.Conv2d(..., padding=1)` left the T1 transplant **green at both 64x64 and 84x84**. The reason is arithmetic and total: every conv in `_make_layer` and `BasicBlock` is `kernel_size=3, stride=1`, so with `out == in` the total padding is `(in-1)*1 + 3 - in = 2` — always even, so `rows_odd`/`cols_odd` are always 0 and the one-sided pad never executes. **`Conv2d_tf` is exactly equivalent to symmetric `padding=1` for this network at every input size.** Still carried, but for reasons that had to be separated from the false one: the null is the base implementation, so removing it would need an argument rather than the absence of one; and the equivalence is a property of *these* hyperparameters, so a later stride or kernel change would silently invalidate a substitution that looks safe today. What is no longer claimed is that it makes any numerical difference now. Note the test written alongside it (`test_conv2d_tf_pads_asymmetrically_where_plain_conv_cannot`) passes only because it constructs a `stride=2` conv — a configuration this network never builds; it demonstrates the mechanism exists, not that the model uses it, and now says so | `rlgen/algos/idaac/encoder.py::Conv2d_tf`, `_make_layer`, `BasicBlock`; `tests/test_idaac_base_parity.py`; mutation M1 | decided | Docstring corrected in place. **Third time today red-green contradicted a claim I had just written** (after "carried forward verbatim" and "closes the dual-network gap"), and the first where the mutation *passing* was the finding rather than a gap in the test — a mutation that fails to break anything is evidence about the code, not only about the test. Worth carrying forward: when a mutation does not fire, ask why before assuming the test is weak |

| 2026-08-16 | **`idaac`'s three encoder classes are three different things, and treating two of them as one was caught by a test within minutes.** The reference defines `ResNetBase`, `ValueResNet` and `PolicyResNetBase` (`model.py:129-265`), and it is tempting to read the latter two as cosmetic variants. They are not. **(a) `ValueResNet.forward` returns the value ALONE** (`model.py:265`), where `ResNetBase.forward` returns `(value, features)` — an initial port subclassed `ResNetBase` and inherited `forward`, with a docstring asserting the two were "structurally identical … same forward". The transplant test failed unpacking two values from one. Fixed with its own `forward`; docstring corrected. **(b) `PolicyResNetBase` is IDAAC's ADVANTAGE head, not a value head**: `critic_linear` is `Linear(hidden_size + num_actions, 1)` and `forward` concatenates the action before it (`model.py:186,217-220`), making it `A(s,a)` rather than `V(s)`. The encoder-decoupling the method is named for depends on that concat. **Continuous adaptation, graded NOT INTRINSIC**: `F.one_hot(actions.squeeze(1), num_actions)` has no meaning for a 7-D continuous action, so the raw action vector is concatenated instead and `num_actions` becomes `act_dim`. Same branch point already decided for `ctrl`'s `action_mlp` (2026-08-14) and decided the same way for the same stated reason — the mechanism needs *a fixed-size vector representation of the action*, and one-hot is Procgen's way of supplying that rather than something the concat depends on. Deliberately consistent across the two baselines so that if the choice is wrong it is wrong **uniformly** rather than selectively (`STEP-ZERO.md` gate 3). The head's trunk transplants from the reference and is tested to; its final layer cannot (different input width), which is precisely the boundary of the adaptation | `_upstream_2fe3020/model.py:129-171,173-222,224-265`; `rlgen/algos/idaac/encoder.py`; `tests/test_idaac_base_parity.py` | decided | Built and tested: T1 on `ResNetBase` at 64 and 84, T1 on `ValueResNet`, trunk-only transplant on `PolicyResNetBase` with the head's divergence asserted to be confined to `critic_linear`, plus a check that the advantage head **actually conditions on its action** — a head that ignored it would pass every shape and transplant check above. Fourth claim of the day falsified by a check rather than by re-reading it |

| 2026-08-16 | **An unexamined filter — "port what the module needs" instead of "port the file, then justify each removal" — caused a re-derivation inside a base-first rebuild.** Surfaced by external challenge asking why duplication was not simply the default. It was not, and the reason is that a selection step was running by feel and unrecorded. **A filter is unavoidable and legitimate**: a hermetic module cannot absorb a whole repo, and `train.py`/`logger.py`/env wrappers really are replaced by the harness — those omissions were declared in each `PROVENANCE.md`. What made this a defect is that the same filter was applied at *class* granularity **inside** a file, where it stops being a protocol step and becomes silent authored decisions. **Concrete cost, in the file where the practice is supposedly strictest**: `rlgen/algos/idaac/model.py::OrderDiscriminator` is hand-written, and its docstring correctly *describes* `LinearOrderClassifier` — the reference's default classifier, ten lines, `Linear(2*emb,2)` + `Softmax`, in `model.py:269-281` of a file already vendored into this repo. A re-derivation from prose where a copy sat on disk: exactly the failure base-first exists to prevent, committed during a base-first rebuild. `NNBase` (`model.py:87-99`) went the same way — inlined as an `output_size` property on three classes because it looked like avoidable duplication, which is the same intra-module union as the `ValueResNet(ResNetBase)` mistake one layer up. Both now copied verbatim; `LinearOrderClassifier`/`NonlinearOrderClassifier` carried with the two details that matter (they emit **probabilities, not logits**, so a logit-expecting loss would be wrong against them; and `NonlinearOrderClassifier`'s `hidden_size` defaults to **4**, small enough that generosity is a real divergence) | `_upstream_2fe3020/model.py:87-99,269-297`; `rlgen/algos/idaac/encoder.py`; `rlgen/algos/idaac/model.py::OrderDiscriminator` | decided | `docs/STEP-ZERO.md` gate 1 extended in place: **copy the FILE, then justify each removal.** The filter's correct form is "remove what the harness replaces, keep everything algorithmic" — between *layers*, at *file* granularity, recorded. **Duplication is the null; every omission carries the burden of proof.** Two other instances found by the same sweep and ranked rather than silently fixed: `ppg` hardcodes `pi_enc`/`vf_enc` instead of carrying `PhasicValueModel`'s `vf_keys`/`get_encoder` machinery (behaviour identical at `arch="dual"`, filter undeclared — open); `ibac_sni`'s omissions were declared at the time and stand |

| 2026-08-16 | **A decision to disaggregate the shared training harnesses was taken 2026-08-14, recorded only in a plan file, and destroyed when I overwrote that file — twice — today. Recovered only because it was asked about.** Question raised: how did the trainers become shared, given non-shared harnesses were discussed long before. Checked rather than answered from memory. **They never became shared by a decision**: `git log --diff-filter=A` shows `rlgen/trainer.py`/`trainer_onpolicy.py` present since `9225e43f` (2026-08-10, "Rebuild as a benchmark repo"), i.e. shared from the start. The decision to *change* that was made on 2026-08-14 and its resolution was explicit — *"hermetic runners for control flow (rollout/replay mechanics, update dispatch, per-baseline loop shape), but every runner emits through one mandatory, shared, contract-defined event-emission call — never its own logging"*, with the counter-risk named and accepted (12 runners = 12 places metric reporting could silently diverge). **It was recorded in `~/.claude/plans/jolly-spinning-valiant.md` and nowhere else.** That file was overwritten by me twice on 2026-08-16 for unrelated planning, so the decision exists in no durable document: no `REGISTER.md` entry, no `INTEGRATION-DELTA.md` row, nothing in `COMPARABILITY_CONTRACT.md`, which still describes the shared trainers as the budget-counting layer without noting they were scheduled for removal. Meanwhile the *algorithm-internal* half of that same decision WAS executed (hermetic `algo.py`/`storage.py` per baseline, `onpolicy_ext.py` deleted) — so the record shows a half-done decision with no trace of the other half being owed | `git log --diff-filter=A -- rlgen/trainer.py rlgen/trainer_onpolicy.py` -> `9225e43f`; `docs/REGISTER.md` 2026-08-14 entries (algorithm half only); absence of any runner entry anywhere | recorded | **This is `docs/SYSTEM.md`'s own central rule, violated within hours of writing it**: durable state (a decision) held in working state (a plan) is lost when the working state expires — and a plan is *designed* to expire. The rule said "write the durable thing durably"; a decision reached during planning is durable the moment it is reached, and belongs in `INTEGRATION-DELTA.md` before the plan that produced it is touched again. Two things now owed: (a) the harness-disaggregation decision must be re-recorded durably and either executed or explicitly deferred with a reason; (b) `SYSTEM.md` needs the specific trigger — **a decision made while planning is written out of the plan immediately**, because plan files are the one working-state slot that gets overwritten rather than merely going stale |

| 2026-08-16 | **Measured against the true null — "the repos as downloaded" — this project replaced FIVE distinct reference training loops with two of its own, and the count matters because 12 was the wrong number.** Prompted by the observation that RL-ViGen ships several baselines *together*, so whatever they share is **original sharing, not authored sharing** — and "disjoint is the null" cannot mean forcing apart what a reference shipped as one thing. Counted from `docs/ORIGINAL_LOCATIONS.md`'s own train/eval-loop table rather than re-derived: `drqv2`/`svea`/`sgqn`/`curl`/`drq` share **one** loop, `RL-ViGen-upstream/train.py`, supplied together; `rad`/`soda` (and the `sac` base under them) share **one**, `dmcontrol-generalization-benchmark/src/train.py`; then `ctrl` (`train_ppo.py`), `alda` (`alda_trainer.py::train`), `ppg` (`ppg.py::learn` + `roller.py`), `ibac_sni` (`agents/ppo.py::train`), `idaac` (`algo/idaac.py` + `train.py`). **That is 5 shared-or-single reference loops covering 12 baselines, not 12 loops** — so "reintroduce the originals" is a materially smaller job than the framing implied, and two of the five are *already* shared by their own authors. This project instead runs everything through `rlgen/trainer.py`/`trainer_onpolicy.py`, two harnesses it wrote. **The standing justification is a citation, and the citation is thinner than the use made of it**: `ORIGINAL_LOCATIONS.md`'s table says "per `porting-directive.md` §2 the harness is deliberately never shared with any reference's own loop", which asserts §2 as settling a question §2 frames rather than answers — §2 is about a *contract* on reported events, not a mandate to author the loop that emits them. The loops were never compared to their references at all: no descent measurement, no behavioural check, nothing analogous to the encoder T1s | `docs/ORIGINAL_LOCATIONS.md:313-340`; `git log --diff-filter=A -- rlgen/trainer*.py` -> `9225e43f` (2026-08-10, present from the rebuild); `porting-directive.md` §2 | recorded | Not decided here, and deliberately not executed under low context — but **recorded durably this time**, which is the specific failure of the entry above it. Two things now stand together and must be resolved as one: the 2026-08-14 decision to disaggregate into hermetic runners (recovered from a plan file that was overwritten, never executed), and this measurement showing the replacement covered 5 reference loops rather than 12. The cheapest honest next step is a **descent measurement of `trainer_onpolicy.py` against those 5**, the same instrument that falsified three baselines' classification — because right now the claim "our harness is a deliberate replacement" has exactly the shape of the claims that instrument has been falsifying all day: asserted, cheaply checkable, never checked |

| 2026-08-16 | **Complete authorship measurement over every first-party file, and two things it overturned.** Built `scripts/authorship.py`: walks all 6,921 first-party code lines under `rlgen/`, `tools/`, `scripts/` and matches each against **every** `.py` in `ext/`, `RL-ViGen-upstream/`, the vendored bases, and the sibling project — no curated list, no sampling. **Result: 39% descends from some reference, 61% is authored here.** **(1) `idaac` is a two-hop port, and `scripts/state.py` hid it.** `authorship.py` shows `idaac/model.py` 100%, `metrics.py` 100%, `storage.py` 100%, `algo.py` 83% — all from `gen-rebuttal/vigen-idaac/vigen_idaac/`, a **sibling project's port**, not from `ext/idaac`. `state.py` had reported `idaac` at 0% because its `BASELINES` list for that row contained only `ext/idaac`; I read "0%" as "descends from nothing" and queued a base-first rebuild on that basis. **A curated reference list reports "not the thing I listed", never "nothing", and fails silently when the hypothesis about origin is wrong.** `idaac` is the `alda` situation, which changes what its rebuild should be. **(2) The harness descends 0% from anything at all** — `registry.py` 356 lines, `agents.py` 264, `evaluate.py` 221, `envs.py` 210, `protocol.py` 163, `trainer.py` 160, `replay.py` 135, `trainer_onpolicy.py` 120: ~1,629 lines authored, against **five reference training loops that exist on disk**, two of which are already shared by their own authors. Also collapsed a false distinction I had been carrying: "deliberate replacement" vs "re-derivation" is not a real difference. Recording a reason makes authored code *declared*, not *derived*; the provenance class is identical either way, and since research code commonly ships train+eval loops, "we needed our own harness" is a choice every porting project faces rather than a property of this one | `scripts/authorship.py` (full run); `scripts/state.py::BASELINES`; `docs/ORIGINAL_LOCATIONS.md:313-340` | decided | `state.py` corrected: `idaac` now lists both hops, and the file documents the limit — a curated list is a *hypothesis about origin*, so use `authorship.py` for "where did this come from" and `state.py` for "does it still match what we believe". **Both instruments now state what they structurally cannot see**, which is the only thing that made this findable. Consequence for the plan: `idaac`'s queued base-first rebuild was justified by a number that meant something else, and needs re-deciding on the `alda` template (audit the sibling against the authors' repo first, rebuild only if the audit says construction rather than renamed reimplementation) |

| 2026-08-16 | **Two measurement instruments were reporting different quantities under the same word, and I quoted them interchangeably.** `scripts/state.py` computes descent as `kept / len(reference file)` — "how much of the reference did we reproduce". `scripts/authorship.py` computes `kept / len(our file)` — "how much of what we wrote descends". Both are legitimate; they answer different questions and can differ substantially on the same file (`ibac_sni/model.py` reads **99%** in `state.py` and **87%** in `authorship.py`, and neither is wrong). Quoted interchangeably in this session's reporting until raised externally. Also recorded, from the same challenge: **a descent percentage says nothing about the residual, and the residual is usually the point.** Inspected `sac.py`'s missing 7% directly — an added 11-line docstring, import-path rewrites (`import utils` -> `rlgen.algos.soda_utils`), `.cuda()` -> `.to(self.device)`, one `self.device` assignment: entirely benign and already documented. **The residuals for `soda` 85%, `augmentations` 83%, and the three storage files at 74-80% have NOT been inspected**, so those numbers are currently uninterpreted rather than understood, and were presented as if the shortfall were known. A different file's missing 7% could be a dropped loss term | `scripts/state.py::descent`, `scripts/authorship.py::main`; `diff` of `ext/dmcontrol-generalization-benchmark/src/algorithms/sac.py` against `rlgen/algos/sac.py` | decided | Both denominators documented in `authorship.py`'s limits section, with the instruction to ask which question you want before reading either number, and an explicit note that the residual must be read before a percentage means anything. **Separately confirmed by measurement rather than assertion**, having been asserted first: the sibling `gen-rebuttal/vigen-idaac/vigen_idaac/*` descends **0% from `ext/idaac`** — every file, no match at any threshold — matching the `vigen_alda` vs `ext/ALDA_Official` result measured earlier. Both two-hop baselines genuinely have zero textual descent from their authors' repositories. The claim was right; the order (assert, then check) was wrong |

| 2026-08-16 | **An undeclared dtype divergence in `sac.py`, inherited by `rad` and `soda`, found by being asked "is that cast for MPS, and what if it's CUDA?"** I had reported `sac.py`'s residual as sixteen benign lines: import rewrites, `.cuda()` -> `.to(self.device)`, and "one `np.float32` cast". Checked: `np.log()` returns `np.float64`, so `torch.tensor(np.log(args.init_temperature))` gives the reference a **float64** `log_alpha` — and therefore float64 `alpha`, float64 Adam state for the temperature, and a float64 entropy term in both the actor and the alpha loss. Ours is **float32** throughout. Initial values differ by 3.2e-08 (nothing); the dtype differs for the entire run (not nothing). The cast exists because **MPS has no float64** — without it the whole SAC family cannot run on this machine. **But it is unconditional**, so on a CUDA box this code still runs float32 where the reference ran float64: a hardware workaround for *this* machine silently changing the arithmetic on the machine the reference targeted and whose published numbers it produced. Confirmed undeclared: no mention in `docs/FAITHFULNESS.md` or this register before now, and it had been classified as "device-agnosticism, not a fidelity change" in `sac.py`'s own docstring since 2026-08-14 | `rlgen/algos/sac.py:47`; `ext/dmcontrol-generalization-benchmark/src/algorithms/sac.py`; dtype checked by direct execution | decided | Declared in place at the call site with the full reasoning, and **left unconditional deliberately**: device-gating it would make this project's own results depend on which machine produced them, which is worse than one stated divergence. The generalisable point is about the word "benign": three of the four residual verdicts I gave were reached from a truncated `diff` and one word each. `.cuda()` -> `.to(device)` really is benign; a cast that changes a tensor's dtype is a different kind of edit wearing the same clothes, and only reading what `np.log` returns separates them |

| 2026-08-18 | **The citation checker reported "0 defects" across 677 citations because its test was vacuous, and the real rate is not zero.** `scripts/check_citations.py` decided `BROKEN` on one predicate: *is the cited line number ≤ the file's line count*. Almost every citation passes that regardless of whether it is right — a file that grew by 40 lines still contains line 128; it simply no longer says what we said it says. The check also reached only 265 of 677 citations (39%); 357 were `AMBIGUOUS` (a bare basename matching several trees) and never tested at all. So the headline covered 39% of the corpus with the weakest available predicate, and `SYSTEM.md`'s stated worry — *"line numbers drift silently when a reference is re-cloned"* — was **exactly the failure mode the instrument could not see**. Added `--content`: 55% of citations here quote the cited code in a span beside them, and that span is the claim, so it is checkable. It also disambiguates — content resolves what the path could not. Of 483 checkable, 22 candidates in the strictest stratum (one candidate file, nearest anchor, bound within 40 chars, shaped like a quoted line rather than a label). **Hand-checking 7 of those 22 found 4 real and 3 false — precision ≈57%, so this is a lead generator, not a verdict machine, and it is reported as one.** The 4 real are all stale line numbers into the port: rlgen/envs.py line 113 is a **blank line** (`obs.shape` is at 133) — written without backtick-citation form on purpose, because it is being quoted as a *stale* pointer and `check_citations --content` correctly flags any live-form citation to it; `rlgen/agents.py:312` is `__getattr__`, not the claimed `on_policy = True` (at 326); `rlgen/protocol.py:207` is a comment about a different field (`time_limit_handling` at 242); `rlgen/evaluate.py:241` is the comment 4 lines above its code. **Drift is toward later lines in all four**, consistent with the port being edited after the docs were written. The 3 false positives are one class: the document quotes the construct in its own notation rather than the file's — the audit writes GAE as `(1 - done[t])` where `ppo_daac_idaac/storage.py` says `self.masks[step + 1]`. That citation is correct and the checker cannot see it, which bounds what this instrument can ever claim | `scripts/check_citations.py --content`; `tests/test_citation_content.py` (34 assertions); starred rows only | open | Findings recorded before repair, per `SYSTEM.md` §"What goes where". **Note the sequence: this row first read "4 of 4 sampled were real" — written after sampling only the port citations. Three further samples, all from the live clone path, were all false. The first number was not wrong so much as drawn from a stratum that was not the whole set** |

| 2026-08-18 | **Building that checker, its own test caught it caching stale file contents.** `_lines()` memoised on path alone with no invalidation, so **a tool whose entire purpose is noticing that a file changed under a citation was itself serving pre-change contents**. Invisible in normal use — nothing edits files mid-run — which is precisely why it survived being written, and it silently made the instrument un-testable: the end-to-end test that simulates an upstream re-clone (insert 40 lines, re-check) returned `CONFIRMED` where `DRIFT` is the only correct answer. Now keyed on `(path, mtime_ns, size)`. Recorded because the failure is general, not local: **a cache is an assumption that the world holds still, and it was placed inside the one component built on the assumption that it does not** | `scripts/check_citations.py::_lines`; `tests/test_citation_content.py::test_the_checker_finds_the_defect_it_was_built_to_find` | decided | Fixed. The test that caught it was written before the fix and failed red first |

| 2026-08-18 | **RL-ViGen's shipped robosuite evaluation selects no difficulty at all.** `scripts/eval.sh`'s robosuite branch passes `env`, `task`, `model_dir`, `seed`, `action_repeat`, logging flags and `wandb_group` — and **no `mode`**. `eval.py` likewise never passes `mode` on the robosuite path (lines 88, 89 and the scene-switch rebuild at 184), while its habitat path passes `mode='test'` explicitly at line 95. So `mode` falls through to `envs/robosuiteVGB/cfg/robo_config.yaml:30`, which ships as **`mode: train`**. Run as shipped, their robosuite evaluation sweeps ten scenes **on the training distribution**. Producing the published Easy/Hard/Extreme table therefore required hand-editing a config file that no shipped script touches. Two consequences: their difficulty axis is not selectable from any command line, which is exactly what our patch **P1** fixes on our side; and the mapping from a published column to a config state is unverifiable from the repository. Separately, the same branch sets **`action_repeat=1`**, the paper's robosuite value — while `scripts/train.sh` is a DMC script (`env=dmc`, `action_repeat=2`, robosuite lines commented out), so the `2` so often cited as "their code default" is not a robosuite setting | `RL-ViGen-upstream/scripts/eval.sh:6-20`; `eval.py:88-95,184`; `envs/robosuiteVGB/cfg/robo_config.yaml:30`; `scripts/train.sh:32-34` | open | Recorded, not acted on. It changes how their published table is read; it does not change our runs, which set `mode` explicitly through P1 |

| 2026-08-18 | **The MPS training path reproduces bit-identically across days, and it was found by accident rather than tested for.** A `drqv2` Door run launched today to produce a checkpoint logged `episode_reward` **0.8461724303550348** at frame 0 and **6.904075909089909** at frame 10,000. The run of 2026-08-17 (`exp_local/2026.08.17/183214...`) logged **the same two values to all 16 significant digits**. The configs differ only in `num_train_frames` (200000 vs 120000) and `save_snapshot`, neither of which touches the trajectory before 10k, and both are seed 1 (`cfgs/config.yaml:25` defaults `seed: 1`, so the run without an explicit seed matches the run with `seed=1`). **Two independent eval points, one before any gradient step and one after 10,000, agreeing exactly.** What this buys: a difference between two of our runs is attributable to configuration, because the backend contributes no run-to-run variation — which is a precondition for every single-seed comparison this project has made and had never been checked. What it does NOT buy: agreement with any other backend. [C41](CONSTRUCTION.md#c41) measured CPU-vs-MPS divergence separately and this says nothing about it; determinism is not correctness, and a deterministic wrong answer is still wrong. It is also one pair of runs on one task, not a policy | `exp_local/2026.08.18/120404.../eval.csv` vs `exp_local/2026.08.17/183214.../eval.csv`; `cfgs/config.yaml:25` | open | Recorded as a property worth converting into an explicit test rather than left as an observation — nothing currently asserts it, so it can regress silently |

| 2026-08-18 | **A third generalisation axis exists in the environment and this project measures none of it.** `make_env` branches on six regimes, not four: `train`, `eval-easy`, `eval-medium`, `eval-hard`, and **`cam-easy` / `cam-hard`** (`envs/robosuiteVGB/robosuitevgb/utils.py`), the last two setting `randomize_camera=True` and loading `robo_setting.yaml`. `Protocol.eval_mode` is a single string fixed at `eval-easy`, so the camera axis has never been constructed, let alone measured, and it does not appear in the register as a gap because nothing named it. Found while writing a test asserting that `scripts/probe_regimes.py`'s hardcoded regime tuple matches what the environment implements — the tuple lists four, the environment has six. **This is the same shape as [C45](CONSTRUCTION.md#c45)**: an axis the benchmark varies and our evaluation holds fixed. It is a smaller finding than C45 only because nothing has been published against it | `envs/robosuiteVGB/robosuitevgb/utils.py` (regime branches); `scripts/probe_regimes.py::REGIMES`; `tests/test_probe_regimes_contract.py` | decided | **[DEFAULT SET 2026-09-04 — excluded deliberately, seventeen days after being recorded and never acted on.** See [`EVAL-PROTOCOL`](EVAL-PROTOCOL.md) §2. Three reasons, weightiest first: the camera axis is a **different kind of generalisation** (viewpoint, not appearance) and the contract's retention question is posed over appearance regimes; it is **not reachable from our harness** as written, because the branch loads `open('../../../../envs/robosuiteVGB/cfg/setting/robo_setting.yaml')` — a cwd-relative path four levels up that resolves only from upstream's own launch directory, so our offline evaluator raises `FileNotFoundError` before building the env; and it would enlarge the grid **1.5x** at a budget already over the grant. Reversible for the price of one PLATFORM-class patch making that path absolute, plus 50% more evaluation. The point of writing it down is that the exclusion is now a decision someone can argue with rather than a gap nobody noticed.]** Not acted on. Recorded so the scope statement is explicit: our numbers vary appearance, hold scene fixed (C45), and hold camera fixed. Whether the camera axis belongs in this project's protocol is a scoping decision, not a defect to fix |

| 2026-08-18 | **An over-claim of this session's own, caught by this session's next measurement and withdrawn.** C45 and C46 were written saying the single-scene evaluation omits an axis *"in the direction that makes the task easier"*, and commit `56ab2d53` says so too. The reasoning was that scene 0 is the training scene, so evaluating there is in-distribution. Then the obvious follow-up was measured: **distance from the training distribution** (scene 0 at `train`) rather than scene-to-scene distance. Ours (scene 0, `eval-easy`) is **0.4689**; theirs (scenes 1/4/7, `eval-easy`) median **0.4536** — **97% of ours, not further**. Appearance randomisation dominates the metric and a scene change moves sideways at the same radius. So the "easier" half was never measured, only inferred from a true premise. Withdrawn in both entries; what stands is that the protocols sample **different** points, which is enough for "not comparable" and is all C45 needs. The thing that *would* make scene 0 easier — its textures being the memorised ones — is invisible to a marginal intensity histogram by construction, so no observation-space instrument can settle it | `docs/CONSTRUCTION.md` C45, C46; measurement reproducible from `scripts/probe_scenes.py::collect` + `scripts/metrics.histogram_tv_distance` | decided | Corrected in place. Recorded because the failure is not arithmetic: a correct fact (scene 0 is the training scene) was allowed to imply a direction nobody had measured |

| 2026-08-18 | **The measurement that overturned the previous row was itself too small, and re-running it reversed its direction.** The row above withdrew an "easier by construction" claim on the strength of held-out scenes sitting at **97%** of our distance from the training distribution — measured on **3 scenes at 2 episodes x 5 frames**. Re-measured at **9 scenes, 3 episodes x 6 frames**: ours 0.4650, held-out median **0.4908 = 106% of ours**, with **7 of 9 scenes further from training than ours**, range 93-121%. The sign flipped. **The correct conclusion is neither 97% nor 106% but that this instrument does not resolve the question at these sample sizes** — a quantity that moves nine points when the sample triples is measured to no better than ten, and the per-scene range straddles our own value from both sides. The withdrawal in the row above still stands, for a better reason than it was made: not "the opposite was shown" but "nothing of the claimed size is visible either way". Appended rather than editing the previous row, per the Log layer's rule that a later repair does not discharge an earlier one — and the pair is more informative than either row alone, because the failure mode on display is **stopping at the first sample that produced a clean-looking answer** | reproducible from `scripts/probe_scenes.py::collect` + `scripts/metrics.histogram_tv_distance`; numbers in `CONSTRUCTION.md` C46 | decided | C46 now carries both passes and states the resolution limit. **Standing lesson: a measurement used to overturn a claim needs at least the sample size that claim would have deserved** |

| 2026-08-18 | **"Reproduces bit-identically" was true for 10,000 frames and false by 20,000 — corrected two hours after it was written.** The earlier row claimed the MPS path reproduces exactly across days, on the strength of two eval points. The third point breaks it: at frame 20,000 the 2026-08-17 run logs **5.809077627328698** and today's logs **5.67769302372665** — a 2.3% gap where frames 0 and 10,000 agreed to all 16 digits. The runs are the same configuration: `diff` of the saved `.hydra/config.yaml` shows only `save_snapshot` (which writes a file and had not fired by 20k, the first save being at `global_step % 5e4`) and `num_train_frames` (the stopping point — the exploration schedule takes its own constant, `linear(1.0,0.1,100000)` in `cfgs/task/easy.yaml`, not `num_train_frames`). Same seed, since `cfgs/config.yaml:25` defaults `seed: 1`. **Replay-order nondeterminism is eliminated**: the sampler globs `sorted(..., reverse=True)` (`replay_buffer.py:128`) and the only unsorted `glob` (`:66`, `_preload`) merely counts episodes and sums lengths, where order cannot matter. Both runs set `replay_buffer_num_workers=0`, so `_worker_init_fn`'s reseeding (`:168-170`) never runs in either. That leaves **non-deterministic MPS kernels** as the leading candidate — stated as a candidate, not a conclusion, because it was not isolated | `exp_local/2026.08.{17,18}/...eval.csv`; `.hydra/config.yaml` diff; `replay_buffer.py:66,128,168-170`; `cfgs/task/easy.yaml:2` | open | **The 10k agreement is still worth having** — it means the pipeline, env seeding and data path are deterministic, and divergence enters through the optimiser rather than the plumbing. What is withdrawn is the unqualified claim. Next step if it matters: re-run with `PYTORCH_ENABLE_MPS_FALLBACK` off and `torch.use_deterministic_algorithms(True)` to see whether the divergence disappears |

| 2026-08-18 | **[C41](CONSTRUCTION.md#c41) predicted this failure in writing, and it has now been observed.** C41 measured MPS-vs-CPU divergence with a golden trace — fixed data, identical batches — found it bounded (growth 0x over 250 steps), and then said plainly what that cannot cover: *"a tiny action difference changes the transition, which changes the replay contents, which changes every later update. That feedback loop is the dominant divergence mechanism in a real run and **this probe cannot see it**."* Two runs of the same configuration on the same backend now show exactly that: identical to 16 digits at 10,000 frames, **2.3% apart at 20,000**. **The finding is not that C41 was wrong — it is that C41's stated limit has moved from a caveat to a measured fact**, and the amplifier is visible in under 20k frames rather than being a theoretical worry. Recorded because a caveat nobody has seen fire is routinely discounted by the next reader, and this one should not be | `CONSTRUCTION.md` C41 ("what this cannot tell you"); `exp_local/2026.08.{17,18}/...eval.csv` | open | No change to C41's conclusion or its licence (ordinal claims yes, table numbers no). **This entry exists to stop the caveat being read as hedging** — it names a mechanism that demonstrably fires at this project's actual frame counts |

| 2026-08-18 | **`probe_heads` exit code contradicted the sentence printed directly above it.** The script ends `return 1 if bad else 0`, and `bad` counts only heads that were built and found WRONG. A run where every head failed to import therefore returned **0** and printed *"All constructible heads initialise to sigma = 1"* — vacuously true of zero heads, and pass-shaped to anything reading the status — immediately followed by its own prose warning *"A SKIP is not a pass: it means the head could not be built here and nothing was checked."* The prose was right and the exit code disagreed with it. Now counts `checked` and `skipped`: zero checked is a FAILURE with a named reason, and a partial run says how many heads the all-clear does **not** cover. Verified by forcing every head to skip (exit 1, "NOTHING WAS CHECKED: all 4 skipped") and restoring (exit 0, 4 checked). Note the first attempt to verify this was itself wrong — breaking the interpreter path produced exit 1 from an uncaught `FileNotFoundError`, the right code for the wrong reason, which is not evidence. Live result unchanged: all four heads build and match H = 9.93257 to 2.3e-07 | `scripts/probe_heads.py` | decided | Fixed. Same shape as the citation resolver's "0 defects" and the register's hash check: **a pass condition that the failure case also satisfies** |

| 2026-08-18 | **Backend nondeterminism alone reaches 12.8% of the return by 30k frames, and is still growing.** A fourth eval point turns the earlier two-run comparison into an amplification curve — same configuration, same seed, same machine, runs a day apart: **frame 0 → 0.846172 vs 0.846172 (0.00%)**; **10,000 → 6.904076 vs 6.904076 (0.00%)**; **20,000 → 5.809078 vs 5.677693 (2.26%)**; **30,000 → 57.443439 vs 50.096433 (12.79%)**; **40,000 → 70.4576 vs 104.6356 (48.51%)**; **50,000 → 89.5000 vs 118.6335 (32.55%)**; **60,000 → 121.0673 vs 179.5595 (48.31%)**; **70,000 → 242.7015 vs 253.0373 (4.26%)**; **80,000 → 292.5496 vs 232.7729 (20.43%)**; **90,000 → 365.3291 vs 447.6736 (22.53%)**; **100,000 → 469.4089 vs 436.2104 (7.07%)**. **The series is now complete to 100k and the shape is clear: same destination, noisy path.** Both runs finish at `success_rate` **1.0** with returns 436-469, having disagreed by up to 48.5% on the way. So the runs are not divergent in outcome — what is unreliable is *any single eval point*, and therefore any claim keyed to a frame count ("solves it by 80k", "reaches X at 50k"). **The sharpest single illustration is at 80k, and it is not a percentage**: run A logs `success_rate` **1.0** and run B **0.4**. "Does this configuration solve Door by 80,000 frames?" has the answers *yes, every episode* and *no, two in five* from the same configuration and seed. **The gap has no trend at all**: after 20k it reads 2.3, 12.8, 48.5, 32.6, 48.3, 4.3 percent — min 2.3%, max 48.5%, and back to near-agreement at 70k where both runs sit at ~250 with SR 0.6-0.7. This row has now been re-characterised twice: it first said "still growing" (the 50k point contradicted it), then "fluctuates in a 30-50% band" (the 70k point contradicts that). **The description that fits is two noisy samples of one learning curve** — the runs are not on divergent trajectories, but any single eval point can disagree with its twin by up to ~50%. And at 40k the *ordering* reverses: the run 13% behind at 30k is 48% ahead at 40k and stays ahead at 50k, so "which run is better" is not stable either. Success rate flips both ways across the series (B 0.1 vs A 0.0 at 30k; A 0.1 vs B 0.0 at 50k; A 0.2 vs B 0.1 at 60k), so at these budgets it carries no signal at all — and at 60k **the two endpoints disagree with each other**, B leading on return by 48% while A leads on success rate. The qualitative reading diverges too: at 30k the newer run logs **success_rate 0.1** where the older logs **0.0**, so "did this policy ever succeed by 30k frames" has different answers in two runs of the same thing. The curve is what [C41](CONSTRUCTION.md#c41) describes — a perturbation changes an action, which changes the buffer, which changes every later update — now with numbers attached | `exp_local/2026.08.{17,18}/...eval.csv`, four matched eval points | open | **This is a noise floor for every single-seed comparison in this project**, and it is larger than several effects already under discussion. One pair of runs, not a distribution, so read it as "at least this much" rather than as an interval. It also means a difference of tens of percent between two of our own numbers is not evidence of anything |

| 2026-08-18 | **The return-space scene instrument was validated against a random policy before a real checkpoint existed, and its control immediately earned its place.** `scripts/eval_across_scenes.py` was run end-to-end on a stub agent emitting uniform actions: it reported **retention 0.330**, which read as a large held-out drop — and the within-scene control (the training scene re-run at a second episode seed) put the **same-scene gap at 1.56 against a held-out gap of 1.85, a ratio of 1.19x**, so the script printed *"the held-out drop is NOT clearly larger than re-running the same scene… a failure to detect a difference, not evidence that scene identity does not matter."* A random policy has no scene-specific knowledge, so the true retention is 1.0 by construction and **0.330 was pure episode noise**. Without the control that number would have been reportable. Also de-risked the load path: the snapshot pickles the agent object, so `algos.drqv2` must be importable at `torch.load` time — verified, `algos` is a package and `_setup()` puts both `RL-ViGen-upstream` and its `algos/` on `sys.path` before the load | `scripts/eval_across_scenes.py`; stub run at 2 scenes x 2 episodes | decided | **Validate an instrument on an input whose answer is known before pointing it at one whose answer is wanted.** The known answer here was retention = 1.0 for a policy that cannot distinguish scenes; the instrument's uncontrolled headline missed it by a factor of three |

| 2026-08-18 | **The project has evaluator agreement and has never reproduced a published number.** `tools/crosscheck_against_rlvigen_eval.py` runs one policy through `rlgen.evaluate` and through a *transcription* of `eval.py::robo_eval` and checks the two agree — implementation agreement, on the **superseded port**, with no trained policy involved. Nothing has ever landed on a cell of `results/evaluation_score.xlsx`. **Two evaluators agreeing is not the same evidence as one number matching**, and [C45](CONSTRUCTION.md#c45) is the case that separates them: both loops would have agreed perfectly while computing a single-scene quantity the reference never published | `tools/crosscheck_against_rlvigen_eval.py` docstring; `scripts/rlvigen_reference.py`; [C48](CONSTRUCTION.md#c48) | open | Compute-blocked. **Recorded without an ETA** — it happens once compute exists |

| 2026-08-18 | **A classification written this session was corrected by external challenge, and the correction moves where the fix belongs.** [C45](CONSTRUCTION.md#c45)'s error class had been framed in discussion as an *inherited defect* — present in faithfully-preserved upstream code, against which the clone-and-ledger discipline offers no protection. The sharper class is **output contract not yet settled**: no metric for the train/eval gap had been agreed, so there was nothing for any gate to check against. That relocates the remedy from the faithfulness layer, which cannot catch it, to contract-building, which can. **Residue kept, because the correction can be over-applied**: the specific fact that train and eval shared a scene was independently checkable throughout, so "nothing could have caught it" is too strong. As practice: items of this shape accumulate for the owner's judgement-constructions, and **finding one only at contract-building time is an acceptable outcome, not a failure of the earlier stages** | discussion 2026-08-18; [C45](CONSTRUCTION.md#c45) | decided | Recorded per `SYSTEM.md`'s rule that *which instruments work* is methodological evidence. Notable because nearly every other correction this session was self-caught by measurement; this one survived internal review and failed on contact with a reader |

| 2026-08-18 | **`success_rate` is one quantity across all twelve, and nothing was holding it there.** It is the only unit-free, task-defined quantity available — Door's return is dense and scale-arbitrary — so it is what a comparison most depends on. Reading all seven accumulation sites shows every one uses `x = x or bool(...)`: **an episode counts as a success if `_check_success` held at ANY step**. That is a deliberate harmonisation, commented at each site against `PART2-METRIC-INVENTORY.md`. **It was unprotected**: one clone edited to read the flag at the last step would move `success_rate` from *universal* to *accidental* — the defect class — with no test failing and no number looking wrong, because both conventions yield a plausible value in [0, 1]. The check also surfaced that **`RL-ViGen-upstream/train.py` holds both conventions at once**: any-step in `eval()` (robosuite, P11) and final-step in `habi_eval()` (habitat, untouched). One file, one name, two quantities, separated only by which method the env family dispatches to — which is the concrete reason a convention needs pinning rather than describing | `tests/test_success_convention.py`, 7 sites covering 12 baselines; red-green verified by flipping `dmc_gb` to the final-step form | decided | Pinned. **Method note: reaching this involved a false step worth recording — I first concluded `ibac_sni` emitted no success at all, from a `grep | head -6` whose six lines happened to come from one alphabetically-earlier file. Truncated output read as absence. `ibac_sni` in fact has 11 such mentions and the standard any-step accumulation at `scripts/evaluate.py:99-101`. Had it stood, the next step would have been an unnecessary construction in a clone to fix a gap that did not exist** |

| 2026-08-18 | **The shared x-axis is held by three different mechanisms and none of them was checked.** `PART2-METRIC-INVENTORY.md` §2 establishes that all twelve plot against env frames — but only while `action_repeat = 1`, and that holds three ways: an explicit flag (`rlvigen.sh`, `dmc_gb.sh`), a config file (`alda`'s robosuite spec), and **absence** (`ppg`, `idaac`, `ibac_sni`, `ctrl` have no repeat mechanism at all, so nothing can rescale their frames). It has already been wrong once — §2 Finding 3, the five running at 2 against the other seven's 1 — and the fix lives in our launcher while upstream's `cfgs/config.yaml` still says 2, so the wrong value is one flag away. **The subtler exposure is `alda`**: its robosuite spec sits beside siblings declaring 2 and 4, and `alda` is one of [C13](CONSTRUCTION.md#c13)'s dead seams, so a spec copied from a neighbour would *declare* a repeat the env silently ignores — the x-axis label wrong while the run was right, which is the harder failure to see because the number is fine. Now pinned across all three mechanisms, including the absence case | `tests/test_x_axis_invariant.py`; red-green verified against three mutations | decided | Pinned. [C13](CONSTRUCTION.md#c13) stays MONITORED — this does not act on it, it removes the word *silently* from its blast radius |

| 2026-08-18 | **A test of mine passed by matching the comment that explains the rule rather than the rule.** `rlvigen.sh` carries the line *"action_repeat=1 is NOT a preference, it is this project's declared protocol"*, and the first version of the x-axis check searched the raw file for `action_repeat=1`. Reverting the actual command to `2` left the comment intact, so the mutation **survived** — the assertion was reading the justification, not the setting. Caught only because the mutation was run; it passed its own first execution and would have read as coverage. Fixed by stripping comment lines before matching. **General form, and it is not confined to shell: a well-commented file is a worse target for textual assertions than a bare one, because the prose restates the thing being checked** | `tests/test_x_axis_invariant.py::read` | decided | Fourth instrument this session found to pass on a condition its failure case also satisfied |

| 2026-08-18 | **A red-green procedure was defeated by Python's bytecode cache, and the failure direction is the dangerous one.** Verifying the horizon check, I mutated `DEFAULT_HORIZON = 500` to `250`, saw the test fail correctly, restored via `git checkout`, and the test **still failed** — reporting 250 while the file on disk read 500 and `git status` was clean. Cause: `.pyc` validation keys on `(mtime, size)`; `250` and `500` are the same byte length and `git checkout` set an mtime inside the same second, so the stale bytecode was judged current. **The direction matters. Here it produced a spurious failure, which is loud. The same mechanism produces a spurious PASS whenever the mutation and the original are the same size — a one-digit edit, a flipped comparison operator, a swapped identifier of equal length — and a mutation that "survives" for that reason is indistinguishable from a vacuous test.** Some of this session's mutation results involved same-length edits and were not re-checked against a cleared cache | reproduced on `rlgen/protocol.py`; `find -name __pycache__ -delete` then green | decided | **Practice: confirm a restore by reading the file, not by re-running the test — the test is the thing being validated and cannot validate its own preconditions.** Where a mutation preserves byte length, clear `__pycache__` before drawing a conclusion |

| 2026-08-18 | **The doubt the row above raised is resolved: no earlier conclusion this session was affected.** The reasoning that closes it is directional. A stale `.pyc` means the mutation never took effect, so the test sees unmutated code and **passes** — a stale cache can manufacture a *survivor* and can never manufacture a *kill*. Every mutation reported KILLED is therefore sound regardless of caching, which covers all of them except two. Both survivors were accepted on cache-independent evidence and both were re-checked: `authorship`'s `count=1` removal still survives with `__pycache__` cleared, and its equivalence was established separately by demonstrating the pattern matches once without `re.MULTILINE` and twice with it — an argument about the regex, not about a test run. The `rlvigen.sh` survivor is a shell file with no bytecode at all, and it was a real defect, found and fixed. **Restores in this check were confirmed by reading the file rather than by re-running the test**, per the practice the previous row records | re-run with cache cleared; `scripts/authorship.py` restored and verified by content | decided | Recorded because a doubt raised and left open is worse than one never raised — it discredits the whole set without telling anyone which part to distrust |

| 2026-08-18 | **Half the baselines already evaluate both regimes, so [C43](CONSTRUCTION.md#c43)'s "we only ever evaluate in one regime" was true of the group that produced the numbers and read as true of all twelve.** Reading every eval construction: `alda` evaluates train, eval-easy *and* eval-hard; `ctrl` evaluates ID (train) and OOD (eval-easy) with separate `succ_id`/`succ_ood`; `dmc_gb` evaluates `env` and `test_env`, covering `rad` and `soda`; `ppg` and `ibac_sni` reach both through checkpoint passes that PART2 §3 records as already verified (1.1829/0.9825 and 0.690/0.690). Only the RL-ViGen five and `idaac` are single-regime. **Regime retention is therefore computable today, without any change, for six of the twelve.** The remedy for the rest is the second `eval_env` patch already scoped in C43/[C45](CONSTRUCTION.md#c45), plus one additional env for `idaac` of the shape its own `test.py` builds — one patch and one line rather than a programme. The generalisation held because the five that were checked are the five that produced almost every number, so the entry was *practically* right and *structurally* over-broad | per-baseline read of `alda_trainer.py:609-611`, `ctrl/train_ppo.py:97-100`, `dmc_gb/src/train.py:115-117`, `idaac/test.py:22-23`; PART2 §3 for the two checkpoint passes | open | Scope narrowed in C43; the decision remains the owner's, and is now a cheaper one |

| 2026-08-18 | **The comparability seam is now checked in two independent ways, because the first way has a demonstrated blind spot.** Five textual checks pin the seam — y-axis, x-axis, horizon/truncation, scene, regime — and textual matching failed three times in one session by matching the *documentation* of a setting rather than the setting (a shell comment restating `action_repeat=1`; a docstring listing `_max_episode_steps`; an argparse `help=` enumerating the regimes). Each passed on its own first run. `tests/test_seam_behaviourally.py` builds an env through the same `robo_make` path the baselines use and reads the answer back out of `last_info`, which `vgb_wrapper` fills from the env's own state rather than from the argument passed in. **All four hold: the env reports `eval-easy` when asked, reports scene 7 when asked (the [C45](CONSTRUCTION.md#c45) fallback would report 0 for every request), matches `Protocol.obs_shape`, and ends at exactly 500 steps.** So the seam is not only described correctly, it is what the environment actually does | `tests/test_seam_behaviourally.py`, 4 assertions; red-green verified; skipped where robosuite is unavailable, and a skip there is not a pass because the textual checks still run | decided | **The general point is about instrument design, not this seam: where a property is cheap to observe, observing it beats asserting on the source that configures it — the source can be right about a mechanism the run does not reach** |

| 2026-08-18 | **The fidelity line was crossed before P14 was ever proposed, and nothing recorded where it sits.** `Protocol.env_patches` hashes *which* patches were present, so any number is traceable to its patch set — but the registry never said what *kind* of change each patch was, and that is the distinction deciding whether a number is attributable to RL-ViGen or to us. `PATCH_CLASS` now records it in three classes: **PLATFORM** (this stack only — device, dtype, GL, imports; nothing measured changes, the number is still theirs), **RESTORES** (makes their own declared intent reachable — `mode` existed in their config and never reached the env; `train` crashed on a missing asset directory), **ENABLES** (adds or changes what is measured — the number is ours). The current split is PLATFORM `P4 P5 P7 P8 P9 P13`, RESTORES `P1 P2 P3 P12`, **ENABLES `P6 P10 P11`**. **So no number this project has produced is a pure-fidelity number**: success rate is not emitted by upstream at all (P10, P11) and the render resolution is fixed at 84 in their config (P6), so RAD and SODA cannot run at their papers' 100 without us. A proposed scene/regime patch would be ENABLES too, and different in kind again — the first to change *which distribution* is evaluated rather than what is recorded about it | `setup/apply_patches.py::PATCH_CLASS`; `--check` now reports by kind; `tests/test_contract.py` pins that no family is unclassified and that the ENABLES set cannot change silently. Red-green against three mutations | decided | **This is the backtracking mechanism, and it already rides in the hash**: adding a patch changes `env_patches`, which changes `Protocol.hash()`, so numbers from either side of a construction can never be silently pooled. What was missing was only the label saying which side is which |

| 2026-09-02 | **The NaN gate failed on a path and would have failed a good seven-hour run.** `family.py::check_finite` unpickles the terminal checkpoint and walks it for non-finite values — [C57](CONSTRUCTION.md#c57)'s remedy. But an RL-ViGen snapshot pickles the *agent object*, so `torch.load` needs `algos.drqv2` importable, and the probe builds its `PYTHONPATH` from `--root`, which the runner passes as the **work root**. On the container that directory is the extracted payload and the import resolves; anywhere else it cannot, and the gate reports `ModuleNotFoundError: No module named 'algos'` — a *path* failure wearing a NaN failure's clothes, arriving after training has already succeeded, on a cell the runner then marks failed. **It had therefore never once passed in a local rehearsal**, which is also why nobody had seen it. Fixed by making the walk not need the classes at all: a `find_class` that substitutes a placeholder for anything that will not import, since only floating-point storages are wanted and those unpickle without their owning class. The stubbed names are **reported, not swallowed** — a degraded walk that looks identical to a clean one is the failure mode this project keeps rediscovering. Now green locally: 33,299,022 floats over 186 arrays, 0 non-finite, six classes stubbed and named | `datasphere/native/family.py::FINITENESS_PROBE`, `check_finite`; verified from a cwd with no `algos` on the path | decided | **The general shape: a fail-closed gate whose failure mode includes its own preconditions will eventually fail a run it was built to protect.** The remedy was to remove the precondition, not to widen the path |

| 2026-09-02 | **RL-ViGen resumes automatically and does not save its replay buffer, which closes off the only cheap answer to the long-job problem.** `train.py:380` checks for `snapshot.pt` in the run directory and calls `load_snapshot()` if it is there — so a resume path exists and fires without being asked. But `keys_to_save = ['agent', 'timer', '_global_step', '_global_episode']`: **the buffer is not in it.** Resuming an off-policy run at 300k therefore continues a policy against a replay buffer refilled from empty, which is a different experiment, not a continuation. This matters because the production arithmetic produces jobs longer than any plausible platform limit — `soda` needs about 78 hours at 600k — and splitting one run across two submissions was the obvious remedy. It is not available to the seven off-policy baselines. It would be closer to sound for the four on-policy ones, whose jobs are 5–10 hours and do not need it | `RL-ViGen-upstream/train.py:352-372, 380-382` | open | **The long-job problem must be solved by budget or by tier, not by resume.** Also a latent hazard: a reused run directory resumes silently, so per-cell run dirs are load-bearing rather than tidy |

| 2026-09-02 | **Local rehearsals run the candidate's training code against the *original* tree's robosuite.** A rehearsal traceback pointed at `/Users/…/ccm-intro/projects/many-gens-rl-vigen/RL-ViGen-upstream/third_party/robosuite/…` while the code under test was the isolated candidate in `/tmp`. Cause: `robosuite 1.4.0` and `procgen` are **editable installs** in the shared venv (`__editable__.robosuite-1.4.0.pth`) resolving to the original checkout, and the launcher puts only `$RLV`, `$RLV/algos` and `$RLV/envs/robosuiteVGB` on `PYTHONPATH` — `third_party/robosuite` is not among them, so `import robosuite` falls through to site-packages. On DataSphere this cannot happen: the payload installs its own. **So `setup/apply_patches.py --check` can certify the candidate's `third_party/robosuite` (P9) while a local run exercises a different copy of that file** | editable `.pth` in `barannikov-work/.venv`; rehearsal traceback path | open | Not fixed: prepending the tree's robosuite locally would diverge from the container, which is the thing rehearsals exist to imitate. **Recorded as a standing caveat — a local rehearsal is evidence about the training code, not about the vendored environment** |

| 2026-09-02 | **`num_eval_episodes` is a total divided by scenes, and three of twelve run no periodic evaluation at all.** P14's loop computes `per = max(1, self.cfg.num_eval_episodes // max(1, len(scenes)))`, so the production value everyone reads as "episodes per scene" is a *budget* spread over the sweep: `EVAL_EPISODES=2` across ten certified scenes is **one episode per scene**, and the floor at 1 means the flag silently stops meaning anything below ten. Auditing the rest to see whether one production value could serve all twelve found it cannot, and not by degree: `ppg`'s `train.py` evaluates nothing at any point; `ibac_sni` evaluates nothing in training and defers to its own `scripts/evaluate.py`; `ctrl` steps `env_test_ID` and `env_test_OOD` **inside** the training loop, so it has no cadence and no episode count to set; `idaac`'s cadence is `--log_interval` in *updates* of 1024 frames; `alda`'s is `eval_n_steps` in environment steps. **Four different shapes, not four different numbers** | `scripts/audit_eval_cadence.py` (new, 12/12 anchors); `RL-ViGen-upstream/train.py:152` | decided | Instrument written, with `--check` pinning a file:line for every audited claim. **The consequence is a design conclusion rather than a defect: R3's "metrics on the same axes" is not reachable from training logs by construction, and is reachable offline from checkpoints — which is the separate evaluation harness the base version already calls for** |

| 2026-09-02 | **A production defaults block written this session repeated C45's exact error, and the register caught it two hours later.** `families.json` gained a `production` block per family; the first pass set one `eval_every` and one `eval_episodes` across all seven, and asserted `dmc_gb` evaluates a single regime with no train-regime denominator, so that "retention is not computable for rad/soda from training logs". **The register's own entry of 2026-08-18 says otherwise and is right**: `dmc_gb/src/train.py:139-141` calls `evaluate()` on the training env *and* on `test_env`, logging `episode_reward` and `episode_reward_test_env` separately — and `normalize_curves.py` was already reading both. So the pipeline was correct and the new description of it was not. Same pass also under-counted `alda`, which builds `env='train'`, `color_env='eval-easy'` and `distract_env='eval-hard'` and evaluates all three: **it is the only baseline in the set that constructs `eval-hard` at all.** Corrected in the audit, the record envelope and the production block | `datasphere/native/families.json`; `runnable/dmc_gb/src/train.py:139-141`; `runnable/alda/trainers/alda_trainer.py:146-148, 607-611`; the 2026-08-18 register row | decided | **This is C45's failure shape reproduced by the person who had just re-read C45** — a claim verified on one family generalised to seven, written into an artifact that looked uniform. It was caught by reconciling against the register rather than by any test, which is an argument for the register and against trusting a fresh audit over a settled one |

| 2026-09-02 | **The record envelope carried the axes that vary and not the conventions that decide what a number means.** Every record already names `regime` and `scene_set`, because those change per measurement. The per-baseline constants did not appear at all — and those are the ones a reader comparing two rows cannot recover: [C1](CONSTRUCTION.md#c1)'s time-limit split (nine baselines zero the value bootstrap on a task where **every** episode ends by time limit, three bootstrap through it), render size, frame stack, and the shape of whatever training-time evaluation exists. Schema 2 carries them on every row. They are duplicated from `rlgen/protocol.py` rather than imported, because `rlgen` is not a payload member and `normalize_curves.py` runs on the container — so `tests/test_record_conventions.py` asserts the copy agrees with the protocol and with the cadence audit, which is what makes it one source of truth in practice rather than in intention | `datasphere/native/normalize_curves.py::CONVENTIONS`, `SCHEMA = 2`; `tests/test_record_conventions.py` (39 tests) | decided | C1's mechanical remedy was already done and pinned by `tests/test_truncation_split.py` — the field is per-baseline inside the hash. **What was missing was that the split never travelled with the data**, so a table assembled from these records could still pool the two conventions silently |

| 2026-09-02 | **A tripwire caught an undischarged ledger obligation from earlier in the same session, and named its own remedy.** `ppg` gained a `--seed` flag and seeding of `random`/`numpy`/`torch` earlier on 2026-09-02, because without it every ppg run this project could launch was seed 0 with an unseeded network and a three-seed row would have been three runs of one configuration. The construction was right and the ledger entry was not written. `tests/test_seed_control_audit.py` had been built with a tripwire for exactly this — asserting ppg *stays* unseeded, with the message *"If a seed knob was added, C20's finding changed and the register must say so in the same commit"* — and it failed on the next full-suite run. **[C20](CONSTRUCTION.md#c20) now carries a dated correction, and the tripwire points the other way**: the failure worth catching is ppg silently losing the knob, which would restore a defect indistinguishable from a working seed set. Count moves eleven → twelve seed-controlled | `tests/test_seed_control_audit.py::test_twelve_of_twelve_are_seed_controlled`, `::test_ppgs_seed_knob_is_ours_and_stays`; `runnable/ppg/phasic_policy_gradient/train.py:43-58, 110` | decided | **The instrument worked exactly as designed and the discipline around it did not** — the change and its record were supposed to land together and did not, by about eight hours. Worth recording because the recovery cost was one test run, which is the argument for building tripwires at the moment a finding is written rather than when it is challenged |

| 2026-09-02 | **The isolated candidate cannot run its own full suite green, and twelve failures split three ways.** Running the whole suite in the candidate gives 12 failures; running the same four files in the untouched original gives **37 passed**. So they are properties of the candidate, not of the project. **Eight** (`test_rlvigen_reference`) and **four** (`test_verify_cells`, `test_results_table`) come from a **deliberate slimming, not a copy accident**, and chasing them established something worth keeping: the candidate's vendored `RL-ViGen-upstream` is **1,591 files lighter** than the working copy — 937 under `third_party/robosuite` (313 `.obj`, 177 `.stl`, 137 `.mtl`, 97 `.dae`, 70 `.png`, 56 `.py`, 46 `.xml`), the DMCVGB video and colour tensors, and the `exp_local/` run history. **The slimming does not reach production, and the first version of this row said it did.** Reasoning from `payload_members` (one launcher) and the absence of robosuite from `requirements-native.txt`, I concluded the base payload ships the vendored tree and that five completed Door cells proved the slim copy sufficient. Reading `run_probe.sh:365-368` shows otherwise: the container **clones `github.com/gemcollector/RL-ViGen` and checks out the pinned commit `90d8b8c40acb63af6f938c1f4cc79a0cfee7d7ec`**, then `pip install --no-deps -e` both `third_party/robosuite` and `envs/robosuiteVGB` *from that clone*, and applies P1–P18 to it. So the remote runs prove the **cloned, complete** tree works and say nothing about the slim one. The candidate's `RL-ViGen-upstream` is a local-work copy only, which also makes the slimming harmless rather than load-bearing. What the slimming also removes is `RL-ViGen-upstream/results/evaluation_score.xlsx`, which no run reads and only these tests do. All twelve now `skipif` on the specific absent asset, and the two `test_results_table` cases that do not render — the refusal path and the denominator rule — deliberately still run. **One failure is left standing on purpose**: `test_docs_integrity::test_internal_links_resolve[PROJECT-INDEX.md]`, whose link `../../gen-rebuttal/vigen-idaac` points at a sibling project the candidate did not copy. The link is correct — it resolves in the real workspace, where the whole file passes — so weakening a genuine link check to accommodate an isolated copy would trade a real guarantee for a cosmetic green. **Two** (`test_seed_control_audit`) were real and are fixed in the row above. A further four (`test_docs_not_stale::test_quoted_patch_range_matches_the_registry`) are **candidate-only**, because only the candidate's `apply_patches.py` declares past P14: the docs say `P14` while it declares `P18`, since P15, P17 and P18 were added without touching `RUNNABLE-ORIGINALS.md`, `STEP-ZERO.md`, `INTEGRATION-DELTA.md` and `README.md`. (There is no P16; the ids are not contiguous.) **The original tree fails `test_docs_not_stale` too, but on different tests** — `test_quoted_deviation_totals_match_the_ledger` for `README.md` and `AUDIT-2026-08-17.md`, and `test_per_baseline_figures_match_the_ledger` — so the two trees are stale in different places and neither is a clean baseline for the other. *(This sentence replaces a claim, written minutes earlier in this same row, that the patch-range failure occurred in both trees. It was asserted from the candidate's result without running the original, and the original disproves it.)* | full-suite run in `/tmp/ccm-intro-native-recovery-workspace-2026-08-31/…`; same four files green in `projects/many-gens-rl-vigen`; `test_docs_not_stale` run in both | decided | Fixed by skipping on the asset, not by carrying it: a red suite that means *"this is the slim tree"* trains the reader to ignore red. **Recorded with its own correction attached rather than rewritten**, because the mistake is instructive: I inferred what the container runs from the payload manifest instead of reading the runner, and the manifest was true but not the whole mechanism. Consequence for local work: a rehearsal exercises a *different* RL-ViGen tree from the one production uses, in the slim direction locally and the editable-install direction for robosuite |

| 2026-09-02 | **Every paid job depends on an unauthenticated GitHub clone succeeding, and that is exactly where `alda` and `ctrl` died.** `run_probe.sh:365` runs `git clone https://github.com/gemcollector/RL-ViGen.git` inside the job, then checks out the pinned commit `90d8b8c4` and installs robosuite and robosuiteVGB editable from it. The pin is right and the reproducibility is real — what is fragile is that the clone happens **at run time, over the network, unauthenticated, in a job that is already being billed**. The two baselines with no successful CUDA run to this day, `alda` and `ctrl`, both failed here rather than in their own code. On a short probe that costs a bootstrap; the exposure grows with the run, and the production schedule contains jobs of 9 to 78 hours whose first act is this clone | `datasphere/native/run_probe.sh:365-368`; the alda and ctrl probe failures | decided | **FIXED the same evening and proven.** The remedy was task 18 — ship the pinned tree as a hash-checked job input — gated until then on learning whether the failure was a rate limit or a transient; it was neither, and it had already cost two baselines their entire CUDA coverage. `run_probe.sh` now prefers `RLVIGEN_ARCHIVE`, verifies its sha256 against `datasphere/native/rlvigen-source.json` before extracting, and keeps the network clone as an announced fallback (`NATIVE_RLVIGEN_NO_INPUT`). Runner contract 8 → 9. Job `bt11hmivok284v4jpn0n` carries `NATIVE_RLVIGEN_FROM_INPUT commit=90d8b8c4… sha256=3f7c4dd4…` and completed its cell. Every job since has reached its own baseline's code |

| 2026-09-02 | **The protocol hash certified a P1–P14 tree while a P1–P18 tree ran — the exact failure that field exists to prevent.** `Protocol.env_patches` is inside `Protocol.hash()`, and its own comment says the point is that a run's patch set is recoverable from its hash. P15 (terminal eval + snapshot), P17 (the `drq` entropy crash) and P18 (preserve intermediate snapshots) were applied to the vendored tree and **the declaration was never updated with them**, so every number produced since P15 landed carries a hash naming fourteen patches over a tree with seventeen. Two of the three missing ones are **ENABLES** — they add measurements — so this is not a bookkeeping slip: `Protocol.hash()` would have reported two runs as protocol-identical across a change in what is measured, which is precisely what the hash is for. Fixed in the declaration, and `tests/test_contract.py`'s ENABLES pin moved from `[P6, P10, P11, P14]` to `[P6, P10, P11, P14, P15, P18]` with the reasoning for each | `rlgen/protocol.py::env_patches`; `tests/test_contract.py::test_env_patches_matches_the_patch_script`, `::test_the_enables_set_is_what_makes_a_number_ours` | decided | **Third undischarged ledger obligation found in one day, all from the same cause**: a change made correctly, its record left for later, and later never arriving in the same session. The other two were `ppg`'s seed knob (C20) and the P14→P18 doc ranges. In all three the instrument was already built and fired on the next full run — the discipline that failed was not detection but *landing the change and its record together*. **Note the asymmetry that makes this one worse than the other two**: a stale doc misleads a reader who can check the code, while a stale hash silently certifies runs as comparable that are not |

| 2026-09-02 | **The runtime GitHub clone now fails on DataSphere for every baseline, and it is not a rate limit, a transient, or upstream going away.** Two probes submitted with the clone retried three times (`bt1nlcstc5kg3qtqro52` alda, `bt1uk6ehfsfo6m49dsdn` ctrl) both died identically after all three attempts: `fatal: could not read Username for 'https://github.com': No such device or address`. **An anonymous clone of a public repo never prompts for credentials**, so that message means the container's request is not reaching GitHub as a normal anonymous fetch. Checked from this machine at the same moment: `api.github.com/repos/gemcollector/RL-ViGen` returns **200** and `git ls-remote` returns exactly the pinned `90d8b8c40acb63af6f938c1f4cc79a0cfee7d7ec`, which is also what the vendored tree is at. So upstream is fine and reachable; the container is not. **And it worked before** — the RL-ViGen five completed Door cells from this same runner — so DataSphere's egress changed under us. `apt-get` and `pip` still work in the same job, which fits an egress policy that permits the platform's own mirrors and not the general internet | job logs `bt1nlcstc5kg3qtqro52`, `bt1uk6ehfsfo6m49dsdn`; `curl`/`git ls-remote` from the laptop; `run_probe.sh:365` | decided | **This is why `alda` and `ctrl` have never had a CUDA run, and it now blocks all twelve.** The retry did not fix it and was never going to — what it bought was a legible error instead of a bare failure, which is what turned a three-week-old suspicion of "rate limit or transient" into a diagnosis in one attempt. Remedy is task 18, no longer optional: ship the pinned tree as a hash-checked job input. **The archive must be PRISTINE, not the working tree** — `git status` on the vendored copy shows 15 modified files, because it is patched, and shipping that would apply P1–P18 twice |

| 2026-09-02 | **Reward normalisation exists in three of twelve and changes NO reported number — established by checking wrapper order, after a first pass of this row claimed the opposite.** `ctrl`, `idaac` and `ppg` (the Procgen on-policy lineage) normalise rewards; `rad`, `soda`, `alda`, `ibac_sni` and the RL-ViGen five do not. The first version of this entry read that `ctrl` "reports reward-NORMALISED returns from its training loop and RAW returns from its offline evaluator", on the strength of `train_ppo.py` passing `normalize_rewards=True` to both test envs under a comment saying *"Report unclipped rewards for test"*. **That was wrong, and the thing that settles it is where the monitor sits.** In `ctrl/vec_env.py:608-611` and `idaac/ppo_daac_idaac/envs.py:104-105` the order is `VecMonitor` **inside** `VecNormalize`, so the monitor records the raw reward and normalisation only scales what the agent trains on; in `ppg/ppo.py:220-222` `gather_roller_stats` runs one line *before* `seg["reward"] = reward_normalizer(...)`, same outcome by a different route. **So all twelve report raw episode returns**, and upstream's confusing comment is in fact consistent with its observable behaviour | `runnable/ctrl/vec_env.py:592,608-611`; `runnable/idaac/ppo_daac_idaac/envs.py:104-105`; `runnable/ppg/phasic_policy_gradient/ppo.py:220-222`; `runnable/_patches/ctrl.patch:353-383` confirms our `_mk` refactor preserved `True → True` | decided | **A positive comparability result, and it was nearly recorded as its opposite.** The near-miss came from reading a constructor argument and inferring the metric, when the metric depends on wrapper *order* — the same shape as the earlier `dmc_gb` regime miscount, and the second time in one session that a families-wide claim was drafted from one file. What the finding does support, still: `normalize_rewards` is a live parameter on our `RLViGenVecEnvCustom`, so the offline harness can construct a raw-reward env deliberately rather than by luck |

| 2026-09-02 | **"Evaluation" names two different estimators across the twelve, and one baseline's own evaluator cannot read its own checkpoints on this target.** Auditing what an offline harness needs beyond the weights — the precondition the owner set before committing to a design — turned up two things no column anywhere recorded. **`idaac` SAMPLES its evaluation actions**: `model.py:332` is `def act(self, inputs, deterministic=False)` and `test.py:53` calls `actor_critic.act(obs)` without the flag, so it takes `dist.sample()` while the RL-ViGen five take `dist.mean` and `dmc_gb` takes `mu`. Same word, different estimator, and for a policy with a wide action distribution the gap is not small. **`ctrl`'s `evaluate_ppo.py` is discrete-only**: `select_action` is `logits.argmax(1)` or `jax.random.categorical(key, logits)`, both a categorical over discrete actions — and robosuite Door is a 7-DoF continuous space whose head, the one this project gave `ctrl`, emits a mean and a log-std rather than logits. Its evaluator cannot run against our checkpoints at all. **`ppg` has no evaluation code of any kind.** So the offline harness reuses ten baselines' own paths and must author two | `scripts/audit_eval_state.py` (new, 12/12 anchors, `--check`); `runnable/idaac/ppo_daac_idaac/model.py:332`, `runnable/idaac/test.py:53`; `runnable/ctrl/evaluate_ppo.py:67-74` | decided | Instrument written and the axis now rides on every record as `conventions.eval_policy_mode`, so a return produced by sampling can no longer be pooled with one produced by a mode. **This is the concrete answer to "will the external evaluator truly house all the normalisation options the original algorithms use?"** — for ten, by reusing their own code; for two, not at all, and that is a work item rather than a design choice |

| 2026-09-02 | **`eval-medium` and `eval-hard` work, had never once been constructed, and do not order by difficulty the way their names do.** Both regimes existed in `robosuitevgb/utils.py` and nothing in this project had ever built one — the standing item read "never exercised once, by any baseline". Run locally through `scripts/probe_regimes.py` at one episode per sample: **all four construct, step and render**, and each separates from `train` at 3.4×–4.2× the within-regime control (two samples of the same regime at different seeds). `eval-hard`'s video background loads from `envs/robosuiteVGB/robosuitevgb/assets/video/eval-hard/` — inside robosuiteVGB, *not* DMCVGB, so the shipped archive's exclusions do not touch it. **The wrinkle: `eval-medium` sits FURTHER from `train` (0.657) than `eval-hard` does (0.505).** Reading the config explains it — `eval-medium` sets `except_robot: False`, so the robot itself is randomised, while `eval-hard` keeps the robot fixed and adds a moving light and a video background. So the two vary different things, and "hard" is not "medium plus more" | `scripts/probe_regimes.py` run 2026-09-02; `RL-ViGen-upstream/envs/robosuiteVGB/robosuitevgb/utils.py:73-90`; `vgb_wrapper.py:250-252` | decided | Retires the "never exercised" unknown and licenses the four-regime grid **at the input level only** — a random policy's observation histogram says nothing about whether a trained policy behaves differently. **The naming point is the one that will bite a report**: presenting easy → medium → hard as an ordered difficulty axis would assert a monotonicity the instrument does not show, and the config says they are two different perturbations rather than two amounts of one. There is also an `eval-extreme` video directory in the assets that no code path here references |

| 2026-09-02 | **Door is 7-DoF and latched, and the latch is a fact about the task nothing here had recorded.** Measured rather than inferred: `robo_make("Door", …).action_spec()` is `shape (7,)`, float32, `[-1, 1]` — six OSC_POSE dimensions plus one gripper, so the "extra control" worth worrying about is not an eighth action. What the measurement *did* turn up is `use_latch=True` on the constructed env (robosuite's default, which RL-ViGen never overrides): **the door is spring-loaded and locked shut, so the policy must rotate the handle before pulling.** The shaped reward says the same — `door.py:238-246` gives `0.25*(1 - tanh(10*dist))` for reaching *plus* `clip(0.25*abs(handle_qpos/(0.5*pi)), -0.25, 0.25)` for rotating, and 1.0 per step once `_check_success` holds. Also measured on the same env: `SingleArm` + `OperationalSpaceController`, horizon 500, `reward_scale 1.0`, `reward_shaping True` | `robo_make("Door", …).action_spec()` and the constructed `VGBWrapper`, run 2026-09-02; `third_party/robosuite/robosuite/environments/manipulation/door.py:139,168,238-246` | decided | **This is why the return scale is what it is**, and why a run can sit near 250 for a long time: reaching and rotating cap at ~0.5/step and only opening pays 1.0. It also means Door is a two-stage task, which bears on any claim that a baseline "fails to learn" — failing to unlatch and failing to pull are different failures and the return alone does not separate them |

| 2026-09-02 | **The entropy-coefficient risk is unequal across the four PPO-family baselines, and `ibac_sni` is the exposed one — an addition to C61, which already had the mechanism.** Re-deriving it on a challenge reproduced C61's analysis rather than extending it: that entry already states the bonus is bounded by `log|A|` for a categorical and unbounded for a Gaussian, that `∂H/∂log σ = 1` per dimension is a permanent upward force, and that C6's 31.8% boundary fraction is therefore a lower bound that grows. **That was written before today and is not new here.** What is new is per-baseline, and it changes where to look first. **Clamping**: `ppg` clamps `logstd.clamp(-5, 2)`; `idaac` (`AddBias(zeros)`), `ibac_sni` (`nn.Parameter(zeros)`) and `ctrl` (`param('log_std', zeros)`) do **not**. **Opposing scale**: `idaac` normalises advantages (`algo/idaac.py:61`), and `idaac`, `ppg` and `ctrl` normalise rewards — **`ibac_sni` does neither**, so its policy-gradient force is whatever Door's raw shaped return produces while the entropy force is a fixed 0.01 | `scripts/metrics.py::gaussian_policy_health`; `runnable/ibac_sni/torch_rl/scripts/train.py` logging block; the four coefficient sites above | closed | **[CONFIRMED BY MEASUREMENT 2026-09-04, on the baseline this row named and for the reason it gave.** This row predicted `ibac_sni` as the exposed one from mechanism alone — no clamp, no advantage normalisation, the 227x model of [C3](CONSTRUCTION.md#c3) — while the instrumentation to test it did not yet exist on three of the four heads. It now does, and `bt1cj6rgeptsu9f3v0o6` (100k) reports `boundary_fraction` **0.318 → 0.580**, crossing the 0.50 trip-wire at ~50k, with `mean_log_std` **0.002 → 0.592** against `idaac`'s 0.024 at the same budget — **25x the drift**. Extrapolated to 6e5 the four rank `ppg` ≈0.12 (clamped) < `idaac` ≈0.145 (normalises advantages) < `ctrl` ≈0.29 (neither) < `ibac_sni` 0.59 **measured**, which is the order this row's mechanism predicts. Closed as a prediction that was tested and held; what it implies for the coefficient is [C61](CONSTRUCTION.md#c61)'s default and remains the owner's.]** **Instrumented, not corrected.** `boundary_fraction` — the mass the policy puts outside the action box, `erfc(1/(σ√2))` averaged over dimensions — is the number that separates healthy exploration from dissolution: 0.317 at σ=1, 0.617 at σ=2, 0.841 at σ=5. Wired into `ibac_sni` first because it is the ranked-worst case. **The decision left open is whether to clamp the three unclamped heads at ppg's own `[-5, 2]`** — an in-family precedent rather than an invented bound, inert in any sane regime, and the only thing that would stop a slow σ drift from consuming a nine-hour run |

| 2026-09-02 | **"`ctrl` needs an authored evaluator" was too strong: it needs repointing, and I found that only because the claim was challenged.** The finding stands that `evaluate_ppo.py:67-74` is discrete-only — `logits.argmax(1)` or `jax.random.categorical` — against a measured 7-DoF continuous space. What the first version of the audit missed is that **`ctrl/algo.py:40-61` already contains the right function**: a `select_action` that returns a *distribution* and takes `pi.mode()` or `pi.sample(seed=key)`, correct for either action space, and it is what training already uses. The eval loop calls its own local helper instead. Same correction for `ppg`, in the other direction: it has no eval loop, but `distr_builder.py` builds the distribution and clamps the log-std, so what is missing is a loop, not a policy | `runnable/ctrl/algo.py:40-61` vs `runnable/ctrl/evaluate_ppo.py:67-74`; `runnable/ppg/phasic_policy_gradient/distr_builder.py:31`; `scripts/audit_eval_state.py` | decided | Audit and tests now say **ADAPT** rather than **NO** for both, and the test asserts nothing is unreachable. **The general lesson is about how the first claim was formed**: I read the evaluator, found it discrete, and concluded the family could not evaluate — without reading the trainer of the same repository, which solved the problem already. "This code cannot do X" is a claim about a whole repository and needs to be checked against the whole repository |

| 2026-09-02 | **The offline evaluation grid runs, and it is the first time any regime beyond `eval-easy` has been measured with an actual policy.** `scripts/eval_grid.py` loads one checkpoint and sweeps regimes × scenes, emitting schema-2 records with `regime`, `scene_set`, `phase="offline-eval"` and the per-baseline `conventions` block. Validated against the local production-mode rehearsal's `snapshot.pt` at 2 regimes × 2 scenes × 2 episodes: six records — a row per scene plus an aggregate per regime carrying its own `per_scene_mean` breakdown. Returns are 0.6–2.7 with success 0.00, which is the *correct* shape for a 3,000-frame checkpoint: the endurance run needed ~75,000 frames before success appeared. **The regime probe of the same day exercised eval-medium and eval-hard with a random policy; this is the first trained one.** It reuses `eval_across_scenes.run_scene`, which verifies the env reports back the regime it was asked for, so a row cannot be silently measured in the wrong one | `scripts/eval_grid.py`; `/tmp/grid-test.jsonl` from `/tmp/prod2/out/cells/drqv2-s8/snapshot.pt` | decided | Covers **the RL-ViGen five only**, and says so in its own docstring rather than in a caveat somewhere else — `audit_eval_state.py` holds what the other seven need. It is R7's fourth requirement in working form: an evaluation harness separate from training, which is both the base version's own shape and the only way R3 is reachable, three of twelve running no periodic training-time evaluation at all |

| 2026-09-02 | **`ppg`'s retained checkpoint disagrees with its own training log about the policy, and the disagreement says the saved model is not the trained one.** The differential entropy of a diagonal Gaussian depends **only** on σ, so the logged `Opt/entropy` pins the trained `log_std` exactly. At d=7 and `log_std=0` the entropy is **9.932570**. The run logged **9.936886** at Iter 0 and **9.937096** at Iter 1 — implying `mean log_std` of **0.000617** and **0.000647**. The saved `pi_logstd` is **exactly `[0,0,0,0,0,0,0]`**, `absmax 0.0`, which is its initialisation. **The gradient path is not the problem** — checked in isolation: `log_prob` produces a nonzero gradient on `pi_logstd`, and an entropy-only backward produces exactly **−0.04 per dimension** (4 samples × 0.01), which is C61's constant force, measured. So the parameter is trainable, it demonstrably moved during training, and what was written to disk is the pre-training value | `.../onpolicy-v30-bt1g0v04qmf0kci2lv7m/.../cells/ppg-s1/{snapshot.pt,progress.csv}`; isolated gradient check against `phasic_policy_gradient.distr_builder` | closed | **[CLOSED 2026-09-04 by measurement, not by argument.** The terminal save added after this row was written is what `retain()` now takes — `retained.json` records `checkpoint_source: model_terminal.jd` — and today's probe checkpoint (`bt1apmmvvtfvjveil6ko`, 4,096 interacts, far below the 100,000 `ic_per_save` boundary that caused the original defect) was loaded and inspected directly: **`pi_logstd` mean 0.00090310, absmax 0.00476238 — not zeros.** Its own `progress.csv` logged `mean_log_std` 0.000819 at frame 4096, and the checkpoint sitting slightly beyond that is exactly what a save taken after the final update should look like. So the file on disk is the trained model, `ppg` is offline-evaluable, and the construction-time save is no longer what is retained. **The row stayed `open` for two days after the fix landed** — the same prose/status drift the new `test_no_register_row_calls_itself_resolved_while_its_status_says_open` guard now catches for the explicit cases.]** **Originally blocking for `ppg`'s row: evaluating this checkpoint would report an untrained policy as ppg's result**, and `retain()` passed it because a file of the right name existed and was non-empty. [C60](CONSTRUCTION.md#c60) recorded that upstream `ppg` never saves at all and this project added a terminal save; **that save is now suspect rather than sufficient.** **MECHANISM FOUND, same session, and it explains everything including why the file exists:** `LogSaveHelper.__init__` calls `self.save()` **unconditionally when `ic_per_save > 0`** (`log_save_helper.py:50-51`) — a save at construction, before a single gradient step — and `ic_per_save` **defaults to 100,000** (`:34`). The probe ran **4,096** interacts, which never crosses a 100,000 boundary, so `__call__` never saved again. **The file on disk is the model as constructed.** This also corrects [C60](CONSTRUCTION.md#c60), which recorded `ppg` as never saving because "`ic_per_save` is never set by any launcher": it is never set and it does not need to be, because its *default* is non-zero — so ppg saves at construction and every 100k thereafter, and at probe scale that is the construction save alone. **At the 600k production budget six boundaries are crossed, so a production ppg checkpoint would be trained — but it would be the model at the last 100k boundary, not the terminal one**, which is a different defect wearing the same clothes. **Both fixed the same day**: `train.py` now writes `model_terminal.jd` after `learn()` returns and `families.json` retains that as ppg's checkpoint, with the helper's `model.jd` kept beside it for comparison. **And the class of failure is now instrumented rather than left to arithmetic** — `check_finite`'s probe reports `policy_log_std` for any checkpoint carrying a state-independent log-std, including `all_exactly_zero`. Validated on both known cases: ppg's retained file reports `mean_log_std 0.0, all_exactly_zero True`; ibac_sni's reports `0.0447, False`. Reported and not fatal, because a legitimately short run can barely move it and this probe should not be what decides that. **The class was then checked across the other families rather than assumed unique**: `dmc_gb` is immune twice over — `train.py:146` guards its periodic save with `step > start_step`, excluding step 0, and an earlier session had already added `finalize_run`'s terminal save for the neighbouring reason that "`save_freq` defaults to 100k, so a 10k or 100k run finishes with NO checkpoint at all"; `idaac` saves on the final update only; `ctrl` and the RL-ViGen five write a terminal checkpoint (P15). **So the defect class was known and defended against in four families, and `ppg` slipped through because its mechanism is different** — not a modulus that step 0 happens to satisfy, but an unconditional save inside the helper's constructor |

| 2026-09-02 | **A fix that worked locally shipped as nothing at all: `alda`'s `models` shim was an absolute symlink, and the payload builder wrote no entry for it.** With the clone blocker gone, `alda` reached its own code for the first time and failed the import gate on `ModuleNotFoundError: No module named 'models'`. The cause is not the shadowing that shim exists to solve — that analysis holds — but the shim's *form*. `runnable/_shim/alda_models/models` was a symlink to an **absolute** path under the developer's `/tmp` workspace; the payload builder emitted no entry for it, so the container had a declared `PYTHONPATH` entry pointing at a directory that did not exist, while `third_party/alda/models` (a declared payload member) shipped correctly all along. Replaced with a real file that re-points the package's `__path__` at the real directory, so `models.sac` resolves **without** putting `third_party/alda` on `sys.path` — which is what would shadow ALDA's namespace `trainers` with `third_party/alda/trainers`, the original problem. Declaring it needed one more step: the builder forbids a `models` component in whatever part of a path a declaration does not name, so `runnable/_shim/alda_models/models` is now named exactly, leaving `__init__.py` as the remainder | job `bt18fm6ds7a1kpetgqpe` stdout; `runnable/_shim/alda_models/models/__init__.py`; `datasphere/native/families.json` alda `payload_members` | decided | **Recorded because the failure mode is "verified locally, absent remotely", and nothing tested the difference.** The local rehearsal could not catch it — locally the symlink resolves. The payload builder could have: it silently skipped a declared-directory entry rather than refusing it. A test now asserts the shim is a real file and not a symlink, which is the cheap half; the expensive half, verifying that every declared `import_gate.pythonpath` entry exists *inside the built payload*, is not yet written and would have caught this before a job ran |

| 2026-09-02 | **The project's only evidence that any baseline learns Door does not reproduce from its own checkpoint, and the gap is 70×.** The `drqv2` seed-12 endurance run reports a train-regime denominator of **445.5 at 75k and 476.3 at 100k with `train_regime_success 1.0`**, and its own training episodes agree — `episode_reward` 468.7 / 479.2 / 464.6 at frames 98.5k–99.5k. Evaluating **that run's own terminal snapshot** offline on the same regime and the same scene gives **6.8** with success **0.00**. Everything that could make this an artefact of my new code has been ruled out: the snapshot is the right model (`_global_step 100000`, `_global_episode 199`, matching `eval.csv`'s last row); two independent offline paths agree to two decimals (`eval_grid.py` 6.83, the older validated `eval_across_scenes.py` 6.81); the env seed matches the run's (12) and changing it moves nothing (4.52 at seed 0); episode length is 500 with `action_repeat` 1 on both sides; and both construct through the same `robo_make(name, action_repeat, frame_stack, seed, mode, scene_id)` that `train.py:92-95` uses. **And the offline path is not simply broken: on `eval-easy` it AGREES with the run's own log — 2.56 against 1.87.** It is only the train-regime number that will not reproduce | `endurance-v27-bt1a5lp648o6bfehnfsg/…/drqv2-s12/{snapshot.pt,train.csv,eval.csv}`; `scripts/eval_grid.py`; `scripts/eval_across_scenes.py`; `RL-ViGen-upstream/train.py:92-95,188-194` | closed | **[Status corrected 2026-09-04: this row said `open` while its own prose said RESOLVED, the second such disagreement found by one scan of all 137 dated rows — the other was the `sgqn`/P19 row. The finding here is discharged: the cause is the renderer ([C95](CONSTRUCTION.md#c95)), the container reproduces the run's logged number, and what remains is not this question but the standing platform rule.]** **HIGHEST PRIORITY, and it invalidates a headline rather than a detail.** `PRODUCTION-READINESS.md` reports this run as the generalisation result — 476 on the training distribution against 1.87 held out, "total generalisation gap, retention 0.4%" — and the 476 is the half that does not survive contact with its own checkpoint. Either the saved agent is not the acting agent (the failure `ppg` was caught with the same day, though `_global_step` argues against it here) or an env constructed **inside a running trainer** differs from a freshly constructed one in a way worth tens of reward per step. **FIRST DIAGNOSTIC DONE, and it rules out the kinder explanation.** The hypothesis was that an env built inside a running trainer differs from a freshly built one. Tested directly: build `train_env` exactly as `train.py` does, then build the eval env second, in the same process, then evaluate — **4.93**, and the train env itself gives **2.76**. Prior env construction changes nothing. **So the difference is the agent, not the environment.** Three *consecutive training episodes* logged 468.7 / 479.2 / 464.6, so the acting agent really was scoring ~470; the pickled agent scores ~5 from the same run's own snapshot. **FIFTH DIAGNOSTIC, and it overturns the leading hypothesis: these checkpoints are NOT near-init.** The obvious reading was the `ppg` failure shape in the largest family — a saved model that is not the trained one. Weight norms say otherwise. The actor's last-layer weight norm is **2.77** in the 6,000-frame S0 checkpoint and **9.66** and **10.03** in the two 100k ones; encoder conv-0 norms are ~8.0 throughout. So the 100k saves carry a policy that has moved a long way from initialisation — they are *trained* models that do not perform. **That kills the stale-save story for `drqv2` and leaves the defect genuinely open**: an agent whose weights show 100k of training, in the environment and action mode that produced ~470, scoring ~5. **SIXTH AND LAST CHEAP EXCLUSION — the training env's own construction.** Every evaluation above used `mode='train'`; `train.py:78-79` builds `self.train_env` with **no `mode` and no `scene_id`** at all. Evaluated that exact call (`robo_make(name="Door", action_repeat=1, frame_stack=3, seed=12)`), sampling actions as training does: `[0.98, 12.56, 7.97]`. Identical, and the config says why — `robo_config.yaml` defaults `mode: train`, and the `train` branch sets `randomize_color = randomize_lighting = False`, so **the training regime is the unrandomized, fixed-appearance Door and `mode=None` is the same env.** *(That fact is worth carrying on its own: "train regime" is not a randomised distribution, so retention compares a fixed-appearance policy against randomised ones — see [C43](CONSTRUCTION.md#c43).)* **Six explanations excluded, all of them environmental or procedural. What remains is not cheap to test locally, and the two bracket jobs are the instrument.** **NINTH CANDIDATE, and it is the strongest one left — raised by the owner, not by me: the DEVICE.** Every offline evaluation here loads with `map_location="cpu"` and runs on CPU; every training run was CUDA. I had been treating that as neutral. This project's own record says it is not: `scripts/eval_across_scenes.py`'s C70 note reads *"Door's success test is a threshold (`hinge_qpos > 0.3`), so a hair's difference in one action can decide whether the door opens"*, and [C41](CONSTRUCTION.md#c41) documents the amplifier — a perturbed action changes the transition, which changes every state after it — with a measured example of two runs of the *same* configuration on the *same* backend diverging 48% by 40k frames. A CPU-versus-CUDA difference in the actor's forward pass is a much larger perturbation than that, applied at every step. **It also explains the shape the other candidates could not**: why 6,000 frames reproduces (an untrained policy scores ~1–2 on both devices, so there is nothing to diverge) while 50k, 60k and 100k do not, and why the two families that reproduce do so. `dmc_gb`'s offline run went through the **MPS-as-CUDA shim** with `--device cuda`. `idaac`'s ran on plain CPU — *(corrected within the hour: I first wrote that idaac used the shim too, and it did not)* — but `idaac` scores ~1.5 and never opens the door, so there is no threshold to cross and nothing for an amplifier to amplify. That is the same reason 6,000 frames reproduces, and it means neither family is evidence against this. **Tested locally on a second device, and it does not recover the performance.** The 60k stamp evaluated through the MPS-as-CUDA shim with `--device cuda` gives **8.58** (`[6.3, 9.8, 11.1, 6.5, 9.1]`) against **20.95** on CPU and **135.71** logged. Two different devices, both far below, and MPS is *lower* than CPU rather than closer — if this were purely device numerics one would not expect both wrong devices to agree on being wrong by an order of magnitude. **That weakens the hypothesis without refuting it**, because MPS is not CUDA and the shim only makes the CUDA API available. **The running resume job is still what settles it**: real CUDA, the real training loop, `EVAL_EPISODES=20`. If that returns ~135 the defect is a device artifact of my harness; if it returns ~8 the file does not carry the policy, on the hardware that wrote it | Note also that every training-time eval number here is a **single episode**: `num_eval_episodes=2` over ten scenes gives `per = max(1, 2 // 10) = 1`, so 476.3 and 445.5 are one draw each, while the offline figure is ten episodes that never once opened the door. **SECOND DIAGNOSTIC DONE — it is systematic, not one bad cell.** The sibling cell from the same pack shows the same thing: `drqv2-s13` logs a train-regime **481.11** and a final training episode of **481.55**, and its own snapshot evaluates offline at **18.38**. (`drqv2-s11` returned only a log — it is the cell the endurance job lost.) So both surviving cells of that pack report ~480 during the run and ~7–18 from the artifact the run left behind. **The trained-ness probe added the same day cannot see this**, because it reads a state-independent `log_std` and DrQ-v2 has none — its exploration is a schedule, not a parameter. **THIRD DIAGNOSTIC — it reproduces at short length and fails at long.** The S0 cell run the same evening (`bt11hmivok284v4jpn0n`, 6,000 frames, seed 1) logs a train-regime **1.215** and evaluates offline at **2.237** — agreement, both within single-episode noise. So the offline harness, the load path, the env construction and the action selection are all sound at 6k; something goes wrong between 6,000 and 100,000 frames. **That is where the next experiment goes and it costs one job:** a ~60k `drqv2` cell with `RLVIGEN_PRESERVE_SNAPSHOTS=25000`, giving stamped checkpoints at 25k, 50k and the endpoint, each evaluable against the eval row logged at the same frame. Whichever stamp stops reproducing brackets the cause — and note the first save boundary, `global_step % int(5e4) == 0`, falls inside that window. **FOURTH DIAGNOSTIC — not the action mode either.** The one remaining benign story was that the policy depends on its exploration noise, so `dist.mean` would understate it. Run both ways on the same loaded agent, in the training env, at step 100000: `eval_mode=True` gives `[1.50, 1.00]` and `eval_mode=False` — the sampling the *training loop itself* uses — gives `[0.99, 12.65]`. **Both are ~1–13 against the ~470 the training loop logged.** So the saved agent does not perform in the exact environment, with the exact action selection, that produced those episodes. **Also excluded, cheaply, before spending that job:** crossed artifacts between the three concurrently-run cells of the endurance pack. Each `retained.json` names its own `artifact_root` (`/tmp/native-work/runs/drqv2-s12` and `…-s13`), and the two snapshots differ by sha256, so neither cell retained another's file. **TENTH DIAGNOSTIC — the 100k bracket landed (`bt12o3nc3fg8g0ffhaso`, `drqv2-s2`, seed 2) and the defect reproduces on a third independent cell, more starkly than anywhere else.** Its `eval.csv` at frame 100000 reads `train_regime_reward` **456.06** with `train_regime_success` **1.0**, while `episode_reward` (the eval regime over ten scenes) reads **5.61** with success **0.0**; its own training episodes at 98k–99.5k read 453.9 / 458.4 / 455.6 / 382.3. That run's terminal `snapshot.pt` evaluated offline at train regime, scene 0, ten episodes gives **0.71, success 0.00**. So four cells across three jobs and three seeds (s2, s12, s13, and the 60k s1) all log 130–480 on the training distribution and all evaluate at 0.7–21 from their own saved file. **This also settles the shape of the curve: the gap is not a fixed offset but grows with training** — 6k reproduces, 60k logs 135.7 against 20.9, 100k logs 456 against 0.71. The better the policy gets, the less of it survives the save. **A hypothesis raised and killed in the same hour, recorded because it was wrong in an instructive way.** The eval-regime numbers (5.61 here, 1.87 for s12) sit suspiciously close to the offline train-regime numbers, which suggested my harness was silently measuring the randomised regime whatever `--regimes` asked for. It is not: offline train gives **0.71**, which is *below* the logged eval regime's 5.61, so the two are not the same measurement. The asymmetry is real and is the sharpest clue available — **the offline path reproduces the randomised regimes and fails only on the fixed one** (s12: offline `eval-easy` 2.56 against a logged 1.87). **Reward shaping is excluded, and cheaply.** A dense-versus-sparse mismatch between the two envs would scale returns without changing behaviour — but `train_regime_success` is **1.0** logged against **0.00** offline, and success is a threshold on `hinge_qpos > 0.3` that no reward setting can move. The door genuinely opens every time during training and never once offline, so the *behaviour* differs, not the bookkeeping. **What that leaves.** Same weights, same env constructor, same action mode, same regime, different behaviour — so the remaining candidates are device numerics, something the pickle does not carry, or an env difference not controlled by any constructor argument. The resume job (`bt1bhbb2pllbs2f6u10q`, resubmitted after v43 died on an empty replay buffer) is the discriminator: it hands the file back to the training loop, on CUDA, and reads `train_regime_reward` over twenty episodes at scene 0. **ELEVENTH THROUGH FOURTEENTH DIAGNOSTICS — four more exclusions, all local, all on the `drqv2-s2` 100k snapshot that logged 456.06 / success 1.0.** *(1) My own seeding interventions are not the cause.* [C69](CONSTRUCTION.md#c69) re-seeds global numpy before building the env and [C70](CONSTRUCTION.md#c70) sets `torch.use_deterministic_algorithms(True)`; `train.py` does neither at eval time, which made them the last structural difference between the harness and the loop. Evaluated with both removed: **0.68 mean over ten episodes, 0/10 success, range 0.5–0.8**. Unchanged, so they are excluded. *(2) The observation is well-formed.* `uint8`, `(9, 84, 84)`, range 0–254, the three stacked frames identical at reset and the last one changing after a step, `action_spec` `(7,)` float32 in [-1, 1], and a zero action earns 0.0024 of shaped reward — so the dense reward is on and a scale error at the encoder's `obs/255 - 0.5` is ruled out. *(3) The encoder is healthy.* Cosine similarity between features of two genuinely different frames is **0.489**, uniform noise gives **0.059**, an all-zero image gives a zero vector, and 92% dead ReLUs is ordinary sparsity for this architecture. It responds strongly to its input. *(4) The actor is deeply saturated, and that is the mechanism of the offline behaviour.* Pre-tanh logits read **[-58.7, 61.2, 104.1, 21.5, 108.5, 4.1, 33.2]**, so `dist.mean = tanh(...)` is exactly ±1 in 89% of entries; over a real episode four of seven dimensions sit at exactly −1/+1/+1/+1 with **standard deviation 0.0 across all 500 steps**, and 97.1% of all emitted action components are saturated. The policy is bang-bang. **The sharpest single number in this entry:** the offline policy earns 0.68 per episode where a *constant zero action* earns about 1.2, so the loaded agent is measurably **worse than doing nothing**, while the loop that saved it logged 0.91 reward per step — near the per-step maximum — and opened the door every time. **So the shortlist is now one item long.** The env is right, the observation is right, the encoder is right, the action mode, regime, scene, seed, device and my seeding are all excluded, and the actor loads in a state that cannot open a latched door. Either DrQ-v2 legitimately saturates to bang-bang and something outside the pickled agent supplied the competence, or the file does not carry the actor that acted. `bt1bhbb2pllbs2f6u10q` distinguishes them and nothing local can. **FIFTEENTH DIAGNOSTIC, AND IT MOVES THE DEFECT INSIDE THE TRAINER.** Until now this was "the training loop disagrees with my offline harness". It is not only that: **one `eval()` call contradicts itself, and the arithmetic is on RL-ViGen's own `eval.csv`.** `cfg-drqv2-bracket-v42.yaml` sets no `RLVIGEN_EVAL_MODE`, so `self._eval_mode` is `None` and *both* halves of `eval()` measure the unrandomised regime: `rep = _eval_regime(None, [0…9], 2)` logged as `episode_reward` **5.61**, and `den = _eval_regime('train', [0], 2)` logged as `train_regime_reward` **456.06**. Scene 0 supplies **2 of `rep`'s 20 episodes**, so if scene 0 were worth 456 then `rep` could not fall below `2 × 456 / 20 = 45.6`. It is 5.61 — eight times too small. **The two modes are not the escape.** `robo_make(mode=None)` and `robo_make(mode='train')` were compared pixel-for-pixel at the same seed and scene: `IDENTICAL PIXELS: True`, max absolute difference **0**. So the register's older claim that these are the same env is confirmed, and it is exactly what makes the contradiction unavoidable. **What differs between the two calls is order, not configuration.** `rep` runs first and builds ten environments; `den` builds the eleventh and is the one that opens the door. **And the success is physical, not a rendering or bookkeeping artefact** — `success` is read from `env.last_info['success']`, which robosuite computes from `hinge_qpos > 0.3` in the simulator, and `den` reports **1.0** against `rep`'s **0.0**. The door really opened in the eleventh environment and really did not in the first ten. **This reframes the whole investigation.** The question is no longer "why does my harness disagree with the trainer" but "why does a freshly constructed RL-ViGen environment behave differently from one constructed after ten others have been built and stepped" — which is the same axis my offline harness sits on, as the *first* environment in a clean process, and would explain why every offline construction I have tried lands at 0.7–21 while the trainer's late env lands at 456. **The resume job tests exactly this, and by accident it is the controlled version.** `RLVIGEN_EVAL_SCENES=0` makes `per = 20`, so `rep` and `den` become twenty episodes each at **the same scene, the same regime, the same weights, back to back in one process**, differing only in that `den` runs second. **CONSTRUCTION ORDER IS NOT THE ANSWER — tested locally within the hour and falsified, before the job returned.** `eval()`'s structure was replicated offline exactly: `set_seed_everywhere(2)` once as `train.py:47` does and never again, then ten envs at `mode=None` over scenes 0–9 with two episodes each, then an eleventh at `mode='train'` scene 0. Result: **`rep = 0.68`, `den = 0.69`**, with per-scene means of 0.5–0.9 across all ten. Being the eleventh environment in a process is worth **nothing** — 0.01 of reward — where the trainer's eleventh was worth 456. **So the reframing in the sentence above is wrong and is retracted.** What the replication does establish is stronger and more uncomfortable: *neither* of the trainer's two numbers reproduces offline. `rep` is logged at 5.61 and measures 0.68 here; `den` is logged at 456.06 and measures 0.69. The offline process cannot reach either half of a single `eval()` call, having matched its regime, scene set, episode count, ordering, seeding discipline and action mode. **THE "INTERNAL CONTRADICTION" ABOVE IS MY ERROR AND IS WITHDRAWN — the premise was false.** I concluded `self._eval_mode` was `None` because `cfg-drqv2-bracket-v42.yaml` sets no `RLVIGEN_EVAL_MODE`, and I checked the job YAML without checking the launcher underneath it. **`runnable/_launch/rlvigen.sh:36` reads `export RLVIGEN_EVAL_MODE="${RLVIGEN_EVAL_MODE:-eval-easy}"`** — every rlvigen cell defaults to `eval-easy` unless told otherwise, which the resume job's own log confirms in the plainest possible way (`Now the mode is eval-easy`, in a job whose config also never mentions it). So `rep` is **eval-easy over ten scenes** and `den` is **train over scene 0**: two different regimes, no arithmetic conflict, and the 5.61-against-456.06 pair is exactly the near-total generalisation gap it was always reported to be. The pixel-identity check of `mode=None` versus `mode='train'` stands as a true result, but it was answering a question that did not need asking. *(Found because the owner suggested looking at naming and setup mismatches between train and eval; the lesson is narrower and worth keeping — **an environment variable's default can live in a launcher two layers below the config you are reading**, and "the config does not set it" is not "it is unset".)* **What survives, and it is the whole finding:** the trainer's `eval()` is self-consistent, and the defect is exactly what it was — a checkpoint that scores 456.06 on train/scene 0 inside the process that saved it and 0.71 outside it. **First evidence that the file is not the problem:** the resume job (`bt1bhbb2pllbs2f6u10q`) loaded the 60k snapshot on real CUDA and its own `eval()` reported **41.66 on eval-easy scene 0 over twenty episodes** at frame 60000, before a single gradient step — against the 0.7–0.8 my offline harness returns for these files in any regime. The checkpoint carries a working policy; something in the offline path does not. That job's `train_regime_reward` — the number that closes this — was computed but lost: retention refused the cell for an empty `train.csv`, because RL-ViGen writes a train row only inside `if metrics is not None` and `num_seed_frames=61499` allowed exactly one update, at 61499, after which the loop exits before the next episode boundary. `cfg-drqv2-resume-v45.yaml` moves that to 60500 and returns eval.csv. **RESOLVED, AND THE CONCLUSION INVERTS: the checkpoints were never the problem — offline evaluation ON THIS LAPTOP is.** The test that settles it is a controlled one. `runnable/_launch/rlvigen.sh` defaults the eval regime to `eval-easy`, so the resume job's console number is *eval-easy, scene 0, twenty episodes, at frame 60000, before any gradient step*: **41.66**. That is a quantity I can measure locally on the same file. Measured three ways: my `eval_grid.py` gives **2.94**; **RL-ViGen's own `_eval_regime`, executed unmodified** — the Workspace allocated without `__init__` and given only the attributes the method touches, so the code running is upstream's — gives **3.41**. Same snapshot, same regime, same scene, same episode count, same eval loop, and a **12-14× gap that follows the machine, not the code**. **So my evaluator is exonerated**, and the earlier exclusion that "two independent offline paths agree to two decimals" is now properly closed: both paths were mine, which showed only that they agreed with each other; upstream's own loop was the missing control and it fails identically here. **The remaining variable is the platform, and it is two variables that this evidence cannot yet separate**: device (container CUDA versus local CPU/MPS) and **render backend — `MUJOCO_GL=egl` in the container against `glfw` on macOS**. The renderer is the stronger suspect on the shape of the evidence: these are *vision* policies, a different rasteriser makes every observation subtly out-of-distribution, and that predicts exactly the pattern the whole investigation kept producing — **the damage scales with how much policy there is to damage**. An untrained 6k agent reproduces because it has nothing to lose; `idaac` at 1.5 and `dmc_gb` at 7 reproduce for the same reason; and the gap grows 6.5× → 60× → 670× as the policy improves from 60k to 100k. It also explains why swapping CPU for MPS moved the number without recovering it: both render through GLFW. **What this costs and what it does not.** It does *not* invalidate the training runs — RL-ViGen genuinely reaches 456.06 with `train_regime_success 1.0` at 100k, and that number was produced and re-measured inside the container. It *does* invalidate **every offline grid measurement this project has made locally on a container-trained checkpoint**, which includes the R7/C43 generalisation numbers. Those must be re-measured on the container, which needs an offline-evaluation entry point the runner does not yet have. **The banner is therefore withdrawn and replaced**: runs of this project may be reported as showing learning, on the evidence of their own in-container evaluation; what may *not* be reported is any locally-computed offline evaluation of a container-trained checkpoint, until a container run separates renderer from device |

| 2026-09-03 | **`ctrl` reached its own code for the first time, and the last thing in its way was the runner, not the baseline.** With `captum` and `kornia` excluded, torch is genuinely absent — `ModuleNotFoundError: No module named 'torch'`, which is the exclusion *working*. What then failed is `run_probe.sh`'s own `environment.json` probe, which did `import torch` unconditionally to record versions and to assert a GPU was visible. **So the one family that had successfully shed torch was failed by the runner for not having it.** Rewritten to ask whichever framework the job actually has — torch, then jax — and to fall back to `nvidia-smi` before declaring no accelerator, which keeps the gate's purpose (a GPU job silently landing on CPU is the failure worth catching) while making it framework-agnostic | job `bt1q2rjpgrms72lltsf0`; `datasphere/native/run_probe.sh` environment block | decided | **Third distinct blocker for `ctrl` in one day, each behind the last**: the runtime clone, then torch arriving transitively through `captum`/`kornia`, then the runner's own torch import. None was in `ctrl`. Resubmitted as `bt1kvn6fjscgati859q5` |

| 2026-09-03 | **`alda` now passes its import gate and still returns no checkpoint, and the remote timing says it never trained.** Two runs (`bt15evs9066in8etvlt4`, `bt1m66qf9d9ch66i5ki5`) both: passed `NATIVE_IMPORT_GATE_PASSED alda`, received the right budget (`n_train_steps: 2560` in its own options dump), peaked at 11.5 GB resident, **finished in 29.4 seconds** and exited 0, leaving a 116-line log that is almost entirely the options dump. 29 seconds cannot contain the step-0 evaluation alone, which is three regimes × ten episodes × 500 steps. `scripts/train.py:136-137` calls `trainer.train()` then `finalize_trainer()`, and neither `NATIVE_FINAL_EVALUATION_COMPLETED` nor `All done` appears — so it left `main()` early, with status 0, before evaluating anything | jobs `bt15evs9066in8etvlt4`, `bt1m66qf9d9ch66i5ki5`; `runnable/alda/scripts/train.py:94-137` | closed | **[CLOSED 2026-09-04: the instrumented run worked and nobody came back to say so.** `bt13km8g093do0fdtc58` reached `NATIVE_CELL_COMPLETED alda-s1` with a 96 MB archive and a finite 27.8M-value `snapshot.pt`, and the `NATIVE_ALDA_STAGE` markers this row introduced tell the whole story in order: `build:begin → build:done → train:begin → train:done → finalize:done`, then `NATIVE_FINAL_EVALUATION_COMPLETED frame=10000`, in **16:41.92** of wall clock rather than 29 seconds. So alda trains on CUDA, the 12/12 claim holds for it, and the instrumentation added here is exactly what made the answer legible.]** A terminal save was added to `alda_trainer.py::train()` before the second run and **is confirmed present in the shipped payload** — so the missing checkpoint is not a missing hook; nothing reached it. **The local run settles the code question: it COMPLETES.** 300 steps, seed 1: three step-0 evaluations, the training loop, `finalize_trainer`, `NATIVE_FINAL_EVALUATION_COMPLETED frame=300`, `All done`, and a written `checkpoints/sac_None_step_300.pt` — which the declared glob `sac_*_step_{endpoint}.pt` matches. So the path is sound and the remote 29-second exit is environmental, not a missing hook. **The next remote run is instrumented rather than repeated**: `scripts/train.py` now prints `NATIVE_ALDA_STAGE build:begin|build:done|train:begin|train:done|finalize:done` to stdout with `flush=True`, so they survive whatever happens to the logger's handlers, and the job says *where* it stops instead of leaving a 116-line options dump. Resubmitted as `bt1j5k32stle6gakhbk6`. **Deliberately parked below the checkpoint-reproduction defect** — `alda` is one baseline of twelve, and that one is every baseline |

| 2026-09-03 | **The completeness guarantee is restorable in the isolated candidate for half its clones, and the other half would lie.** The candidate's `runnable/*` clones had no `.git`, so `scripts/deviations.py` — which diffs each clone against its `PRISTINE:` first commit, on the rule that *everything after that commit is ours* — could not run there at all. Restored by copying the six `.git` directories from the working tree (296 MB, read-only from the source), and `deviations.py` now regenerates. **But the answer is only true where the working tree is whole**, and it is not: counted as files `git status` reports deleted, the candidate's clones are missing **ppg 462, alda 113, dmc_gb 112, ibac_sni 1, idaac 0, ctrl 0** — against zero for every clone in the working tree. `ppg` consequently reports 456 files and 684,101 changed lines in the candidate against **8 files and 107 insertions** in the working tree, and the large number is deletions, not authorship | `scripts/deviations.py` run in both trees; `git status --porcelain` per clone | decided | **Half-discharged, and the half that works is named:** `idaac` and `ctrl` are complete in the candidate and their counts are trustworthy, `ibac_sni` is one file short, and `ppg`/`alda`/`dmc_gb` are slimmed enough that any deviation figure from the candidate would be fiction. **The authoritative deviation count stays the working tree.** Same shape as the RL-ViGen archive: a slimmed copy that runs the target fine and cannot answer questions about completeness — worth knowing before a number from it reaches a report |

| 2026-09-03 | **`alda` was being OOM-killed all along, and the runner reported it as success — because GNU `time -v` prints an exit status after the signal line.** Three jobs failed identically with a 116-line log, ~11.5 GB peak resident, and no traceback. The third, with stage markers added, printed **none of them** — and its resource block reads `Command terminated by signal 9` at line 2978 and, twenty-three lines later, **`Exit status: 0`**. The runner was reading the second. So an OOM kill during buffer allocation looked like a clean run whose checkpoint had merely gone missing, and three attempts were spent on the wrong question. **`alda` needs 32 GB and was submitted to `gt4.1` (16 GB)** — the production table has said `alda` occupies a `gt4i.1` alone since the memory law was written, and the probe config simply did not follow it | jobs `bt15evs9066in8etvlt4`, `bt1m66qf9d9ch66i5ki5`, `bt1j5k32stle6gakhbk6`; `time -v` block at stdout.log:2978 and :3001 | decided | **Two fixes, and the second is worth more than the first.** `alda`'s config moved to `gt4i.1` (resubmitted `bt1qamel7lbg3rr9a9d9`). And `run_probe.sh` now greps the cell log for `Command terminated by signal` **before** trusting the exit status, failing the cell with `NATIVE_CELL_SIGNALLED` and the signal line. **Any cell on any baseline could have been killed and reported clean** — this was not an `alda` problem, it was a reporting problem that `alda` happened to expose, and at production length an OOM at hour six would have looked exactly the same |

| 2026-09-03 | **The offline grid now covers a second family, and `dmc_gb` reproduces its own logged number — which the RL-ViGen five do not.** `scripts/eval_grid.py` gained a `--family dmc_gb` path: `dmc_gb`'s own `make_env` and its agent's own `select_action` (the deterministic `mu`, which is what `train.py::evaluate` uses), emitting the same schema-2 records so the two families differ in env and policy call and **not** in what a row means. Validated against `soda-s1`'s retained checkpoint at 10,000 steps: offline **7.059** against the run's own logged `episode_reward` of **7.182** — agreement within episode noise. **That is the first cross-family control on the reproduction defect**, and it points away from "the offline harness is wrong" and toward something specific to RL-ViGen's save path. Note the caveat that keeps it from being conclusive: `soda` ran 10k and the RL-ViGen checkpoint that reproduces ran 6k, so a length effect is not yet excluded — which is exactly what the bracket jobs are for | `scripts/eval_grid.py --family dmc_gb`; `soda-s1/{snapshot.pt,eval.log}`; `/tmp/grid-soda3.jsonl` | decided | Two of six families now have a working offline path (7 of 12 baselines). **Local runs of the dmc_gb path need `RLGEN_MPS_AS_CUDA=1` and `_shim` on `PYTHONPATH`** — its agent asserts CUDA at 22 sites and the project's answer to that is the launcher shim, not 22 authored deviations; on the T4 it runs unmodified |

| 2026-09-03 | **Restoring the clones' git metadata broke every payload build, and the builder's message made it look like smuggling.** `.git` is in `FORBIDDEN_PARTS` — the list that stops a clone's `results/`, `models/` or `wandb/` reaching a job — so the moment the candidate's clones regained their history, `build-payload` failed with `forbidden payload member: runnable/alda/.git/COMMIT_EDITMSG`. The rule was right and the category was wrong: history is not a smuggled artifact, it is the thing `deviations.py` diffs against, and no job needs a byte of it. `.git` is now **skipped** during member collection rather than left to trip the forbid check | `datasphere/native/contract.py` member collection; build failure after the 21a restoration | decided | **Two defects of my own in the same hour, both from editing shell under `set -euo pipefail`.** A `` `time -v` `` written inside a double-quoted string became command substitution, and the signal check referenced `$cell_out`/`$spec` in `run_measured`, whose parameters are `output_dir` and no spec at all — `unbound variable` at line 38, which `tests/test_datasphere_native_contract.py` caught immediately. Both fixed; 79 contract tests green. **The runner is a job input uploaded fresh per submit, so a broken one reaches the next job and not the running ones** — `bt1g0diufvr8jb26ctvj` went out in that window and carries it, which is survivable because ctrl fails or passes at its import gate before any cell runs |

| 2026-09-03 | **`alda` ran on CUDA for the first time, and the OOM diagnosis was the whole of it.** Moved to `gt4i.1` and job `bt1qamel7lbg3rr9a9d9` completed: import gate passed, `NATIVE_FINAL_EVALUATION_COMPLETED frame=2560`, finiteness clean over 27,782,494 floats across 366 arrays with nothing stubbed, cell completed, checkpoint retained. **Eleven of twelve baselines now have a successful CUDA run**; only `ctrl` is outstanding. It also produced **three records** — `train`, `eval-easy`, `eval-hard` — which is the first time any baseline's own training loop has delivered a three-regime row through the record envelope, and it confirms `audit_eval_state`'s reading of `alda_trainer.py:146-148` from the other end | job `bt1qamel7lbg3rr9a9d9`; `ex/records.jsonl`; `datasphere/native/normalize_curves.py::read_alda` | decided | **One defect the success exposed: all three records had `frame: None`.** `ALDA_STEP` expects a logger prefix ALDA does not emit at the end of a run, so the cell produced three correct measurements on an unknown x-axis — the thing `eval_grid.py` refuses to emit and the envelope had no guard against. The frame is now read from `retained.json`'s `checkpoint_source`, because `retain` copies the file to `snapshot.pt` and the original name `sac_<env>_step_<frame>.pt` survives nowhere else — a first attempt globbing the cell directory still returned `None` for exactly that reason. Now reads 2560 |

| 2026-09-03 | **The bracket reproduced the checkpoint defect in a run designed for it, and moved the break below 50,000 frames.** `bt15c9cn0v99rphglvgc` — 60k `drqv2`, `RLVIGEN_PRESERVE_SNAPSHOTS=25000`, 20-episode evals — completed and kept two stamps. (25k was never stamped: upstream saves on `global_step % int(5e4)`, so a preserve *cadence* can only filter saves that happen, and below 50k none do. Worth knowing before choosing a cadence.) Its own logged train-regime numbers rise 0.78 → 25.37 → 94.98 → 135.71, and its **own training episodes at 57–59.5k read 99.45, 90.51, 114.80, 126.46, 68.46, 73.48** — consistently ~95. Evaluated offline on the same regime and scene: **50k stamp → 1.59** against 94.98 logged, **60k stamp → 20.95** against 135.71. So the gap is present at 50,000 frames, where 6,000 reproduces cleanly, and **this is a fresh run I designed rather than a re-reading of the endurance data** | job `bt15c9cn0v99rphglvgc`; `/tmp/br-50000.jsonl`, `/tmp/br-60000.jsonl`; `ex/cells/drqv2-s1/{eval.csv,train.csv}` | open | **The alternative explanation is under test rather than dismissed.** Door's return distribution is heavy-tailed — the 60k offline draws were `[8.4, 71.0, 7.6, 9.7, 8.0]` — and every logged training-time evaluation is a **2-episode mean** (`per = max(1, 20 // 10)`), so a logged 135 could in principle be two lucky draws. What argues against it is the *training* curve: six consecutive episodes at ~95 is not two lucky draws. **SETTLED, and the noise explanation is dead.** Twenty offline episodes on the 100k checkpoint, train regime, scene 0: **mean 4.34, median 2.49, max 13.3, min 1.2**, sorted `[1.2, 1.5, 1.6, 1.8, 2.0, 2.1, 2.1, 2.2, 2.3, 2.4, 2.6, 2.6, 3.7, 4.1, 5.2, 7.3, 7.9, 8.0, 12.6, 13.3]`. **Not one draw in twenty exceeds 13.3.** A distribution with that support cannot produce a two-episode mean of 476, and it certainly cannot produce three *consecutive* training episodes at 464.6 / 479.2 / 468.7. The heavy tail is real — the 60k stamp drew a 71 — but it is nowhere near large enough to explain the gap. **The defect is confirmed at proper sample size, on two independent runs, at 50k, 60k and 100k, with 6k reproducing** |

| 2026-09-03 | **`RLVIGEN_PRESERVE_SNAPSHOTS` cannot stamp a checkpoint at an arbitrary frame, and a plan recorded in this register assumed it could.** The earlier bracket design said "a ~60k `drqv2` cell with `RLVIGEN_PRESERVE_SNAPSHOTS=25000`, giving stamped checkpoints at **25k**, 50k and the endpoint". The job ran and its `checkpoints/` holds `snapshot_50000.pt` and `snapshot_60000.pt` — **no 25k stamp, and there could never have been one.** P18 is a *filter* on the saves upstream already performs, and `train()` calls `save_snapshot()` only inside `if self.cfg.save_snapshot and (self.global_step % int(5e4) == 0)`, a hardcoded 50,000-frame gate P18 does not touch; the endpoint block adds one more save at `num_train_frames`. So the reachable stamps are exactly the multiples of 50k, plus the endpoint, and the cadence variable can only remove members of that set — never add one | `RL-ViGen-upstream/train.py:352-376`; `bt15c9cn0v99rphglvgc/…/drqv2-s1/checkpoints/` (two files, not three) | closed | **The consequence is for the budget curve, and it is mild but must be stated where the curve is planned.** A 500k production cell yields points at 50k…500k, which is a ten-point curve and ample; what is *not* available at any budget is a point below 50,000 frames other than the endpoint of a deliberately short run. Since the checkpoint defect brackets between 6k (reproduces) and 50k (does not), narrowing it further needs **short cells whose endpoint is the frame of interest**, not a finer preserve cadence — the endpoint save is the only mechanism that can place a stamp off the 50k grid. Recorded because the wrong version of this was written down as a plan and would have been read as an instruction |

| 2026-09-03 | **`ctrl`, the twelfth baseline, was blocked three times in a row — and all three blocks were mine, not the clone's.** (1) The W&B shim was fail-closed by construction and `train_ppo.py:111` calls `wandb.init` unconditionally with no flag to disable, so attempt #10 could not start. (2) With that fixed, #11 trained a full PPO epoch (`PPO took 8.844827 seconds`) and died in the first CTRL cluster update: `algo.py:517-520` computes `size_batch = num_envs * (n_steps - cluster_len + 1) // 2` and reshapes it into `n_minibatch_ctrl` groups, which at our `num_envs=4` is `reshape((494,), (-1, 61))` — invalid. PPO's own batch is `num_envs * n_steps` and divides at every `num_envs`, so nothing catches this until the cluster step. (3) With `num_envs=16` — the smallest value satisfying both constraints at upstream's own `n_steps`/`cluster_len`/`n_minibatch_ctrl`, and closer to upstream's 64 than 4 was — #12 **trained and reached its endpoint, exit status 0**, then was failed by the finiteness gate: `could not load snapshot.pt: ModuleNotFoundError: No module named 'torch'`. The probe picked its loader with `path.endswith(".msgpack")`, but the runner copies every family's checkpoint to `snapshot.pt`, so `ctrl`'s flax msgpack took the torch branch — and `ctrl` excludes torch on purpose, being JAX | `runnable/_shim/wandb.py`; `runnable/ctrl/algo.py:517-520`; `datasphere/native/family.py` FINITENESS_PROBE; jobs `bt1efc5ma3m0lohlnq2d`, `bt1qgrrl5dcpn07jcq40`, `bt1f1kg6kiemsj7j8f6q` | open | **The pattern is worth more than the three fixes.** Every one of these is a piece of *our* scaffolding rejecting a baseline that was working: a safety shim with no off switch, a memory-driven constant that silently violated an upstream divisibility assumption, and a gate that identified a file format by its name. The third is a repeat — this register already records an alda save "verified" by filename rather than bytes — so the probe now sniffs magic bytes and tries torch, msgpack and numpy in that order, reporting which succeeded, with both directions under test. **`num_envs=16` is a fidelity improvement, not a compromise**, and is recorded in `families.json`'s `constants_note` with the arithmetic. **Attempt #13 (`bt1171kuvf77sbnd1v56`) SUCCEEDED — `NATIVE_CELL_COMPLETED`, exit status 0, and `ctrl` is the twelfth and last baseline to make CUDA contact.** The finiteness gate that failed #12 now reports `"loader": "msgpack"` on the same `snapshot.pt`, having sniffed the magic bytes rather than read the extension, and passes 9,955,955 floating-point values with **zero** non-finite. Returned artifacts: `snapshot.pt`, `train_console.log`, `retained.json`, `resources.json`, and `wandb_offline.jsonl` — the last of which exists only because the shim records instead of refusing. **Also measured: RSS 13.4 GB at `num_envs=16`**, which is why ctrl stays on `gt4i.1` — the 16 GB tier would have been tight, and that is the failure alda spent a whole job discovering |

| 2026-09-03 | **[C95](CONSTRUCTION.md#c95) resolved in mechanism: the DEVICE is exonerated and the RENDERER is the cause — and the same job validates `eval_grid.py` against the training loop.** The 60k snapshot, same grid, run twice inside one container: **CUDA gives train 131.54 / eval-easy 45.16; CPU gives train 131.57 / eval-easy 39.90.** The train-regime numbers agree to three significant figures across devices, so CUDA-versus-CPU explains nothing. The same quantity on this laptop is **13.85**. What differs between a container CPU at 131.57 and a laptop CPU at 13.85 is not the device and not the code — it is `MUJOCO_GL=egl` against macOS `glfw`. **And 131.57 reproduces the run's own logged `train_regime_reward` of 135.71 at frame 60000**, from two episodes against these twenty. So `scripts/eval_grid.py` is correct and always was; it was being run on a machine that renders different pixels than the one that trained the policy | `bt1a4gsqf1h3sd5u72tl` `offline_eval_{cuda,cpu}.jsonl`; `bt15c9cn0v99rphglvgc/…/eval.csv` | closed | **This closes the defect that has driven the whole investigation, and it closes it as a harness fact rather than a result.** Every candidate excluded along the way was excluded correctly; the one never tested was the one nobody thinks of, because the renderer is not part of the model, the data or the code. **The transferable rule is in C95 and is worth more than the fix**: when your instrument disagrees with a reference, the decisive control is *the reference's own instrument on your hardware* — running upstream's `_eval_regime` locally is what exonerated the code and indicted the machine, and two of my own evaluators agreeing had merely measured my own consistency. **Retroactive effect**: the register's "70×" and "670×" gap rows describe a rendering mismatch, not a checkpoint defect, and the withdrawn "no run shows learning" banner was withdrawn correctly |
| 2026-09-03 | **The first trustworthy generalisation measurement this project has produced — and the policy is scene-specific, not merely appearance-sensitive.** `drqv2-s2` at 100k, evaluated on the container where it was trained, ten certified scenes × five episodes per scene per regime: **train regime mean 89.47 at success 0.24; eval-easy mean 3.07 at success 0.00.** The per-scene breakdown is the finding, and an aggregate would have hidden it entirely — train regime: **scene 0 = 386.1, scene 4 = 280.4, scene 2 = 210.3**, and scenes 1, 3, 5, 6, 7, 8, 9 = **0.7, 0.7, 3.8, 0.7, 0.7, 0.7, 9.9**. The policy solves **three of ten scenes** and does nothing on the other seven. Scene 0 at 386.1 corroborates the run's own logged 456.06 at scene 0, five episodes against two | `bt1dluvrqrbab7ulcpsc` `offline_eval_cuda.jsonl` (schema-2, per-scene rows plus aggregate) | open | **Two ratios, and they answer different questions — [C43](CONSTRUCTION.md#c43) needs both stated separately.** *Scene retention within the training regime*: the nine held-out scenes average **≈56.5** against scene 0's 386.1, so **≈15%**. *Appearance retention*: eval-easy's 3.07 against the train regime's 89.47 is **≈3.4%**, and against scene 0 alone it is **≈1%**. The earlier local figure of 27.8% ([C47](CONSTRUCTION.md#c47)) was computed on the laptop and is void under C95; this supersedes it. **The success column is the honest one**: 0.24 in the train regime means the door opened in 12 of 50 episodes and never once under appearance randomisation, and success is a physics threshold that no reward scaling can flatter. **What this does NOT license**: one seed, one baseline, one budget. It is a measurement, not a result about DrQ-v2 — and [C45](CONSTRUCTION.md#c45) is now *driver-limited for three families*, not plumbing-limited for seven |

| 2026-09-03 | **Three of twelve baselines report a SAMPLED return where nine report a mode return, and the audit that tracks this had one of the three recorded as unresolved.** `scripts/audit_eval_state.py` read *"idaac samples where the other nine take a mode"*, with `ibac_sni`'s cell left as the placeholder `"see scripts/evaluate.py"`. Read it: `--argmax` is a `store_true` defaulting **False**, so the flag that would take the mode is off unless asked for — `ibac_sni` samples. `ppg` samples too, and for a stronger reason: its repository contains no deterministic-action path at all, which `runnable/_launch/ppg_eval.py` has stated since it was written. So the count is **idaac + ibac_sni + ppg = 3 sampling, 9 taking a mode** | `scripts/audit_eval_state.py`; `runnable/ibac_sni/torch_rl/scripts/evaluate.py` `--argmax`; `runnable/_launch/ppg_eval.py` | closed | **It is a comparability axis, not a detail.** A sampled return and a mode return are different quantities — the sampled one is usually lower, by an amount that depends on the policy's entropy at the checkpoint and therefore on how far training got. Averaging the two into one column, or comparing a sampling baseline against a mode baseline without saying so, attributes to the *algorithm* a difference that belongs to the *estimator*. **The fix is not to make them uniform**: each baseline's own evaluator is the null this project is measuring against, so forcing a mode on `ppg` would be a deviation from its authors, and `ppg` has nothing to force. The fix is that the axis is declared wherever the numbers are, which is now `COMPARABILITY_CONTRACT.md` §5c. **Recorded also as a lesson about the instrument**: the audit was right about what it had checked and silent about what it had not, and a placeholder string reads as an answer to anyone skimming the column |

| 2026-09-03 | **The pre-production pass found three defects in its first four cells, and two of them were in the machinery that REPORTS results rather than in any baseline.** (1) `run_probe.sh`'s places365 branch opened with a bare `[[ -n "$asset_archive" ]]`, which under `set -e` ends a job with **no message**: two jobs stopped mid-import with byte-identical 2838-line logs and status ERROR. My configs had omitted the asset input that `svea`/`sgqn`/`soda` need, but a silent `exit 1` is indistinguishable from a crash and cost far more to diagnose than to fix. (2) `ibac_sni` trained, evaluated and printed `R:μσmM 1.74 … SR 0.0000`, and **none of it reached the record set** — `read_ibac_sni` parsed only `log.csv` while its evaluator prints one line to stdout, so the records held 79 training rows and zero eval rows. (3) `ctrl`'s eval record carries `episodes: None`, which is **correct and is a comparability axis nobody had named**: it never evaluates a saved policy, but reports `Eprew200`/`Eprew0`, a running mean over a trailing window, from inside the training loop | `datasphere/native/run_probe.sh`; `datasphere/native/normalize_curves.py` `IBAC_EVAL_LINE`; `scripts/preprod_table.py`; jobs `bt1bciubjre32859p455`, `bt1hgetjvsf2s56opbv6`, `bt1eo5sv6vf0qmit15ds`, `bt1fer7809i7bpob7g2f` | closed | **The transferable one is (2), and it is why the table is generated rather than written.** A break in the *recording* path and a baseline that genuinely scored nothing produce **the same shape in a results column** — a blank or a zero — and the second is a finding while the first is a bug. Nothing in the pipeline distinguished them until a generator printed "no eval number for `ibac_sni`" beside a cell whose own log said `SR 0.0000`. Twelve rows across seven log formats is precisely the hand-maintained artefact this register already has several entries about rotting. **Also recorded: R4's "equal training length" is an intent, not a fact.** A requested 10,000 frames executed as **9216 / 10000 / 10112 / 10240** for `idaac` / `ctrl` / `ibac_sni` / `ppg` — four quanta, none equal — because each family floors the budget to its own rollout granularity. The table's `frames` column carries what actually ran. **FOURTH FINDING, and it is the one I nearly wrote off.** Three of the first seven rows had a blank episode count and it looked like one formatting gap. The causes are three and the differences matter: `dmc_gb` takes `--eval_episodes` from `families.json`, so the runner's value **is** its N; `alda` takes no eval flag at all and its N is `alda_trainer.py:74`'s own default of **10, which coincides with ours by accident**; and `ctrl` has **no N at all**, because it reports a trailing window — stamping one would assert a fixed-policy evaluation that never happened. `normalize_curves.declared_eval_episodes()` now fills the first two with their provenance in `native` and leaves the third blank on purpose. **Why this is worth a finding rather than a fix**: `audit_comparability_seam.py` states that across the twelve "only **N** differs, and N is precision, not quantity". That is true *once N is known*, and for two of seven families it was not known at all — so a blank in this record set is now meaningful, and a filled value carries where it came from. A number that agrees with ours by coincidence must never be read as one we controlled. **FIFTH FINDING — the table committed the exact error it was built to prevent, and the printed column is what caught it.** `preprod_table.py` selected each cell's eval record as `evals[-1]`. For the five RL-ViGen baselines that is the **train-regime** row, because patch P14 emits `episode_reward` (the eval regime) and `train_regime_reward` (the retention denominator) as two records and writes the denominator second. So the first complete table showed `drqv2` at **21.06** and `svea` at **28.67** in a column of seven eval-easy numbers — four train-regime values pooled with seven held-out ones. Corrected to select by target regime, marking any row that has no record for it rather than substituting silently; the same figures read **21.88** and **42.92** on the eval regime, and `curl` moved 4.23 → 11.71 while `drq` moved 1.56 → 1.10. **The lesson is about instrument design, not about this bug**: it was visible only because the generator prints the regime beside the number. A table that had shown the value alone would have been clean-looking, wrong, and unfalsifiable by inspection — which is the argument for printing every axis a comparison depends on, even when it looks redundant |

| 2026-09-03 | **`sgqn` is the one baseline of twelve that does not complete a pre-production cell, and it is not a resource failure.** In the five-cell RL-ViGen job (`bt1hs1lpt7bskh8sdri0`), `drqv2`, `svea`, `drq` and `curl` all reached `NATIVE_CELL_COMPLETED`; `sgqn` reached **F: 5000** of 10,000 and then died with `RuntimeError: DataLoader worker (pid 8025) is killed by signal: Aborted`. **Maximum resident set size was 3,766,064 KB — 3.77 GB on a 32 GB tier**, so the obvious explanation is excluded: this is not OOM. `num_seed_frames` is 4,000, so the abort lands roughly 1,000 frames into the first updates, which is the first point at which `sgqn`'s own update path — saliency attribution, the thing that distinguishes it — is exercised under load | `bt1hs1lpt7bskh8sdri0` stdout.log, cell `sgqn-s1`; four sibling cells completed in the same container, same payload, same tier | closed | **Recorded rather than retried, deliberately.** A re-run costs ~234 RUB and the failure has the shape of a deterministic one: a fixed budget, a fixed seed, a crash at a reproducible phase boundary, and no resource pressure to make it flaky. Spending on a repeat before forming a hypothesis is how the resume experiment consumed three jobs. **RESOLVED the same day by [P19](INTEGRATION-DELTA.md)**: `_load_places` hardcodes `num_workers=8` with `pin_memory=True` and the container has **4 logical CPUs**; `sgqn` alone loses the race because `update_aux` calls `random_overlay` an extra time per update. With the dial at 0 it completes at **2.348**, so the pass validates **12 of 12**. The diagnosis cost three jobs and each changed exactly one thing — a plain retry would have reproduced the mute abort and taught nothing. **The cheap next step is not a re-run but a read**: `SIGABRT` from a DataLoader worker is a C-level abort, so the next evidence is whether `sgqn`'s attribution path allocates in a worker process at all, and `faulthandler` or `num_workers=0` would separate a genuine crash from a worker-transport failure for the price of one short cell. **[Superseded — that next step was never needed: P19 took the `num_workers=0` half of it as the fix, and the status column above was left reading `open` until 2026-09-04 even though this reflection had said RESOLVED since the day it was written.** A row whose prose and whose status disagree is read by people from the prose and by `scripts/open_decisions.py` from the status, so it inflated the open-findings count by one for a day. Corrected by reading the row, not by a new measurement.] |

| 2026-09-03 | **A blind-spot pass over the evaluation path found two defects that would have produced plausible numbers, and one that merely crashed.** Run deliberately, before committing to the common-grid proposal in [`EVAL-PROTOCOL.md`](EVAL-PROTOCOL.md), because that proposal makes our offline harness the *only* source of comparable metrics. (1) **The evaluation seed was the training seed.** `OFFLINE_EVAL` passed one variable to both `--seed` (which labels the run) and `--episode-seed` (which seeds the evaluation). Via [C69](CONSTRUCTION.md#c69) the latter seeds the **global numpy RNG**, which is what robosuite's `UniformRandomSampler` draws the door's position from — so a three-seed production set would have evaluated each seed's checkpoint on a **different set of door placements**. (2) **Three of four families never seeded that RNG at all.** `run_scene` has done so since C69; `run_scene_dmc_gb`, `run_scene_idaac` and `run_scene_ppg` never did, so for them the evaluation seed reached only the texture wrapper and never the door. (3) `idaac`'s own `train.py:5` installs `np.bool = bool` for legacy OpenAI baselines; the harness imports its modules directly, so the shim never ran and the first guard job died on `AttributeError: module 'numpy' has no attribute 'bool'` | `datasphere/native/run_probe.sh` (`OFFLINE_EVAL_EPISODE_SEED`); `scripts/eval_grid.py` `seed_the_placement_rng`, `_idaac_setup`; jobs `bt1c6vj2iv6ucu9683nk`, `bt1dbmtc04sg5nhc18r6` | closed | **The first two are the dangerous ones and they share a shape: nothing would have crashed.** Both produce numbers that look entirely normal — the across-seed spread would simply have carried a term with no algorithmic meaning, and cross-family comparisons would have been on different door placements while every record claimed the same seed. That is [C95](CONSTRUCTION.md#c95)'s shape a second time: **a measurement invalidated by something outside the model, the data and the code under test**, and invisible in the output. (3) by contrast announced itself. **The lesson for the plan**: the blind-spot pass earned its place — it was scheduled on the argument that this session's three largest findings were all accidental, and it immediately found two more of the same kind. It should run again before production, not once |

| 2026-09-04 | **A shared evaluator was built and then validated by a RATIO, which sidesteps this project's own standard — corrected, and the correction found three undeclared deltas.** [`EVAL-PROTOCOL.md`](EVAL-PROTOCOL.md) proposes that our offline harness produce every reported number, because same-axes metrics are unreachable from training logs for a quarter of the set. I discharged that burden with `scripts/audit_shared_evaluator.py`, which compares our number to each baseline's own and grades the ratio. **That is corroboration, not proof.** Agreement to 3% on one checkpoint shows two instruments produced similar numbers; it does not show they measure the same thing, and it lets an undeclared difference hide behind "close enough" until the checkpoint that separates them. The standing rule is the opposite — two implementations differ until shown otherwise, deltas are deliberate, differences a reviewer would notice are explicit — and `INTEGRATION-DELTA.md` applies it to the twelve algorithms while **nothing applied it to the evaluator**, which under the proposal becomes the instrument behind every number | [`EVALUATOR-DELTA.md`](EVALUATOR-DELTA.md); `scripts/audit_shared_evaluator.py` | open | **Writing the ledger immediately surfaced three material deltas that existed in no document.** (1) `torch.use_deterministic_algorithms(True)` is **ours and not upstream's** — it makes our number reproducible, not upstream's, and [C70](CONSTRUCTION.md#c70) exists precisely because Door's success threshold amplifies 1e-7 differences. (2) The rlvigen family's verification `reset()`/`step()` probe runs **before** the episode loop and consumes RNG draws, so our door-placement sequence is offset from the trainer's by a fixed amount — harmless for a mean, not harmless for an episode-by-episode comparison, and the reason 131.57 and 135.71 should not be expected to match exactly. (3) `idaac` and `ibac_sni` count episodes as **the first N completed across a vec env**, a race; ours is a fixed sequential count per scene — different sampling of episode starts, and one reason the 1.663-against-3.01 comparison was never like-for-like. **The ratio audit is kept and demoted** to what it is: evidence that the declared deltas do not add up to a behavioural difference on the checkpoints tested. `ppg` is the sharpest case — it has **no** evaluator of its own, so "faithful to theirs" is not even the right question; the honest obligation there is to state what we built |

| 2026-09-04 | **`ctrl` reports a SAMPLED return, not a mode return, and the estimator axis is 4/8 rather than 3/9.** Found while establishing `ctrl`'s evaluator family — the work done *before* reporting its numbers rather than after. `scripts/audit_eval_state.py` recorded `ctrl` as *"deterministic (`pi.mode()`) via `algo.select_action"`, which is true of the **helper**: `select_action(..., sample=False)` returns `pi.mode()`. But the two calls that produce the numbers a `ctrl` cell actually reports — `train_ppo.py:208` and `:217`, the ID and OOD test-env steps behind `Eprew200`/`Eprew0` — **both pass `sample=True`** | `runnable/ctrl/train_ppo.py:208,223`; `runnable/ctrl/algo.py:40-61`; `scripts/audit_eval_state.py`; `scripts/preprod_table.py` | closed | **A capability is not a usage, and an axis must record the reporting path.** The misclassification was not a misreading of `algo.py` — that entry was correct about the helper — it was reading the API instead of the call sites. **The dig continued to the other eleven rather than stopping at the one**, because one wrong classification makes the rest suspect: re-verified per reporting path, `rad`/`soda` via `src/train.py::evaluate → select_action → actor(compute_pi=False)`, the RL-ViGen five via `_eval_regime`'s `act(eval_mode=True)`, `alda` via its own `evaluate()`, `idaac` via `test.py` omitting the flag, `ibac_sni` via `--argmax`'s default. **Eleven held; one did not.** Corrected in both instruments and in `COMPARABILITY_CONTRACT.md` §5c, and the pre-production table regenerated — `ctrl`'s row now reads SAMPLE, which matters because it was already the one row marked non-comparable for a *different* reason |
| 2026-09-04 | **The C61 policy-health diagnostic was written "so the four PPO-family baselines report the same quantity" and had reached two of the four.** `scripts/metrics.py::gaussian_policy_health` says exactly that in its own docstring; `idaac` and `ibac_sni` called it, `ppg` and `ctrl` did not, and nothing failed — **an absent diagnostic and one whose value never moves are the same observation**, which is why this survived a session that had already read all four files for other reasons. Completed to both, and the completion immediately produced a comparability finding rather than four matching columns: **`ppg` is the only one of the four that CLAMPS** (`distr_builder.py:31`, `.clamp(-5.0, 2.0)` before `.exp()`), so its parameter and the distribution the env samples from are different objects the moment the clamp binds. The shared keys are therefore computed on the **clamped** values — the policy that acted, the one comparable in kind to `idaac`'s and `ctrl`'s unclamped heads — and the raw parameter gets its OWN keys, `train/log_std_raw_mean` and `train/log_std_clamped_fraction`. The second is the C61 failure made observable: with a bound clamp the entropy term keeps pushing `log_std` up, the clamp keeps cancelling it, and **no logged number moves at all** | `runnable/ppg/phasic_policy_gradient/ppo.py` (`compute_advantage`, beside the reward summary already there); `runnable/ctrl/train_ppo.py` (`_find_log_std` over the flax param tree, folded into the dict already going to `wandb.log`); `scripts/metrics.py:395`; `tests/test_record_conventions.py::test_each_continuous_ppo_head_reports_shared_policy_health` | closed | **Two cross-checks fell out for free and both held**: entropy at `log_std = 0` computes to 9.933, the constant `ppg/train.py:104` already records independently, and `boundary_fraction` at σ=1 is 0.317, the figure C61's own note states. Neither was arranged. The guard test is source-level and coarse on purpose — it asserts the four *call the shared definition*, because the defect it exists to catch is a private near-miss of a shared quantity, not a wrong value. The squashed families are deliberately excluded: a `tanh` puts `boundary_fraction` at zero by construction, so the same call there would report a constant and read as evidence |
| 2026-09-04 | **`ctrl` computes dozens of metrics that reach no file, and the fix needed no edit to `ctrl`.** `train_ppo.py:277-293` builds `metric_dict` — its PPO and CTRL-specific losses — and passes it to `wandb.log`. It never prints it; its stdout carries exactly four numbers (`Eprew200`, `Eprew0`, `SR_ID`, `SR_OOD`), so **no amount of log parsing recovers the rest** and `read_ctrl` accordingly emitted two native keys for a baseline computing dozens. `alda` is the same shape. What closed it was infrastructure already built for another reason: `runnable/_shim/wandb.py` stopped being a fail-closed stub on 2026-09-03 and became an offline sink that writes every `wandb.log` call to `$run_dir/wandb_offline.jsonl`, so **these numbers had been landing on disk in every cell since and simply had no reader**. `read_wandb_sink` is that reader — family-independent, so any clone that starts logging is picked up with no code change | `datasphere/native/normalize_curves.py::read_wandb_sink`; `runnable/_shim/wandb.py`; `tests/test_record_conventions.py::test_wandb_sink_merges_calls_that_share_a_step`, `::test_wandb_sink_promotes_nothing_to_a_shared_column` | closed | **Nothing from the sink is mapped to a shared column**, and the test that pins this is the one that matters. `ctrl`'s `ep_return_200` IS `Eprew200`: a trailing window over ~200 episodes of *successive* policies, which `COMPARABILITY_CONTRACT` §5d already records as a different estimand from every other baseline's saved-policy evaluation. Promoting it to `episode_return_mean` would put a declared difference back underground one field at a time — **while making every table look more complete**, which is why it is the plausible future edit rather than an unlikely one. Keys are kept verbatim, prefix and all, for the same reason: renaming is what makes two quantities look like one |
| 2026-09-04 | **This tree cannot regenerate its own deviation record, and says so rather than producing a wrong one.** Instrumenting `ppg` and `ctrl` shifted line numbers cited in eight places, so `runnable/_patches/*.patch` — the version-controlled record of what we authored in each clone, generated by `scripts/deviations.py --export` from `git diff` inside each clone — is now stale for two families. It was not regenerated wholesale: `deviations.py` reports the clones are **not whole in this tree** (`ppg` −462 tracked files, `alda` −113, `dmc_gb` −112, `ibac_sni` −1), and `--export` from here would record hundreds of deletions that did not happen. The tool refuses to print a total for the same reason, which is the check working. **Corrected the same day, by reading the list instead of generalising from it: `ctrl` and `idaac` are NOT on it.** `runnable/ctrl` has zero tracked-but-absent files, so its patch was regenerated exactly as `--export` would (`git diff` against the clone's first commit, `bc7f1fce`): 583 → 733 lines, carrying `_find_log_std`, `clip_fraction` and `approx_kl_k3`. **`ppg`'s remains stale and is the one still open**, because its clone is the most incomplete of the four | `scripts/deviations.py:160-170`; `runnable/_patches/{ppg,ctrl}.patch` (stale, knowingly); `datasphere/native/payload-v60.tgz` ships `runnable/` directly, so the **clone edits themselves take effect** on the container once a payload is rebuilt — the patch files are a record, not the delivery path | closed | **[CLOSED 2026-09-04 — the tree CAN regenerate its deviation record, and the blocker dissolved on inspection rather than needing the whole clones.** The refusal rested on "these clones are not whole", which is true and was never checked for *what* is absent. Every absent path is a **non-source artifact**: `dmc_gb`'s `src/env/data/color_*.pt` and video assets, `ppg`'s copies of upstream's published `results/*/progress-*.csv`, and `ibac_sni`'s single stray `toy-classification/results/.gitignore` — restored with one `git checkout`, so ibac_sni is now whole. The clones' **source** was complete all along. `deviations.py` now excludes deleted paths when a tree is slimmed and labels those rows **PARTIAL** rather than skipping them, so all six count: **38 files, 1,449 insertions, 861 code lines authored across the six clones**, and `--export` regenerates every patch including today's checkpoint edits. The one thing the filter cannot see is a deletion *we* made, which is acceptable only because clone edits here are additive by policy — stated in the code as a limit, not waved past.]** **The stale citations were split by kind, not fixed wholesale.** Live instruments (`audit_eval_state.py`'s anchor, `preprod_table.py`, `eval_grid.py`, and the two live docs) were repointed; `REGISTER.md`'s rows were **left alone on purpose** — `train_ppo.py:208` was true when that row was written, and rewriting a line number inside a dated observation falsifies the record of what was seen rather than updating it. The anchor mechanism in `audit_eval_state.py` is what caught the shift at all, on the first test run after the edit |
| 2026-09-04 | **`drqv2` solves Door's training scene and retains essentially nothing — the project's first competent policy, and its first defined retention number.** At 100k frames (`bt15e9v1k2ngmb71hnjn`, 20 eval episodes every 25k, no `RLVIGEN_EVAL_MODE` override so the eval column is **eval-easy** and `train_regime_*` is train): train regime **480.56 return, success 1.00**; eval-easy **1.439 return, success 0.00**. At 75k the same pair reads 482.42/1.00 against 2.435/0.05. **Retention is 1.439 / 480.56 = 0.30%**, and the held-out return is *below* the 1.82 random floor ([C55](CONSTRUCTION.md#c55)) while the training return is 264x it | `bt15e9v1k2ngmb71hnjn` `cells/drqv2-s1/eval.csv`; checkpoint finite (`non_finite: 0` over 33,299,022 float values); `datasphere/native/cfg-shape-drqv2-v62.yaml` | open | **This is the first row in this project where the competence gate PASSES and the retention number is therefore meaningful rather than undefined** — §3b #15's worry was that a method which never learns shows gap ≈ 0 and reads as perfect generalization; here the method plainly learns, so the near-zero retention is a *result* and not an artefact of incompetence. Two cautions carried forward: it is **one seed**, so it ranks nothing; and eval-easy landing *under* the random floor is the shape that deserves an adversarial read before it is believed — a policy trained to exploit one appearance can plausibly do **worse** than random when the appearance changes, but "worse than chance" is also what a broken eval path looks like, and [C95](CONSTRUCTION.md#c95) is the precedent for preferring the second explanation until it is excluded |
| 2026-09-04 | **`idaac`'s shared-evaluator burden is discharged — and the point ratio that would have decided it was misleading in both directions.** `bt1ip5f8c6mqqm7fd2bn` ran our harness on the 100k checkpoint, 20 episodes, eval-easy scene 0, container CUDA: **9.023 (sd 6.488)** against idaac's own **5.347**. As a ratio that is **1.69x HIGHER** — which the audit's bands would have waved through as MARGINAL, because every upward deviation fell into one unbounded band. It is also not a real disagreement: their number is a mean of **10** episodes of a stochastic policy, and the difference of +3.676 is **z = 1.46**, not distinguishable at 95%. A one-sample interval on our 20 episodes alone would have said the opposite — `[5.99, 12.06]` **excludes** 5.347 | `scripts/audit_shared_evaluator.py::dispersion_test`, verdict `CONSISTENT`; supersedes the 1.663-vs-3.01 row on the 9,216-frame checkpoint, which was UNDERPOWERED because both numbers straddled the floor | closed | **Three defects in one instrument, all found by having a powered comparison rather than by inspection.** (1) The upward band was unbounded, so a harness reading 10x HIGH would have counted as a discharged burden — the same conflation the downward bands were split to remove, left standing on the far side because "ours is higher" felt benign. Now `> 1.5x` is `UNEXPLAINED-HIGH`. (2) The bands compared point estimates when the records carry spread; dispersion now outranks them, which the file's own docstring had already demanded and nothing had implemented. (3) The reference publishes **no** spread, so ours stands in for it — an assumption that makes the test conservative about declaring disagreement, which is why the verdict is `CONSISTENT` and deliberately not `PASS`. **2 of 12 discharged**, up from 1 |
| 2026-09-04 | **The cheap-artefact mechanism delivered nothing for the one job shape that needs it, and the job reported success.** `RECORDS_OUT` exists so eval records can be fetched without the ~300 MB archive; it copied `records.jsonl`, which `normalize_curves.py` writes from training **cells**. An offline-eval job has no cells, so `records.jsonl` was legitimately empty, `NATIVE_RECORDS_EMPTY` was announced, and the four eval records rode home inside `result.tgz` — where they had to be extracted by hand. Fixed by concatenating `offline_eval_<device>.jsonl` alongside it, with an `if` rather than `[[ -s ]] && cat`, because a false test as the last command of a loop body is the body's exit status and that is exactly how two earlier jobs died silently under `set -e` | `datasphere/native/run_probe.sh` (records block); observed on `bt1ip5f8c6mqqm7fd2bn`; `tests/test_datasphere_native_contract.py` 81 passing | closed | **The I3 arithmetic this was built for is now measured rather than estimated.** One `drqv2` cell at 100k returns **296 MB**: 199 MB of checkpoints (2 stamps x 104 MB, confirming [C60](CONSTRUCTION.md#c60)'s 50k cadence), 99 MB terminal snapshot, 21 MB tensorboard. At 6e5 the cadence yields 12 stamps, so a cell is **~1.5 GB**, and 12 baselines x 3 seeds is **~53 GB** against ~30 GB free on this laptop. [`EVAL-PROTOCOL`](EVAL-PROTOCOL.md) §6's rule — checkpoints stay remote, records come back — is not a preference at production scale; it is the only shape that fits, and until today the mechanism enforcing it was broken for eval jobs |
| 2026-09-04 | **The adversarial read on `drqv2`'s sub-floor eval number, performed rather than deferred: it is indistinguishable from random, not worse than random — and the random floor is regime-invariant by code, not by assumption.** The row above flagged eval-easy 1.439 against the 1.82 floor as the shape a broken eval path also produces. Two things settle it. **(1) The floor transfers.** `robosuitevgb/utils.py:67-104` sets `randomize_dynamics = False` in **every** branch — `train`, `eval-easy`, `eval-medium`, `eval-hard`, `cam-*` and the `else` — so the regimes differ in colour, lighting, moving light and video background and in **nothing** that touches physics, reward or the success test. A uniform-random policy ignores observations, so its return distribution is identical in every regime and [C55](CONSTRUCTION.md#c55)'s 1.82, measured in train, applies unchanged to eval-easy. **(2) The gap is noise.** 1.82 − 1.439 = 0.38, against a per-episode spread of order 6 at these budgets: far inside one episode's variation over 20 episodes. The correct statement is **"at the floor"**, and "below the floor" should not be written | `RL-ViGen-upstream/envs/robosuiteVGB/robosuitevgb/utils.py:67-104`; `vgb_wrapper.py:189-195`; eval.csv of `bt15e9v1k2ngmb71hnjn` | closed | **A side finding worth having: `DynamicsModder` is dead code on this path.** `VGBWrapper.__init__` defaults `randomize_dynamics=True` (`vgb_wrapper.py:59`) and instantiates the modder when set — but the factory every one of our runs goes through overrides it to `False` in all six branches, so the default is never reached. Anyone reading the wrapper alone would conclude the eval regimes perturb dynamics, and would be wrong. This is the second time this session that a constructor default read as behaviour and the call site decided otherwise — the same shape as the reward-normalisation near-miss, where a constructor argument was read and the wrapper *order* was what actually settled the question |
| 2026-09-04 | **A validation job was submitted that could not have validated the thing it was for, and the tell was ordering, not content.** `cfg-metrics-probe-ctrl-ppg-v66` was built to exercise the day's `ctrl` and `ppg` metric additions on hardware — the `ctrl` one being an aux-tuple edit inside a `jax.lax.scan` under `value_and_grad(has_aux=True)`, which nothing local can execute. It pointed at `payload-v61`, built **before** that edit. The job would have run, gone green, exercised C61 and the `RECORDS_OUT` fix, and been read as covering the aux-tuple change it never contained. Cancelled ~90 s in; `payload-v62` rebuilt; **each of the five changed files verified present in the archive by name** before resubmitting as v67 | `bt1heevq4b79i41oeaj4` (CANCELLED), superseded by `bt1k6p2b9dn9qu18itkd`; `datasphere/native/cfg-metrics-probe-ctrl-ppg-v67.yaml` header | closed | **A passing test that never ran the code is worse than a failing one**, and payload staleness produces exactly that with no symptom — the archive builds, verifies, uploads and runs. The general defect is that a payload's version number certifies *when it was built*, not *what it contains*, and every config names it by filename. The mitigation used here is cheap and should be the habit: after building, `tar xzO` each changed path and grep for the new identifier, which is four seconds and is the only step that distinguishes "the payload built" from "the payload carries the change". Related in kind to `job.sh`'s own header warning, written after a `sed` repointed a config at the wrong payload version |
| 2026-09-04 | **Two consecutive submissions of the same probe were wrong for two different reasons, and the guards caught both for about three minutes of compute.** v66 named a payload built before the edit it existed to validate (caught by hand, cancelled ~90 s in); v67 put `ctrl` and `ppg` in one job, which `family.py check-co-schedulable` refused in ~2 minutes because ctrl is jax and ppg is torch and they cannot share one python environment. Split into v68/v69, one family each, same cells and budget | `bt1heevq4b79i41oeaj4` CANCELLED, `bt1k6p2b9dn9qu18itkd` ERROR, `bt11jl4knvhss42nag5g` + `bt1apmmvvtfvjveil6ko`; `datasphere/native/run_probe.sh:449-451` | closed | **Recorded because the guard being right is the finding, not the slip.** `check-co-schedulable` was added 2026-09-02 to refuse exactly this *before* the bootstrap rather than after it, and it returned the constraint as a plain sentence naming both families and the fix. The contrast with the v66 failure is the point: **that one had no guard**, which is why it needed a human catch and now has `verify-payload --expect`. A constraint that is only written in a doc gets violated by whoever did not reread the doc; the same constraint in a preflight costs two minutes and cannot be skipped |
| 2026-09-04 | **The `ppg` half of the metric work is validated on hardware, and every prediction the config wrote down beforehand held.** `bt1apmmvvtfvjveil6ko` (2,560 frames, gt4.1): all six C61 keys appear in `progress.csv` — `train/entropy`, `train/mean_log_std`, `train/sigma_mean`, `train/boundary_fraction`, plus ppg's own `train/log_std_raw_mean` and `train/log_std_clamped_fraction`. `clamped_fraction` is **0.0**, as the config predicted for this budget. **`records.jsonl` came back at 4,246 bytes**, so the `RECORDS_OUT` fix works on the training branch as the idaac guard exercised the eval branch | `bt1apmmvvtfvjveil6ko` `cells/ppg-s1/progress.csv`; `datasphere/native/cfg-metrics-probe-ppg-v69.yaml` header, which names the falsifying outcomes | closed | **Two constants cross-checked exactly and neither was arranged**: entropy at `log_std = 0` came back **9.93257**, the value `ppg/train.py:104` records from an independent derivation, and `boundary_fraction` at σ=1 came back **0.3173**, the 0.317 C61 states. **One caveat stated rather than glossed**: `log_std_raw_mean` equals `mean_log_std` here, which is consistent with the clamped/raw split being wired correctly *and* with both being computed from one array — the two are only distinguishable when the clamp binds, which it does not at 2,560 frames. The split's correctness rests on the local test that drove a 3-of-7 clamp and got `clamped_fraction = 0.429`, not on this run |
| 2026-09-04 | **Every `ppg` record this project has ever produced had `frame: null` — a curve with no x-axis — and it was found by reading a returned CSV, not by any test.** `read_ppg` looked for `misc/total_timesteps` or `total_timesteps`; `phasic_policy_gradient/log_save_helper.py` writes neither. The column is **`Misc/InteractCount`** (2048, 4096, …, which is also why a requested 2,560 frames executes 4,096). Fixed, with the two old names kept as fallbacks for other PPG-lineage forks. The eval record was separately `frame: null` by construction and now carries the last training row's count, an **inference stated in the code**: ppg has no training-time evaluator, so `ppg_eval.py` runs once after training and the policy it measures is the endpoint | `datasphere/native/normalize_curves.py::read_ppg`; `bt1apmmvvtfvjveil6ko` `cells/ppg-s1/progress.csv`; re-normalised that cell to 2048 / 4096 / 4096 | closed | **A null field does not look like a defect, it looks like a field nobody filled** — which is why this survived a pre-production pass that ppg completed successfully, a metric inventory that catalogued ppg's y-axis in detail, and a table generator that prints ppg's frame count correctly *from a different reader*. The x-axis was never the thing being checked. **Left explicitly unresolved: whether one `InteractCount` equals one `frame` as the RL-ViGen five count them**, given action_repeat — recorded as an axis question rather than closed by the assumption that two similarly-named counters are one quantity, which is this project's standing rule and the reason the fix does not rename the key |
| 2026-09-04 | **The W&B sink reader's merge did not merge, and the claim that it did was mine, made the same day and checked only against a synthetic fixture.** `read_wandb_sink` grouped calls by the `step=` kwarg. `ctrl/train_ppo.py` issues **three** `wandb.log` calls per iteration and passes `step=` on only the first; the other two carry the identical count as a **data field**, `<env_name>/step`. So one iteration became three records — losses on a stepped one, `ep_return_200` and `ep_return_all` on two unstepped ones with `frame: null` — which is exactly the scattering the merge exists to prevent. Found by running the reader against a **real returned archive** rather than the fixture that was written to test it. Fixed by falling back to a `*/step` data field; the real archive's six records collapse to **two of twelve keys each** | `datasphere/native/normalize_curves.py::read_wandb_sink`; `bt1fer7809i7bpob7g2f` (a 2026-09-03 ctrl cell, re-read today); `tests/test_record_conventions.py::test_wandb_sink_finds_the_step_when_it_is_a_data_field` | closed | **The fixture encoded my assumption about the producer, so it could not contradict it** — both the code and its test believed `step=` was always the kwarg, and they agreed with each other perfectly. What broke the loop was replaying a real archive, which cost nothing: the artifact was already on disk from the day before. **The recovery itself is confirmed by the same run**: `ent`, `loss_actor`, `value_loss`, `total_loss`, `myow_loss`, `proto_loss`, `gae_val`, `target_val` and `value_pred` now reach the record set for `ctrl`, and before today **none of them reached any file at all** |
| 2026-09-04 | **`ctrl` needs `CUDA_ROOT=/usr/local/cuda` in its job env, and without it fails its import gate before any cell — the requirement existed only inside one config file.** `flax → jax → jax/_src/lib::_cuda_path()` raises `TypeError: expected str, bytes or os.PathLike object, not NoneType` on `nvidia.cuda_nvcc.__file__`. jax consults `CUDA_ROOT`/`CUDA_HOME` **first** and returns early when set; only when unset does it fall through to `_try_cuda_nvcc_import()`, the branch that crashes. **The variable does not mitigate the defect, it skips it.** `cfg-preprod-ctrl-v52.yaml` set it and succeeded; two probes derived from a *torch* job's config did not, and both failed | `bt11jl4knvhss42nag5g` (gt4.1) and `bt1pntaqvrqn8qj58pd8` (gt4i.1) both ERROR; `bt1fer7809i7bpob7g2f` the succeeding reference; `datasphere/native/families.json` ctrl `dependency_note`; `cfg-metrics-probe-ctrl-v71.yaml` | closed | **Two plausible explanations were proposed and both were killed by evidence rather than by argument, in that order.** (1) *Dependency drift* — jax is pinned (`jax[cuda12]==0.4.35`) but its `nvidia-*-cu12` wheels are not, so a floating CUDA wheel breaking a pinned jax is the textbook story. Killed by diffing the returned `resolved_packages.json`: **109 packages, zero differences**, `nvidia-cuda-nvcc-cu12==12.9.86` in both, and all five pip steps byte-identical including install order. (2) *The tier* — the success was on gt4i.1/L4, the first failure on gt4.1/T4, and `environment.json` read `cuda: true` against `cuda: false`. Killed by re-running on gt4i.1, which failed identically; and the `cuda` flag turned out to be **computed by importing jax**, so it was a restatement of the failure rather than evidence about it. **The actual cause was found by diffing the two job configs — the cheapest comparison available, and the last one tried.** Both wrong hypotheses are kept in the record next to the right one, because each was reasonable and the next person will think of them first |
| 2026-09-04 | **A `ctrl` cell passed its budget gate, trained, retained a valid checkpoint, and carried no measurement whatsoever — and nothing about it looked failed.** `bt14kc47s66282ic8ssb` ran 2,560 frames to `NATIVE_CELL_COMPLETED` with a 7.4 MB snapshot, **zero `Eprew200` lines** in `train_console.log`, and a W&B sink holding only the `init` event. ctrl's logging branch fires every **4112** frames (rows at 4112 and 8224 in the 10k cell `bt1fer7809i7bpob7g2f`), so the budget bought training and no curve. ctrl's declared floor was **1024** — "below one rollout nothing is written" — which guarantees a *rollout* while `check_budget`'s own docstring says its purpose is to refuse a budget too small to produce a *curve*. Those two conditions differ, and ctrl is the family where they differ. Floor raised to 4112 with the evidence recorded in it; the gate now rejects 2,560 and accepts 10,000 | `datasphere/native/families.json` ctrl `min_frames`; `datasphere/native/family.py::check_budget`; `bt14kc47s66282ic8ssb` vs `bt1fer7809i7bpob7g2f` | closed | **The run was not wasted: it retired the largest risk in the day's code.** The C28 change edits the aux tuple of `loss_actor_and_critic`, consumed inside `jax.lax.scan` under `value_and_grad(has_aux=True)` — a shape error there is unreachable from this laptop, which has neither jax-CUDA nor robosuite, and would otherwise surface first in a production cell. ctrl trained to completion with it in place, so the edit traces and runs. What it could **not** show is any metric arriving, because none was emitted; that needed a budget above the logging interval, which is `v73`. **An empty success is the failure mode worth naming here**: a floor set to the wrong invariant produced a green job with no data, and green jobs are not re-examined |
| 2026-09-04 | **The `ctrl` half of the metric work is validated end to end, and the C61 drift ranks across three baselines in the order the mechanism predicts.** `bt1ums2q8170s3cq5p9l` (10k, gt4i.1, `CUDA_ROOT` set): records now carry **18 keys** where the pre-edit archive carried 12. C28 arrives — `clip_fraction` 0.0846 → 0.0457, `approx_kl_k3` 0.00657 → 0.00427, declining as the policy settles, which is what a PPO clip fraction should do. C61 arrives — `mean_log_std` 0.00205 → 0.00398, `boundary_fraction` 0.3183 → 0.3192. Extrapolated to 6e5: `ppg` ≈ 0.12 (clamped), `idaac` ≈ 0.145 (normalises advantages), **`ctrl` ≈ 0.29 (neither)** — roughly double, and ctrl is the baseline [C61](CONSTRUCTION.md#c61) singles out as least protected | `bt1ums2q8170s3cq5p9l`; `runnable/ctrl/algo.py` (aux tuple), `runnable/ctrl/train_ppo.py` (`_find_log_std`); recovered through `read_wandb_sink`, since ctrl prints none of these | closed | **The ordering is the result; the individual numbers are weak.** Three budgets (4k, 8k, 99k), linear extrapolation from one or two intervals, and no repeats — a ranking reproduced across three baselines at three scales is a weaker instrument than it looks, and is recorded as suggestive rather than established. What it does support is that no measured head shows runaway entropy, and that the ordering is visible **at all**, which it was not a day ago on two of the three. **The edit that carried the most risk is now the best evidenced**: the aux-tuple change sits inside `jax.lax.scan` under `value_and_grad(has_aux=True)`, unreachable from this laptop, and it traces, runs, and emits values of the right magnitude. `ibac_sni` stays unmeasured and is still the exposed one |
| 2026-09-04 | **A finding recorded on 2026-08-17 was re-derived eighteen days later and briefly written up as new, because a gap was declared without searching the record.** Asked about augmentations and eval-time consistency, I read `audit_comparability_seam.py`, saw `render resolution` reporting 84/64/100, and concluded the `rad`/`soda` **100-render-then-84-centre-crop** was an unlisted axis. It is documented in full in [`AUDIT-2026-08-17.md`](AUDIT-2026-08-17.md) Question 4 — whose choice the sizes are, why patch **P6** exists, and the two failures it prevents: **RAD silently degrades to SAC** (`random_crop` computes `crop_max = 84 - 84 = 0` and its own guard returns the input unaugmented, no error, no warning) and **SODA hard-asserts** on `x.size(-1) == 100`. **The owner caught it**, from memory, mid-task — and then corrected the first version of THIS ROW, which claimed the finding lived in one dated audit "the script does not reference" | Recorded in at least five live documents, not one: `PART2-METRIC-INVENTORY.md:265` (*"`rad`/`soda` see an 84 crop of a 100"*, under §4 "The observation is NOT common"), `CONSTRUCTION.md:1497,1507,5745`, `AUDIT-2026-08-17.md` §Q4, `FAITHFULNESS.md`, `INTEGRATION-DELTA.md:182`; source at `dmc_gb/src/arguments.py:91-93` and `algorithms/modules.py:67-80` | closed | **The first version of this row understated the failure and has been rewritten.** It said one dated doc held the finding and the mechanical audit could not see it. In fact the finding is **pervasive in the record** — including `PART2`, the bottom-up inventory I had just cited as the thing the audit should be diffed against, and which the audit's other docstrings already cite by name. So the link existed and I did not follow it. **What I actually did was read PART2's section HEADINGS, map §4 to "covered", and never open the section.** A table of contents is not the content, and treating the mechanical axis list as the index of what is known inverts the epistemics: the audit is DOWNSTREAM of reading implementations, and using it to decide what has been established is how a finding recorded five times becomes invisible. Fixes: the axis is derived and cites its origin; "grep `docs/` before declaring a gap" is in the protocol comment; and the standing correction is that **coverage is established by reading the section, not by matching its title**. Committed a miniature of the same error while fixing it — the replay-buffer entry went into `NOT_COVERED` as a *comment*, leaving the list empty and the audit still printing "0 underived" |
| 2026-09-04 | **[C61](CONSTRUCTION.md#c61)'s trip-wire fired on the baseline the entry named, hours after the trip-wire was written — `ibac_sni`'s policy is mostly clipping by 50k frames.** `bt1cj6rgeptsu9f3v0o6`, 100k: `boundary_fraction` **0.318 → 0.580**, crossing the 0.50 threshold at ~50k; `mean_log_std` **0.002 → 0.592**; σ **1.002 → 1.811**, monotone at every stamp. At the SAME budget `idaac` sits at 0.024 — **25x less drift**, with no extrapolation in the comparison. More than half of `ibac_sni`'s action mass now falls outside the box robosuite accepts, so most of what its network emits is decided by the clip rather than by the policy | `bt1cj6rgeptsu9f3v0o6` `cells/ibac_sni-s1/log.csv`; `runnable/ibac_sni/torch_rl/scripts/train.py:253-257`; shared definition `scripts/metrics.py::gaussian_policy_health` | open | **The entry predicted this baseline by mechanism, before any measurement: no clamp, no advantage normalisation, and the 227x model of [C3](CONSTRUCTION.md#c3).** The morning's default — hold 0.01 everywhere and decide on the diagnostic — was written with a named threshold precisely so this could not be argued about after the fact, and the threshold did its job on the first cell that could test it. **Left OPEN and handed up rather than defaulted**, because the two options are not equivalent in kind: lowering `entropy_coeff` for `ibac_sni` alone is a declared per-baseline deviation from the authors' value, while leaving it makes every `ibac_sni` number a measurement of a policy that is mostly clipping. **Not established**: one seed, one budget, and the return column is noisy across the same stamps (12.79 → 0.97 → 7.63 → 4.50 → 4.38 → 4.74), so the drift is not yet tied to a loss of reward — whether the clipping COSTS return on Door is a separate measurement |
| 2026-09-04 | **The recovery workspace is under version control at last — 637 files, 8.2 MB, one local commit — and the act of doing it reproduced the day's error a third time.** This tree had no `.git` at all; everything since 2026-08-31 lived in one directory on one laptop. Tracked content follows the rule the project had already written for itself in `scripts/deviations.py`: clones carry their own `.git` and are reproducible from `ext/` plus `runnable/_patches/*.patch`, so patches are tracked and the 200 MB clones are not. **But I wrote `.gitignore` without reading it, and one already existed** — the authored file from the versioned original, which is strictly better than what I replaced it with: `runnable/*/` plus negations rather than six hardcoded clone names, and `/data/`, `/wandb/`, `.greenmark`, `*.pth`, `/lift.xml`, which mine dropped. Consequence: `.greenmark`, a 17-byte token the original ignores deliberately, **was committed**. Recovered by copying the original back verbatim and appending only the genuinely new rules; token untracked; commit amended | commit `5459e39`; original at `ccm-intro/projects/many-gens-rl-vigen/.gitignore` (1,104 bytes, the recovery source); `git ls-files -- .greenmark` now empty | closed | **Third instance in one session of acting on a surface without opening the source**, after the `rad`/`soda` crop and PART2's section headings — and the worst of the three, because the other two only cost re-derivation while this one destroyed an authored artifact. It was recoverable **only because the versioned original still holds the file**, which is precisely the property this tree lacked and the commit exists to supply. The `/data/` rule it nearly lost carries its own scar: *"it must never be committed -- it was, and it is why `.git` is ~480 MB."* **The general lesson is narrower than "read before writing": a file whose absence is plausible is not thereby absent**, and `Write` reported "updated", not "created", which said so at the time |
| 2026-09-04 | **The twelve share exactly ONE checkpoint frame — the endpoint — so the cross-baseline offline curve the evaluation protocol is built on cannot currently be produced at all.** [`EVAL-PROTOCOL`](EVAL-PROTOCOL.md) §0 makes checkpoints primary, because three of twelve run no periodic training-time evaluation. But per-family save cadence is: `rlvigen` 50k stamps, `dmc_gb` 100k, `alda` 50k configured though its pattern names only `{endpoint}`, and **`ctrl`, `ibac_sni`, `idaac`, `ppg` have no `save_every` at all** — single-file checkpoints, terminal only. Verified against returned archives, not just the descriptor: the `drqv2` 100k cell has **5 eval points (0/25k/50k/75k/100k) and 2 checkpoints (50k, 100k)**, and the `idaac`, `ppg` and `ctrl` cells each returned exactly one. So three of five during-train eval points for `drqv2` cannot be reproduced offline, extended to other scenes, or compared across baselines | `datasphere/native/families.json` production blocks; `bt15e9v1k2ngmb71hnjn` `cells/drqv2-s1/{eval.csv,checkpoints/}`; `bt1lnh6b11u231cvh4ho`, `bt1apmmvvtfvjveil6ko`, `bt1ums2q8170s3cq5p9l` | open | **This corrects the plan's own §3b #3 recommendation** — *"terminal-only for the endpoint plus the 50k stamp grid for a curve"* — which is not executable for seven of twelve, because that stamp grid exists only for the RL-ViGen five. **The cost of fixing it is graded, and the grades are what make it decidable rather than one lump**: `ibac_sni` has a **live** `--save-interval` (`torch_rl/scripts/train.py:286`, default 0) and `dmc_gb` a live `--save_freq`, so those two are configuration and cost no fidelity at all; `ctrl` declares `checkpoint_interval` (`train_ppo.py:98`) and **never reads it**, so wiring it is a small declared deviation closer to RESTORES than ENABLES; `idaac` and `ppg` have no mechanism, so periodic saving there is authored behaviour and a real deviation. **Default, pending the owner: report the cross-baseline table at the ENDPOINT only** — the one frame every baseline can supply — take the two free configuration wins, and leave `idaac`/`ppg` terminal-only rather than author saving into them for a curve nobody has asked for yet |
| 2026-09-04 | **The undeclared-modification guard had been INERT for the entire recovery-workspace era, and adding version control is what exposed it — first by making it fire on the wrong tree.** `apply_patches.py::undeclared_modifications` runs `git -C RL-ViGen-upstream status --porcelain`, which walks UP to the nearest enclosing repository. In this workspace `RL-ViGen-upstream` is unpacked from the `rlvigen-door2-*.tgz` job input and has **no `.git`**, so since 2026-08-31 the command failed, the function returned `[]` on its error path, and the check passed on every run while verifying nothing. The instant a repository existed at the project root the command succeeded against the **wrong** tree and reported this session's edited `docs/*.md` and `scripts/eval_grid.py` as "undeclared modifications in the vendored tree" | `setup/apply_patches.py::undeclared_modifications`; `git -C RL-ViGen-upstream rev-parse --show-toplevel` resolved to the project root; `tests/test_contract.py::test_upstream_patches_are_applied` | closed | **One unchecked assumption — "UPSTREAM is its own repository" — produced a silent false negative for weeks and then a loud false positive in the same hour.** The loud one was the useful half: it is why the silent one was found at all. Fixed by verifying the resolved toplevel equals `UPSTREAM` and, when it does not, **announcing the abstention on stderr instead of returning an empty list** — because a guard that cannot run and a guard that found nothing must not look the same, which is this project's signature join defect ([STEP-ZERO](STEP-ZERO.md) §8) in yet another place. On a machine where `setup/install.sh` clones upstream normally, the check behaves exactly as before. **Note the direction of causation for the record**: `git init` did not break this check, it revealed that the check had never been running here |
| 2026-09-04 | **Every production cost this project has quoted was computed over TEN baselines while being described as twelve, because a stale exclusion note survived the runs that invalidated it.** `plan_production.py`'s header read *"Costs cover only the ten baselines with measured throughput; alda and ctrl have no successful CUDA run yet"*. Both had since completed — `alda` at `bt13km8g093do0fdtc58`, `ctrl` at `bt1ums2q8170s3cq5p9l` — so the totals were short by a sixth of the fleet. Corrected by timing both from their returned archives: alda 10,000 frames in 1001.92 s and ctrl 10,000 in 587.83 s, both on gt4i.1, divided by the 1.14 tier factor this file already measures on `drqv2` to reach its gt4.1 basis. **The revision moves the headline number across a threshold**: a twelve-baseline 1e5 × 1-seed sweep is **1.1× the grant, not 0.9×** — over it, not inside it | `datasphere/native/plan_production.py` `MEASURED_FPS_GT4_1`, `FPS_BASIS`; `cells/alda-s1/training.log`, `cells/ctrl-s1/training.log` | closed | **The two new rates are CONVERTED, not measured, and the file's own rule forbids pooling those silently** — so `FPS_BASIS` records which is which and `throughput_source` now emits `converted from gt4i.1 x1.14` for exactly those two rows. **A number I reported to the owner was wrong in the direction that mattered**: I described a shape as fitting inside the grant when the fleet it was computed over was incomplete. The defect is not arithmetic, it is that a *cached judgement about which baselines work* outlived the evidence — the same shape as the register rows whose status contradicted their prose, and as the guard that had been inert for a fortnight |
| 2026-09-04 | **R7 moves from NOT MET to NEEDS JUDGEMENT, and the reason it was failing was that this project threw its own measurements away.** Every job's records were read once out of a scratch directory and discarded, so the repo held **0 training logs and 0 eval result sets** while six jobs had produced them. `results/records/` and `results/logs/` now retain them — **1,001 records and five training curves across six jobs, 1.6 MB total** — which is exactly the storage rule [`EVAL-PROTOCOL`](EVAL-PROTOCOL.md) §6 already prescribed (*"checkpoints stay remote, records come back"*) and which nothing had implemented. With the artifacts present, every mechanical link of the clone-to-curve chain holds; what remains is the human acceptance test, which no script can perform. **R3 is now the only hard NOT MET, and it is the owner's decision (P-C76).** | `scripts/requirements.py::r7`; `results/README.md`; jobs `bt15e9v1k2ngmb71hnjn`, `bt1lnh6b11u231cvh4ho`, `bt1ip5f8c6mqqm7fd2bn`, `bt1apmmvvtfvjveil6ko`, `bt1ums2q8170s3cq5p9l`, `bt1cj6rgeptsu9f3v0o6` | closed | **Two of R7's four links were graded against the PRE-C95 LOCAL pipeline** — `exp_local/**/train.csv` and `results/**/*__train.json`, both written only by local drivers, and [C95](CONSTRUCTION.md#c95) established that a locally produced number is invalid. So the predicate could not have passed no matter what the clone era produced. Widened to accept returned-archive shapes **in addition to**, not instead of, the old globs, which stay correct in the canonical tree; link 3 still demands two independent result sets and only counts a records file that actually carries eval rows. **I also nearly buried the fix**: my `.gitignore` addition included `/results/`, the very directory `requirements.py` names as R7's deliverable home, written without checking what the path means here — the second ignore-rule error of the day, caught by testing the rule rather than by reading it |
| 2026-09-04 | **The checkpoint-cadence table asked the wrong question and got four families wrong; eight of twelve can produce a curve, not five.** Yesterday's pass graded families on *do they declare a `save_every`*. The question that decides whether a curve exists is **whether each stamp gets its own filename or overwrites one path**. Re-read: `dmc_gb` writes `model/{frames}.pt` and `alda` writes `sac_{env}_step_{env_steps}.pt` (`alda_trainer.py:688`) — **both keep every stamp**; `ibac_sni` passes `--save-interval 1` *literally* in `families.json`'s `options`, so it saves **every update**, into `utils/save.py`'s fixed `model.pt`, overwritten each time; `idaac` is guarded by `if j == num_updates - 1` (`train.py:245`), terminal by upstream design. So the curve-capable set is `rlvigen` (via P18) + `dmc_gb` + `alda` = **eight baselines**, and only `idaac`, `ibac_sni`, `ppg`, `ctrl` are endpoint-only | `datasphere/native/families.json` `options` blocks; `alda_trainer.py:688`; `ibac_sni/torch_rl/utils/save.py:11,24-27`; `idaac/train.py:245` | closed | **I nearly shipped two wrong changes off a single-surface read.** Having grepped only `runnable/_launch/*.sh` for `SAVE_EVERY`, I concluded nothing consumed it and wired `--save_freq` into `dmc_gb.sh` and a frames→updates conversion into `ibac_sni.sh`. Both were wrong: `families.json`'s **`options`** already passes `--save_freq {save_every}` (so mine duplicated the flag) and already passes `--save-interval 1` (so mine would have fought a literal value and cut ibac_sni's saving by 390x). Caught only because a tool result printed the `options` block I had not looked at. **The same one-surface error as the crop and the `.gitignore`, three times in a day, and the surviving lesson is narrow and mechanical: a launcher is not the only thing that can pass an argument.** What survives of the change is real: `dmc_gb`'s cadence moved 100000 → 50000 to share the RL-ViGen stamp grid, which is a genuine free win because its stamps have distinct names. **The endpoint-only DEFAULT is unaffected** — the intersection across twelve is still the endpoint |
| 2026-09-04 | **The citation checker's strictest stratum is empty for the first time, and two of the three defects it held were caused by my own edits hours earlier.** `check_citations.py --content` grades a small high-precision stratum (one candidate file, nearest anchor, bound within 40 chars, shaped like a quoted line); the 2026-08-18 row that built it measured ~57% precision and called it *"a lead generator, not a verdict machine"*. Run today it held three: `eval_across_scenes.py:328 → 342` and `:166 → 218`, both shifted by the per-episode-flag edit I made this morning, and `dmc_gb/src/train.py:149 → 174` cited from `AUDIT-2026-08-17.md` — which is the **canonical** audit ("Edit it, not the HTML"), so actionable rather than a frozen snapshot. All three repointed against the live files | `scripts/check_citations.py --content`; `docs/CONSTRUCTION.md:4926,5819`; `docs/AUDIT-2026-08-17.md:339` | closed | **The third was not a defect at all and was the more useful find**: the anchor `succeeded = succeeded or ...` contains a literal ellipsis, so the writer abbreviated the line rather than quoting it, and no such span can ever match source — it would report ABSENT forever however correct the citation. `usable_anchor` now rejects ellipsis spans, because **a permanent false positive is worst precisely in the strictest stratum**: that stratum's whole value is that a starred line is worth reading, and one that always fires teaches the reader to skip them. 168 candidates remain in the looser strata at the ~57% precision the original row measured; they are leads, and the corpus-wide sweep is not claimed as done |
| 2026-09-04 | **`apply_patches.py --check` was reported as failing twice, and the broken thing was the verification loop.** A sweep of the form `for s in "scripts/a.py" "setup/apply_patches.py --check"; do python $s; done` returned exit 2 for the one entry containing a space. **zsh does not word-split unquoted parameter expansions** (bash does), so `python $s` passed the whole string as a single filename and python exited 2 on `can't open file '.../apply_patches.py --check'`. Run directly the check exits 0 and prints *"The vendored tree matches its pinned commit plus exactly the declared differences."* With `eval`, every instrument exits 0 except `deviations.py`, which exits 1 by design because the clones are not whole in this tree | reproduced both ways in one command; `Shell: /bin/zsh` per the environment | closed | **I saw this signal earlier the same day, ran the check directly, got 0, and wrote it off as "my earlier loop misreported" without finding out why.** That was the correct conclusion and the wrong response: a discrepancy between two runs of the same command is itself a finding, and leaving it unexplained meant reporting the same phantom failure a second time hours later. **The general form is the day's recurring one at one more remove — the harness that checks the work can be the thing that is broken**, and it is the least suspected component precisely because it is not the work. Cheap rule: when a checker disagrees with itself, reproduce both invocations side by side before believing either |
| 2026-09-04 | **Retaining job records turned three tests from SKIPPED to FAILING, and the tests were right to fire — their skip guard was a proxy.** `test_verify_cells.py` and `test_results_table.py` guarded on `not (ROOT / "results").is_dir()`, reading *"does `results/` exist"* as *"has anything been tabulated here"*. That held only while `results/` had exactly one use. Adding `results/records/` and `results/logs/` brought the parent into existence for an unrelated reason, and the tests began checking retention grids (`results/regime-retention-c69/`) that **have never existed in this tree**. Guards narrowed to name the directory each actually needs | `tests/test_verify_cells.py:97`, `tests/test_results_table.py:30`; suite back to its one deliberate failure | closed | **A skip guard is a claim about the world, and this one was load-bearing without being true.** The pattern is the day's, one layer down: a proxy standing in for the condition it approximates, correct until something else moved. Worth noting the direction — the tests failed *loudly* the moment their premise broke, which is the good failure mode; had they instead skipped on a narrower-but-wrong condition they would have gone quiet and nobody would have looked. **Separately, `test_nd_ln_style_train` failed once with `linalg_qr: MPS kernel failed with error code 0` and passed twice on rerun** — a hardware flake, recorded here so a future run does not read it as a regression, and consistent with [C84](CONSTRUCTION.md#c84)'s narrowing that MPS-execution faults are real and local-only |
| 2026-09-04 | **All twelve baselines can now keep intermediate checkpoints, so "endpoint" and "best over the trajectory" are both reconstructible from retained data.** Requested by the owner, under the standard they set: *the original algorithms and models stay fidelity-bound; checkpointing and metric reporting may be ours where we are sure of them.* Four families were endpoint-only and each needed a different, minimal intervention — **and two needed no clone edit at all**: `ppg` already implements `save_mode="all"` (`log_save_helper.py:129`, writing `model<NNN>.jd`) and its own `train.py` hardcoded `"last"`, so exposing `--save_mode`/`--ic_per_save` with upstream defaults was enough; `ctrl` **declares `checkpoint_interval` at `train_ppo.py:98` and never read it**, so wiring it is nearer RESTORES than ENABLES. `idaac` (saved only at `j == num_updates - 1`) and `ibac_sni` (saved every update into a fixed, overwritten `model.pt`) each gained a frame-cadence flag defaulting to **0 = off**, so an unchanged command line is unchanged. All seven families now share a **50k** grid with declared `intermediate_checkpoints` globs | `families.json` (7 families); `ctrl/train_ppo.py`, `idaac/train.py`, `idaac/.../arguments.py`, `ibac_sni/.../train.py`, `ppg/.../train.py` | open | **Every edit is ENABLES-class on the strict reading — it changes what is RETAINED, never what is learned** — and each writes *beside* the endpoint file, never over it, so the checkpoint of record is untouched and each repo's own evaluator still finds what it expects. **Not yet validated on hardware**, which is the honest status: these are five clone edits that no local test can execute, and the last time I shipped one unvalidated it was only luck that it worked. A single short probe per family would settle it. **Five line-number anchors went stale as a side effect** and were repointed; one test (`test_families_with_no_runtime_dial_resolve_empty`) encoded the old fact that these four families had *nothing* to dial, and was rewritten to assert what it actually cares about — that none of them gains an EVAL cadence, because none evaluates during training at all |

*(155 entries as of 2026-09-04 — counted by `tests/test_docs_not_stale.py::test_the_register_entry_count_is_true`, after the hand-maintained figure was found to be two short before this session even began. Expect this to grow, not shrink, as Stage 2 work proceeds —
per §5, an empty or short register is a failure of the register, not evidence of a clean
codebase.)*
