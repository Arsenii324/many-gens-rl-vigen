--index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121
--extra-index-url https://pypi.org/simple
# [Codex 2026-09-01 10:51 MSK: pin direct Robosuite requirements because the native bootstrap intentionally installs that upstream package with --no-deps]
numpy==1.26.4
numba==0.59.1
scipy==1.13.1
mujoco==2.3.7
gym==0.25.2
dm_control==1.0.14
dm_env==1.6
hydra-core==1.3.2
hydra-submitit-launcher==1.2.0
opencv-python==4.10.0.84
Pillow==10.4.0
# [Codex 2026-09-01 15:01 MSK: robosuiteVGB imports matplotlib unconditionally but its setup metadata is empty; failed remote job bt15uqu1idas8dkq38vf exposed the missing closure]
matplotlib==3.7.5
# [Codex 2026-09-01 15:20 MSK: Robosuite wrappers import demo_sampler_wrapper unconditionally, so this requirements-extra dependency belongs in the audited native import closure]
h5py==3.11.0
PyOpenGL==3.1.7
pyyaml==6.0.2
tensorboard==2.17.1
imageio==2.35.1
imageio-ffmpeg==0.5.1
pynput==1.7.7
# [Codex 2026-09-01 10:17 MSK: RL-ViGen logger imports this undeclared package; raw job bt1kit... failed without it]
termcolor==2.5.0
# [Claude 2026-09-02 04:20 MSK: dmcontrol-generalization-benchmark's own setup/conda.yaml declares
# these four and this tree installs none of them; rad and soda import kornia and tqdm on their
# training path. Pinned exactly for the same reason every line above is: the resolver must not
# choose a different version between one baseline's calibration and another's.]
kornia==0.7.3
einops==0.8.0
xmltodict==0.13.0
tqdm==4.66.5
# [Claude 2026-09-02 07:45 MSK: RL-ViGen's rl_utils.py does `from captum.attr import
# GuidedBackprop, GuidedGradCam` at module scope, and only algos/sgqn.py imports rl_utils. Hydra
# swallows the ModuleNotFoundError and reports "Error locating target 'algos.sgqn.SGQNAgent'",
# which reads like a config problem. Raw evidence: cell sgqn-s1 of job bt1qb6jomkvrqki8nsj9.
# Fifth undeclared import on this path after termcolor, numba, matplotlib and h5py.]
captum==0.7.0
