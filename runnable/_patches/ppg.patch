diff --git a/phasic_policy_gradient/distr_builder.py b/phasic_policy_gradient/distr_builder.py
index 41c447f..b368221 100644
--- a/phasic_policy_gradient/distr_builder.py
+++ b/phasic_policy_gradient/distr_builder.py
@@ -10,8 +10,26 @@ def _make_categorical(x, ncat, shape):
 
 
 def _make_normal(x, shape):
-    warnings.warn("Using stdev=1")
-    return dis.Normal(loc=x.reshape(x.shape[:-1] + shape), scale=1.0)
+    """CONTINUOUS HEAD -- authored here, not descended from anything.
+
+    Upstream this function was UNREACHABLE: `tensor_distr_builder` raised on every non-Discrete
+    eltype, so nothing ever called it, and its body hardcoded `scale=1.0` under the authors' own
+    `warnings.warn("Using stdev=1")`. RL-ViGen's robosuite action space is Box(-1,1,(7,)), so a
+    real Gaussian head has to exist. It is authored; there is nothing in this repo to verify it
+    against.
+
+    `x` arrives as [loc | log_stdev] concatenated on the last axis. The log_stdev half is a
+    state-INDEPENDENT nn.Parameter held by PhasicValueModel and broadcast there -- that is what
+    PPO does for continuous control. Emitting it from the policy head instead would have needed
+    zero lines in ppg.py, and was rejected: a state-dependent stdev is a different algorithm,
+    not a cheaper spelling of this one.
+    """
+    loc, logstd = th.chunk(x, 2, dim=-1)
+    return dis.Normal(
+        loc=loc.reshape(loc.shape[:-1] + shape),
+        # Clamp only guards exp() overflow; the range is wide enough never to bind in practice.
+        scale=logstd.reshape(logstd.shape[:-1] + shape).clamp(-5.0, 2.0).exp(),
+    )
 
 
 def _make_bernoulli(x, shape):  # pylint: disable=unused-argument
@@ -31,6 +49,10 @@ def tensor_distr_builder(ac_space):
             eltype.n * ac_space.size,
             partial(_make_categorical, shape=ac_space.shape, ncat=eltype.n),
         )
+    if isinstance(eltype, Real):
+        # Width is ac_space.size, NOT 2*size: PhasicValueModel concatenates the state-independent
+        # log_stdev onto this head's output before calling _make_normal. See _make_normal.
+        return (ac_space.size, partial(_make_normal, shape=ac_space.shape))
     else:
         raise ValueError(f"Expected ScalarType, got {type(ac_space)}")
 
diff --git a/phasic_policy_gradient/envs.py b/phasic_policy_gradient/envs.py
index 0292a29..958b663 100644
--- a/phasic_policy_gradient/envs.py
+++ b/phasic_policy_gradient/envs.py
@@ -1,6 +1,11 @@
 import numpy as np
+import hashlib
 import gym3
-from procgen import ProcgenGym3Env
+# [Gemini 2026-08-31] ProcgenGym3Env is optional for Robosuite continuous control
+try:
+    from procgen import ProcgenGym3Env
+except ImportError:
+    ProcgenGym3Env = None
 
 def get_procgen_venv(*, env_id, num_envs, rendering=False, **env_kwargs):
     if rendering:
@@ -14,7 +19,87 @@ def get_procgen_venv(*, env_id, num_envs, rendering=False, **env_kwargs):
         env = gym3.ViewerWrapper(env, info_key="rgb")
     return env
 
+def get_robosuite_venv(*, env_id, num_envs, seed=0, scene_id=0, mode="train", **_ignored):
+    """RL-ViGen robosuite as a gym3 venv. `_ignored` swallows procgen-only kwargs."""
+    import os, sys, gym
+    condition_seed = _ignored.get("condition_seed")
+    placement_witnesses = []
+    _rlv = os.environ["RLVIGEN_ROOT"]
+    for _p in (_rlv, os.path.join(_rlv, "envs", "robosuiteVGB")):
+        if _p not in sys.path:
+            sys.path.insert(0, _p)
+    from robosuitevgb.utils import make_env as _robo_make_env
+
+    class _HWCUint8(gym.ObservationWrapper):
+        """RL-ViGen yields {'rgb': (C,H,W) uint8} under a float64 Box it never fills.
+        ImpalaCNN takes (H,W,C) and gym3 typechecks against the space, so fix both."""
+        def __init__(self, env):
+            super().__init__(env)
+            c, h, w = env.observation_space["rgb"].shape
+            self.observation_space = gym.spaces.Box(0, 255, (h, w, c), dtype=np.uint8)
+
+        def observation(self, obs):
+            return np.asarray(obs["rgb"], dtype=np.uint8).transpose(1, 2, 0)
+
+        def seed(self, seed=None):
+            "gym3.FromGymEnv calls this unconditionally; RL-ViGen's `seed` is an int attribute,"
+            "not a method, and the env is already seeded by make_env(seed=...) above."
+            return []
+
+    class _ConditionSeed(gym.Wrapper):
+        def __init__(self, env):
+            super().__init__(env)
+            self._episode_index = 0
+
+        def reset(self, **kwargs):
+            if condition_seed is not None:
+                value = int(np.random.SeedSequence(
+                    [int(condition_seed), int(scene_id), self._episode_index]
+                ).generate_state(1, dtype=np.uint32)[0])
+                np.random.seed(value)
+                self._episode_index += 1
+            observation = self.env.reset(**kwargs)
+            digest = hashlib.sha256()
+            value = observation
+            if isinstance(value, dict):
+                for key in sorted(value):
+                    array = np.asarray(value[key])
+                    digest.update(str(key).encode() + str(array.shape).encode())
+                    digest.update(array.tobytes())
+            else:
+                array = np.asarray(value)
+                digest.update(str(array.shape).encode() + array.tobytes())
+            placement_witnesses.append(digest.hexdigest())
+            return observation
+
+    applied_regimes = []
+
+    def _make_one(i):
+        # ConcatEnv hides the Gym wrappers it receives.  Retain the construction-time read-back
+        # here, before it is hidden, so the offline evaluator can refuse a silently ignored
+        # mode/scene without consuming a reset (which would perturb C69's global placement RNG).
+        base = _robo_make_env(task_name=env_id, seed=seed + i, scene_id=scene_id, mode=mode)
+        applied_regime = {
+            "mode": getattr(base, "_mode", None),
+            "scene_id": getattr(base, "_scene_id", None),
+        }
+        if None in applied_regime.values():
+            raise RuntimeError("RL-ViGen did not expose its applied mode/scene at construction")
+        applied_regimes.append(applied_regime)
+        return gym3.FromGymEnv(_ConditionSeed(_HWCUint8(base)))
+
+    venv = gym3.ConcatEnv([_make_one(i) for i in range(num_envs)])
+    applied_regime = applied_regimes[0]
+    if any(item != applied_regime for item in applied_regimes[1:]):
+        raise RuntimeError("RL-ViGen vector members disagree about the requested regime")
+    venv._vigen_regime = dict(applied_regime)
+    venv._placement_witnesses = placement_witnesses
+    return venv
+
 def get_venv(num_envs, env_name, **env_kwargs):
+    if env_name.startswith("robosuite:"):
+        return get_robosuite_venv(
+            env_id=env_name.split(":", 1)[1], num_envs=num_envs, **env_kwargs)
     venv = get_procgen_venv(num_envs=num_envs, env_id=env_name, **env_kwargs)
 
-    return venv
\ No newline at end of file
+    return venv
diff --git a/phasic_policy_gradient/log_save_helper.py b/phasic_policy_gradient/log_save_helper.py
index af09fe3..ad1474a 100644
--- a/phasic_policy_gradient/log_save_helper.py
+++ b/phasic_policy_gradient/log_save_helper.py
@@ -1,3 +1,10 @@
+try:
+    from safe_checkpoint import safe_torch_save
+except ImportError:
+    def safe_torch_save(obj, path, torch_kwargs=None, **_kwargs):
+        import torch as _torch
+        _torch.save(obj, path, **(torch_kwargs or {}))
+        return True
 import os
 import time
 import resource
@@ -69,6 +76,11 @@ class LogSaveHelper:
         self.roller_stats = {
             "EpRewMean": self._nanmean([] if roller is None else roller.recent_eprets),
             "EpLenMean": self._nanmean([] if roller is None else roller.recent_eplens),
+            # SUCCESS RATE -- see vec_monitor2.py. PPG has no eval env at all, so this is the
+            # TRAIN distribution's success rate, not a generalisation number.
+            "EpSuccessMean": self._nanmean(
+                [] if roller is None else
+                [float(i.get("episode_success", float("nan"))) for i in roller.recent_epinfos]),
         }
         if roller is not None and self.log_new_eps:
             assert roller.has_non_rolling_eps, "roller needs keep_non_rolling"
@@ -131,7 +143,7 @@ class LogSaveHelper:
         basename += f"{suffix}.jd"
         fname = os.path.join(logger.get_dir(), basename)
         logger.log("Saving to ", fname, f"IC={self.total_interact_count}")
-        th.save(self.model, fname, pickle_protocol=-1)
+        safe_torch_save(self.model, fname, torch_kwargs={"pickle_protocol": -1}, label="ppg.model")
         self.save_idx += 1
 
     def _nanmean(self, xs):
diff --git a/phasic_policy_gradient/ppg.py b/phasic_policy_gradient/ppg.py
index 69e951c..ad6b40f 100644
--- a/phasic_policy_gradient/ppg.py
+++ b/phasic_policy_gradient/ppg.py
@@ -6,6 +6,7 @@ import itertools
 from . import torch_util as tu
 from torch import distributions as td
 from .distr_builder import distr_builder
+from gym3.types import Real
 from mpi4py import MPI
 from .tree_util import tree_map, tree_reduce
 import operator
@@ -106,6 +107,13 @@ class PhasicValueModel(PhasicModel):
         lastsize = self.get_encoder(self.pi_key).codetype.size
         self.pi_head = tu.NormedLinear(lastsize, pi_outsize, scale=0.1)
         self.aux_vf_head = tu.NormedLinear(lastsize, 1, scale=0.1)
+        # CONTINUOUS HEAD (authored; see distr_builder._make_normal). PPO holds the Gaussian's
+        # log-stdev as a state-independent parameter rather than emitting it from the policy
+        # head. Discrete action spaces are untouched -- this stays None and forward() skips it.
+        self.pi_logstd = (
+            th.nn.Parameter(th.zeros(pi_outsize))
+            if isinstance(actype.eltype, Real) else None
+        )
 
     def compute_aux_loss(self, aux, seg):
         vtarg = seg["vtarg"]
@@ -142,6 +150,8 @@ class PhasicValueModel(PhasicModel):
 
         pi_x = x_out[self.pi_key]
         pivec = self.pi_head(pi_x)
+        if self.pi_logstd is not None:
+            pivec = th.cat([pivec, self.pi_logstd.expand_as(pivec)], dim=-1)
         pd = self.make_distr(pivec)
 
         aux = {}
@@ -185,7 +195,13 @@ def aux_train(*, model, segs, opt, mbsize, name2coef):
         mb = tree_map(lambda x: x.to(tu.dev()), mb)
         pd, _, aux, _state_out = model(mb["ob"], mb["first"], mb["state_in"])
         name2loss = {}
-        name2loss["pol_distance"] = td.kl_divergence(mb["oldpd"], pd).mean()
+        # PPO's continuous log-probability sums over the 7 action coordinates.  The original PPG
+        # line was written for a categorical distribution, where KL is already scalar per sample;
+        # applying its mean reduction to the adapted diagonal Normal weakens beta_clone by the
+        # action dimension.  Sum event coordinates, then average batch/time, preserving the PPG
+        # coefficient's scale at this continuous design point.
+        kl = td.kl_divergence(mb["oldpd"], pd)
+        name2loss["pol_distance"] = kl.sum(-1).mean() if kl.ndim > 1 else kl.mean()
         name2loss.update(model.compute_aux_loss(aux, mb))
         assert set(name2coef.keys()).issubset(name2loss.keys())
         loss = 0
diff --git a/phasic_policy_gradient/ppo.py b/phasic_policy_gradient/ppo.py
index 807c681..3d5f482 100644
--- a/phasic_policy_gradient/ppo.py
+++ b/phasic_policy_gradient/ppo.py
@@ -15,6 +15,18 @@ from .reward_normalizer import RewardNormalizer
 import math
 from . import logger
 
+# [OURS] C61/C28 policy-health diagnostics. `scripts/metrics.py` owns the definitions so the four
+# continuous PPO-family baselines report the SAME quantity rather than four near-misses of it;
+# `idaac` and `ibac_sni` already call it. Loaded by path because this clone runs with its own
+# repository root on sys.path and `scripts` is not importable from inside it.
+import importlib.util as _ilu
+import pathlib as _pl
+_spec = _ilu.spec_from_file_location(
+    "_vigen_metrics", _pl.Path(__file__).resolve().parents[2].parent / "scripts" / "metrics.py")
+_m = _ilu.module_from_spec(_spec)
+_spec.loader.exec_module(_m)
+_policy_health = _m.gaussian_policy_health
+
 INPUT_KEYS = {"ob", "ac", "first", "logp", "vtarg", "adv", "state_in"}
 
 def compute_gae(
@@ -57,6 +69,30 @@ def compute_advantage(model, seg, γ, λ, comm=None):
     vpredfinal = model.v(finalob, finalfirst, seg["finalstate"])
     reward = seg["reward"]
     logger.logkv("Misc/FrameRewMean", reward.mean())
+
+    # [OURS] C61. Once per iteration, beside the reward summary that is already here: the entropy
+    # coefficient multiplies a 7-D Gaussian's entropy, which is unbounded above, and nothing in a
+    # return curve shows a policy dissolving into saturated noise until it already has.
+    #
+    # **PPG is the one of the four that CLAMPS.** `distr_builder.py:31` applies
+    # `.clamp(-5.0, 2.0)` to the log-stdev before `.exp()`, so the parameter and the distribution
+    # the env actually sees are different objects once the clamp binds. Both are logged, and they
+    # are deliberately NOT the same key:
+    #   * `train/*` are computed on the CLAMPED values -- the policy that acted, and the only one
+    #     of the two comparable in kind to `idaac`'s and `ibac_sni`'s, which have no clamp.
+    #   * `train/log_std_raw_mean` and `train/log_std_clamped_fraction` describe the PARAMETER.
+    # A binding clamp is the failure C61 is about, silently: the entropy term keeps pushing
+    # log_std up, the clamp keeps cancelling it, the gradient is spent and no logged number moves.
+    # `clamped_fraction` is that event, made observable rather than inferred afterwards.
+    _logstd = getattr(model, "pi_logstd", None)
+    if _logstd is not None:
+        _raw = _logstd.detach().cpu().numpy().ravel()
+        _clamped = _raw.clip(-5.0, 2.0)
+        for _k, _v in _policy_health(_clamped).items():
+            logger.logkv(f"train/{_k}", _v)
+        logger.logkv("train/log_std_raw_mean", float(_raw.mean()))
+        logger.logkv("train/log_std_clamped_fraction",
+                     float((_raw != _clamped).mean()))
     adv, vtarg = compute_gae(
         γ=γ,
         λ=λ,
@@ -109,6 +145,12 @@ def compute_losses(
     with th.no_grad():
         diags["clipfrac"] = (th.abs(ratio - 1) > clip_param).float().mean()
         diags["approxkl"] = 0.5 * (logratio ** 2).mean()
+        # [OURS] C28. The line above is the authors' own k2 estimator and is left exactly as it
+        # is -- it is their number under their name. k3 = (r - 1) - log r is added BESIDE it so
+        # the cross-baseline column is one estimator everywhere, matching
+        # scripts/metrics.py::approx_kl's default. Unbiased and non-negative, which k2 is not.
+        # Logged automatically as Opt/approx_kl_k3 by the logkv loop below.
+        diags["approx_kl_k3"] = ((ratio - 1) - logratio).mean()
 
     return losses, diags
 
diff --git a/phasic_policy_gradient/torch_util.py b/phasic_policy_gradient/torch_util.py
index 8ad07ab..6931011 100644
--- a/phasic_policy_gradient/torch_util.py
+++ b/phasic_policy_gradient/torch_util.py
@@ -507,6 +507,14 @@ def register_distributions_for_tree_util():
         lambda d: ((d.logits,), None),
         lambda _keys, xs: dis.Bernoulli(logits=xs[0]),
     )
+    # CONTINUOUS HEAD (authored). aux_train stores `oldpd` inside the segment dict and slices it
+    # per minibatch, so the Normal returned by _make_normal must be a pytree too, or
+    # tu.tree_slice raises on it. Two leaves, unlike the one-leaf Categorical/Bernoulli above.
+    tree_util.register_pytree_node(
+        dis.Normal,
+        lambda d: ((d.loc, d.scale), None),
+        lambda _keys, xs: dis.Normal(loc=xs[0], scale=xs[1]),
+    )
 
 @functools.lru_cache()
 def warn_no_gradient(model, task):
diff --git a/phasic_policy_gradient/train.py b/phasic_policy_gradient/train.py
index e32b3a1..170fb40 100644
--- a/phasic_policy_gradient/train.py
+++ b/phasic_policy_gradient/train.py
@@ -7,7 +7,18 @@ from . import logger
 from .envs import get_venv
 
 def train_fn(env_name="coinrun",
+    # [OURS 2026-09-04] checkpoint retention; defaults reproduce upstream exactly.
+    save_mode='last',
+    ic_per_save=100_000,
     distribution_mode="hard",
+    # [Claude 2026-09-02 07:10 MSK: `seed` and `log_dir` were unreachable from the command line.
+    # `get_robosuite_venv` already takes a seed and `train_fn` never passed one, so every PPG run
+    # this project could launch was seed 0 with an unseeded network -- a three-seed comparison was
+    # not expressible. `log_dir` was fixed at /tmp/ppg, so two cells in one job would have written
+    # their curves and their saved model over each other. Neither changes what is computed for a
+    # given seed; both are the difference between a runnable schedule and one that silently
+    # collides. PLATFORM-class.]
+    seed=0,
     arch="dual",  # 'shared', 'detach', or 'dual'
     # 'shared' = shared policy and value networks
     # 'dual' = separate policy and value networks
@@ -32,13 +43,22 @@ def train_fn(env_name="coinrun",
     if comm is None:
         comm = MPI.COMM_WORLD
     tu.setup_dist(comm=comm)
+    # seed the same three sources every other baseline in this project seeds
+    import random as _random
+    import numpy as _np
+    import torch as _th
+    _random.seed(seed)
+    _np.random.seed(seed)
+    _th.manual_seed(seed)
+    if _th.cuda.is_available():
+        _th.cuda.manual_seed_all(seed)
     tu.register_distributions_for_tree_util()
 
     if log_dir is not None:
         format_strs = ['csv', 'stdout'] if comm.Get_rank() == 0 else []
         logger.configure(comm=comm, dir=log_dir, format_strs=format_strs)
 
-    venv = get_venv(num_envs=num_envs, env_name=env_name, distribution_mode=distribution_mode)
+    venv = get_venv(num_envs=num_envs, env_name=env_name, distribution_mode=distribution_mode, seed=seed)
 
     enc_fn = lambda obtype: ImpalaEncoder(
         obtype.shape,
@@ -66,7 +86,13 @@ def train_fn(env_name="coinrun",
             n_epoch_pi=n_epoch_pi,
             clip_param=clip_param,
             kl_penalty=kl_penalty,
-            log_save_opts={"save_mode": "last"}
+            # [OURS 2026-09-04] `save_mode` was hardcoded "last", which writes one `model.jd`
+            # and overwrites it, so ppg could only ever be evaluated at its endpoint. The
+            # repository already supports the alternative -- `log_save_helper.py:129` writes
+            # `model{save_idx:03d}.jd` under `save_mode="all"` -- so intermediate checkpoints
+            # need no new mechanism, only the freedom to ask for one. Both knobs default to
+            # exactly the previous behaviour, so an unchanged command line is unchanged.
+            log_save_opts={"save_mode": save_mode, "ic_per_save": ic_per_save}
         ),
         aux_lr=aux_lr,
         aux_mbsize=aux_mbsize,
@@ -76,6 +102,29 @@ def train_fn(env_name="coinrun",
         comm=comm,
     )
 
+    # [Claude 2026-09-02] TERMINAL SAVE. Without this the retained checkpoint is whatever
+    # LogSaveHelper last wrote, and that is not the trained model at any budget this project runs.
+    #
+    # The helper saves once in its own __init__ -- before a single gradient step -- whenever
+    # `ic_per_save > 0`, and `ic_per_save` DEFAULTS to 100_000 (log_save_helper.py:34, :50-51).
+    # So a 4,096-interact probe crosses no boundary and the only save on disk is the untrained
+    # one; that is exactly what the 2026-09-02 ppg cell produced, and it was caught by arithmetic
+    # rather than by a gate: entropy of a diagonal Gaussian depends only on sigma, the run logged
+    # Opt/entropy 9.936886, and log_std = 0 gives exactly 9.932570. See C60's correction.
+    #
+    # At 600k the helper's cadence does produce a trained model -- but at the last 100,000
+    # boundary, not at the declared endpoint, while every other baseline here is evaluated at its
+    # endpoint. Both failures are fixed by saving once more, at the end, under a name of ours.
+    #
+    # Written beside the helper's output, never over it: `model.jd` stays whatever the helper
+    # decided, so this adds an artifact and removes none.
+    if comm.Get_rank() == 0:
+        import os as _os
+        import torch as _th
+        _dest = _os.path.join(log_dir or ".", "model_terminal.jd")
+        _th.save(model, _dest)
+        logger.log(f"NATIVE_PPG_TERMINAL_SAVE {_dest} interacts={interacts_total}")
+
 def main():
     parser = argparse.ArgumentParser(description='Process PPG training arguments.')
     parser.add_argument('--env_name', type=str, default='coinrun')
@@ -87,6 +136,17 @@ def main():
     parser.add_argument('--clip_param', type=float, default=0.2)
     parser.add_argument('--kl_penalty', type=float, default=0.0)
     parser.add_argument('--arch', type=str, default='dual') # 'shared', 'detach', or 'dual'
+    # [Gemini 2026-08-31] Expose interacts_total CLI argument for custom budget control
+    parser.add_argument('--interacts_total', type=int, default=100_000_000)
+    # [Claude 2026-09-02 07:10 MSK: see train_fn's note]
+    parser.add_argument('--seed', type=int, default=0)
+    parser.add_argument('--log_dir', type=str, default='/tmp/ppg')
+    # [OURS 2026-09-04] Checkpoint retention, defaulting to upstream's effective behaviour.
+    parser.add_argument('--save_mode', type=str, default='last',
+                        help="'last' overwrites one model.jd (upstream); 'all' writes "
+                             "model<NNN>.jd per save, which is what an offline curve needs")
+    parser.add_argument('--ic_per_save', type=int, default=100_000,
+                        help='interactions between saves')
 
     args = parser.parse_args()
 
@@ -100,6 +160,11 @@ def main():
         n_aux_epochs=args.n_aux_epochs,
         n_pi=args.n_pi,
         arch=args.arch,
+        interacts_total=args.interacts_total,
+        seed=args.seed,
+        log_dir=args.log_dir,
+        save_mode=args.save_mode,
+        ic_per_save=args.ic_per_save,
         comm=comm)
 
 if __name__ == '__main__':
diff --git a/phasic_policy_gradient/vec_monitor2.py b/phasic_policy_gradient/vec_monitor2.py
index 7eab369..d60743f 100644
--- a/phasic_policy_gradient/vec_monitor2.py
+++ b/phasic_policy_gradient/vec_monitor2.py
@@ -65,6 +65,13 @@ class VecMonitor2(PostActProcessing):
             self.non_rolling_buf = None
         self.eprets = np.zeros(self.num, "f")
         self.eplens = np.zeros(self.num, "i")
+        # SUCCESS RATE (see docs/PART2-METRIC-INVENTORY.md). RL-ViGen patch P10 puts robosuite's
+        # own _check_success in `info`; Door's return is dense and scale-arbitrary and this is
+        # the only unit-free quantity. Convention shared with every baseline here: the episode
+        # counts as a success if the flag held at ANY step. Accumulated here rather than read off
+        # the recorded Episode's info, because that info is sampled when `first` is set -- i.e.
+        # AFTER the reset -- and so describes the next episode, not the one being recorded.
+        self.epsucc = np.zeros(self.num, bool)
 
     def process(self):
         lastrews, _obs, firsts = self.env.observe()
@@ -72,9 +79,12 @@ class VecMonitor2(PostActProcessing):
         self.eprets += lastrews
         self.eplens += 1
         for i in range(self.num):
+            self.epsucc[i] = self.epsucc[i] or bool((infos[i] or {}).get("success", False))
             if firsts[i]:
                 timefromstart = round(time.time() - self.tstart, 6)
-                ep = Episode(self.eprets[i], self.eplens[i], timefromstart, infos[i])
+                epinfo = dict(infos[i] or {})
+                epinfo["episode_success"] = bool(self.epsucc[i])
+                ep = Episode(self.eprets[i], self.eplens[i], timefromstart, epinfo)
                 if self.ep_buf is not None:
                     self.ep_buf.append(ep)
                 if self.per_env_buf is not None:
@@ -84,6 +94,7 @@ class VecMonitor2(PostActProcessing):
                 self.epcount += 1
                 self.eprets[i] = 0
                 self.eplens[i] = 0
+                self.epsucc[i] = False
 
     def clear_episode_bufs(self):
         if self.ep_buf:
