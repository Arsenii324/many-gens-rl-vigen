# Changes made in direct response to reviews 17/18 — mechanical changelog

Ordered chronologically. Every entry is a real commit in this repository; the hash is the anchor
to verify everything else in this response set against. `git show <hash>` or `git log -p -1
<hash>` reproduces the exact diff.

Note on scope: this list starts from the point reviews 17/18 entered the session (Codex's mailbox
Q52) and ends at the point this document was written. It does not include this session's earlier,
unrelated work (the evaluator-identity `door.xml` fix, the A27/A35/A36 pilot submissions before
either review existed) except where a later commit corrects something from that earlier work as a
consequence of a review claim.

| commit | what it did |
|---|---|
| `5f7bbed` | First pass: read both reviews in full, cross-checked the highest-impact claims against the live code (not either review's summary), fixed three confirmed errors in `notes/DECISION-SHEET.md` — see the three entries below for what each was — and found one item neither review resolved cleanly (CTRL's paper-vs-code conflict). |
| `9894b3b` | Filed `C97` in `docs/CONSTRUCTION.md`: CTRL's production parameters match the official code, not the paper table; both reviews read the identical evidence and reach opposite prescriptions; declared the conflict rather than silently picking a side, default set to keep the official-code profile (option 2 of three named options). |
| `2a585ab` | Posted the above as a mailbox reply (`notes/claude-answers.md`, entry `A60`) summarizing the three DECISION-SHEET fixes and the new `C97` register entry. |
| — | (Several commits between here and the next review-relevant one covered an unrelated owner-ratification-batch task; omitted from this list.) |
| `c5014c2` | Built (did not submit) `datasphere/native/cfg-ctrl-paper-profile-v175.yaml` — `C97`'s option 3, CTRL's paper-table profile as a declared second arm, verified runnable via `contract.py verify-payload`/`verify-evaluator-binding`. |
| `4072e23` | Reported the built CTRL config to the mailbox (`A63`). |
| `8799070` | `A64`: a full one-main-config-per-algorithm decision map across all twelve baselines, produced in response to a scope-narrowing instruction (Codex mailbox Q55: "one predeclared main run per algorithm," additional variants not active branches). This is where the SVEA/CURL backbone finding, the ALDA utd/budget resolution, and the IDAAC/PPG target-vs-interim distinction were all stated as a single table for the first time. |
| `a022371` | ALDA: `n_train_steps` 500,000 → 600,000, per review 18's ALDA-budget point, confirmed real via `scripts/audit_executed_hyperparameters.py`'s own `EXCLUDED_CLAIMS` entry before changing it. Regenerated `runnable/_patches/alda.patch`, fixed the consequent stale diff-stat figure in `docs/RUNNABLE-ORIGINALS.md` and a stale reason-string in the audit script. |
| — | (Codex's own concurrent work landed a correction to `C97`'s status field and retired the now-out-of-scope `cfg-ctrl-paper-profile-v175.yaml`, per Q55's scope narrowing — this is Codex's authorship, committed by me in `b112c11` below because it was sitting correct and uncommitted while Codex was unreachable for two hours; independently verified via `register.py --check` and the register test files before committing.) |
| `b112c11` | Committed the above Codex correction together with a new register entry, `C98` (see next row). |
| `d6a0975` | **`C98`**: found and fixed a false-certification bug in `scripts/eval_grid.py` while tracing IDAAC's frame-stack implementation gap. `--frame-stack`/`--image-size` CLI flags default to dmc_gb's own geometry (3, 100) and `run_probe.sh` never overrides either for any real production cell, so every family but rad/soda had its `evaluator_scope.frame_stack`/`image_size` recorded incorrectly, regardless of what it actually ran at — verified directly against retained records (idaac and ppg's A35/A36 pilot records both claimed `frame_stack=3, image_size=100` while genuinely running 64×64, one frame). Fixed by sourcing both fields from `rlgen/protocol.py::OBSERVATION_GEOMETRY[baseline]` instead. Not discovered by either review; discovered as a consequence of acting on one of review 17's claims. |
| `07a765a` | Reported `C98` to the mailbox (`A66`) and stated the plan to continue IDAAC's implementation. |
| `b9a0ac7` | `A65`: a full, precise trace of what IDAAC's frame-stack path actually needs (three things, not the two I'd assumed) — deliberately stopped *before* writing code, given the depth the trace surfaced (it reaches into the evaluator-identity mechanism this session had already had one incident with). Posted to the mailbox as an explicit "here's what's needed, tell me how you want it sequenced" ask. |
| `93ae962` | Given an explicit owner instruction to take the whole task rather than leave it idle while Codex was unreachable: implemented IDAAC's frame-stack machinery for real. A `FrameStack` wrapper (channel-concat) in `make_rlvigen_venv`; `VecPyTorchProcgen` made channel-count-aware (both its declared observation shape and its reset/step transpose heuristic, which previously hardcoded 3 and would have silently mis-transposed an already-correct tensor rather than crashing); a real `update_linear_schedule` for the paper's linear LR decay, its arithmetic verified in isolation before wiring it into `train.py`'s update loop across whichever optimizer attributes exist on `agent` (covers plain PPO and IDAAC/DAAC's three named optimizers with no algo-specific branch); new `--frame_stack`/`--use_linear_lr_decay`/`--lr_decay_env_steps` flags, all defaulting to PRISTINE/off (this file also serves the original Procgen benchmark). Verified end-to-end locally (construction, forward pass through the real network, multi-step rollout, 2-env parallel construction) at both `frame_stack=1` (byte-identical) and `frame_stack=3` before any of this was committed. |
| `15b4e73` | Made the full published recipe `families.json`'s actual idaac production config — not just a reachable capability. `num_processes` 4/16→1 (the v100 host-profile override removed entirely: this is now a fidelity-fixed constant, not a per-host throughput knob), `num_steps` 256→2048, `num_mini_batch` 8→32, `ppo_epoch` 1→10, `lr` 5e-4→3e-4, `gamma` .999→.99, `entropy_coef` .01→0, `value_freq` 1→32, `adv_loss_coef`/`order_loss_coef` .25/.001→.1/.1 (the reviews' headline 100× finding on `order_loss_coef`), `frame_stack` 1→3, linear LR decay on. `log_interval` recomputed 25→12, not removed or set to 1 (a real mistake I made and caught myself mid-edit: the coupled online-eval branch costs a fixed 5,000 env-steps every time it fires regardless of rollout size, so 1 would have reintroduced exactly the 5× training-cost problem the original 25 was chosen to avoid). `OBSERVATION_GEOMETRY["idaac"]` updated to `(64,3)`; a second, independent copy of the same fact in `normalize_curves.py`'s `CONVENTIONS` dict, caught immediately by `test_record_conventions.py`. `run_scene_idaac` (the real offline production evaluator) now reads `OBSERVATION_GEOMETRY["idaac"][1]` before constructing its env — without this, every future checkpoint evaluation would crash on a channel-count mismatch; this closes `A65`'s deferred third item, which stopped being optional once C2 became the actual default rather than a documented target. `production-schedule-v100.json` (a checked-in generated artifact) regenerated to match. |
| `819db7c` | Updated roughly a dozen tests and two docs whose hardcoded numbers assumed idaac's old geometry/rollout arithmetic — the 8/4 Procgen-single-frame split (now 9/3) in two independent instruments (`test_observation_geometry.py`, `test_comparability_seam_audit.py`), idaac's now-removed v100-specific budget floor, several `num_processes × num_steps` arithmetic checks, an exact-signature-string test, and the `RUNNABLE-ORIGINALS.md` diff-stat figure. Full test suite clean (0 failures) after this commit. |
| `b2b7ad5` | Rewrote `CLAIMS-LEDGER.md`'s idaac row to describe the actual current config instead of the retired P adaptation; added an `IMPLEMENTED` note to `DECISION-SHEET.md` A35 stating plainly this is the best technical default, not yet validated by a full-length run. Also committed Codex/Luna's already-verified SGQN and CTRL `CLAIMS-LEDGER.md` row updates, which were sitting correct and uncommitted. |
| `93fe9c2` | Reported the full implementation to the mailbox (`A67`). |
| `2860353` | Found while drafting this response set: `docs/PART2-METRIC-INVENTORY.md`'s own Finding 5 still said "8/4" and still listed `idaac` as single-frame — my own new test's assertion message had explicitly said to update this doc "in the same commit" as `15b4e73`, and I hadn't. Fixed, and fixed `rlgen/protocol.py`'s matching inline comment. |
| `4031c04` | Ran the grep this gap implied I should run (`04-blind-spots-and-unverified-claims.md` names this explicitly): found a few more stale "8/4" mentions (`docs/FAITHFULNESS.md`, `docs/SYSTEM.md`, a backward-reference inside `PART2-METRIC-INVENTORY.md` itself), and one more significant thing — `docs/CONSTRUCTION.md#c2`'s own register entry still argued idaac's frame-stacking is "structurally incoherent," the exact theoretical claim its own text admits was never citation-backed and that `ext/idaac/raileanu21a-supp.pdf` directly contradicts. Added a correction rather than rewriting the entry. |

## What this changelog does not include

- Codex's own commits and in-progress work on reviews 17/18 (the IBAC-SNI arXiv-ID fix across four
  files, `PRIMARY-SOURCE-FIDELITY-RECONCILIATION.md`, `review-17-triage.md`/`review-18-triage.md`,
  `EXTERNAL-REVIEW-ARTIFACT-BLUEPRINT.md`) — those are a different author's work, described in the
  two item-by-item files only where I directly verified a specific piece of it.
- PPG's own frame-stack/rollout-geometry implementation and IBAC-SNI's architecture-gap fixes
  (12-sample VIB, 256-d latent, posterior scale, L2, `-uda`) — explicitly Codex's open task per
  mailbox Q54, not attempted by me.
- Any renaming of RL-ViGen variants (`drqv2-rlvigen`, `CURL-RLViGen`, etc.) — not done by anyone as
  of this writing, named as open in both item-by-item files.
- A formal, standalone source-precedence-policy document (review 18's seven rules) — not created.
