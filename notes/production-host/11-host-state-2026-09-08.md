# Host state — read fresh every session; this is a snapshot, not a fact

**Machine identity VERIFIED, not inferred**: `hostname` on 100.98.2.11 returns **`cds2`**. The
Netbird guide's IP table was the starting point, not the confirmation.

## Update 12:47 UTC+3 (09:47 UTC): card 1 is now FREE

```
0, Tesla V100-SXM2-32GB, 0 MiB used, 32495 MiB free, 0 %
1, Tesla V100-SXM2-32GB, 0 MiB used, 32495 MiB free, 0 %   <- ours, now free
```

Thirteen minutes earlier card 1 held **16301 MiB at 88%**. That load finished. **This is the whole
argument for reading occupancy fresh every time rather than trusting a previous reading** — and for
not assuming the reverse either: it can come back mid-run, which is what the upper-bound rule is
about.

**GPU blocker: cleared for now, conditional on a fresh read at the moment of starting.**
**Disk blocker below: NOT cleared.**

---

# Original reading, 2026-09-08 12:34 UTC+3 (09:34 UTC) — TWO BLOCKERS

Read with aggregate queries only: `nvidia-smi --query-gpu=...` (which deliberately does **not**
enumerate processes), `nproc`, `uptime`, `free -g`, and `df -h "$HOME"` scoped to our own
filesystem. No `ps aux`, no `docker ps`, no other users' paths, no system directories.

## BLOCKER 1 — the card assigned to us is the one in heavy use

```
index  name                 total       used       free    util
0      Tesla V100-SXM2-32GB 32768 MiB      0 MiB  32495 MiB    0 %
1      Tesla V100-SXM2-32GB 32768 MiB  16301 MiB  16194 MiB   88 %
```

**Our assignment for 8 September is `cds2`, `V100-1`. That is card 1 — 88% utilised, 16.3 GiB
resident.** Card 0 is idle and **is not ours**; the idle card being free is not a reason to use it,
and using it would be taking someone else's assignment.

Against the 16194 MiB free on card 1, our observed peaks (`measured-vram-bounds.json`):

| family | observed peak | against 16194 MiB free |
|---|---:|---|
| `ctrl` | 20.78 GiB | **would not fit even at the observed peak** |
| `ppg` | 6.98 GiB | fits at the observed peak |
| `rlvigen` | 4.44 GiB | fits at the observed peak |
| `idaac` / `alda` / `dmc_gb` / `ibac_sni` | 1.79 / 1.39 / 1.36 / 0.88 GiB | fit at the observed peak |

**"Fits at the observed peak" is not permission to run.** The rule
(`10-resource-upper-bound-rule.md`) requires being sure the remainder covers our **whole** run, and
neither side of that is known here: our figures are past observations rather than production-config
bounds, and the resident 16.3 GiB belongs to a load whose peak we cannot bound and must not
investigate. Two peaks landing together is precisely how a CUDA OOM happens — and it would be
**their** job that dies as readily as ours.

**Do not investigate what is on card 1.** That it is occupied is the whole answer
(`05-privacy-and-non-alarm.md`).

**Status: no GPU work may start.** Either wait for card 1 to free, or raise the assignment with the
owner. Do not use card 0.

## BLOCKER 2 — the shared filesystem is at 99%

```
Filesystem  Size  Used  Avail  Use%  Mounted on
/dev/sda2    20T   19T   325G   99%  /
```

325 GB free on a 20 TB volume that everything shares — including `/`. What one cell needs, first
time through:

| GB | item |
|---:|---|
| 24 | Places365 train split (once, shared by `svea`/`sgqn`/`soda`) |
| 36 | replay episode files, one off-policy 600k cell |
| 15 | checkpoints, counted three times over (trainer, retained, packaged) |
| 5 | payload, pip wheels, apt packages |
| **80** | **one cell end to end, first time** |

The campaign is **36 cells**, and 8 of the 12 baselines are off-policy and carry the ~36 GB replay
each. **325 GB does not cover it.**

Worse than insufficient, it is *hazardous*: at 99% the margin is thin enough that our writes could
be what pushes a shared root filesystem to full. That breaks **every writer on the machine at
once**, including jobs mid-write that lose hours — the highest-blast-radius mistake available here
(`04-resource-safety.md`).

**Status: no large write may start.** Not Places365, not a 600k cell. This needs the owner: where
the campaign's data may live, whether another volume exists, and what our quota actually is.

## What is fine

```
cores 16, load 1.16 / 1.41 / 1.86     -- light
RAM   125 GB total, 112 GB available  -- ample
user  varaksin_as, groups: varaksin_as docker   -- Docker is usable without sudo
```

## The conclusion

**Both blockers are stops, not obstacles to route around.** Neither has a workaround that stays
inside the rules: using card 0 takes someone's assignment, and writing anyway risks a full shared
root. Report and wait — the correct outcome per `06-before-any-action.md`.

Re-read this state before each session; it is a snapshot, and the disk figure especially will move.


---

# Which baselines fit *surely*, and why the answer is not "the smallest"

The question is not peak size. It is **whether the peak was measured at the configuration
production will run.** VRAM scales with environment parallelism, not with the frame budget — the
replay buffer lives in host RAM, not on the card.

| family | observed peak | parallelism measured → production | safe to read as representative? |
|---|---:|---|---|
| `ppg` | 6.98 GiB | `num_envs` 1 → 1 | **yes** |
| `rlvigen` | 4.44 GiB | single env → single env | **yes** |
| `idaac` | 1.79 GiB | `num_processes` 1 → 1 | **yes** |
| `alda` | 1.39 GiB | single env → single env | **yes** |
| `dmc_gb` | 1.36 GiB | single env → single env | **yes** |
| `ibac_sni` | 0.88 GiB | `procs` **1 → 16** | **no** — 16× the workers |
| `ctrl` | 20.78 GiB | `num_envs` **16 → 64** | **no** — 4× the envs, from an already-large base |

**Five of seven were measured at their production parallelism.** `rlvigen`, `dmc_gb`, `alda`,
`idaac` and `ppg` change only their frame budget between the measured cell and the production one,
and frames do not drive VRAM. Those are the ones that fit surely.

The two that do not are the two already flagged elsewhere for the same reason:

- **`ctrl`** — 20.78 GiB at 16 envs, production is 64. Its host-RAM figure is likewise an
  unverified 4× extrapolation. Both of its production resource figures are unmeasured, in the same
  direction. It must be measured alone, capped, and watched.
- **`ibac_sni`** — 0.88 GiB at `procs=1`, production is `procs=16`. The smallest observed peak in
  the fleet is also one of the two least trustworthy, which is exactly the trap in ranking by size.

**Order of first production runs, on this evidence:** `dmc_gb`, `alda`, `idaac` (all well under
2 GiB at matched parallelism), then `rlvigen`, then `ppg`. `ctrl` and `ibac_sni` only after their
own measurement.

# Monitoring the card: occasionally, never as a standing job

**Do not leave a polling process running on the host.** A recurring job that repeatedly queries the
GPU is the shape of a resource-scraping script and can trip security tooling, which costs the
account and the project with it.

Check **occasionally and by hand**, with the aggregate query that does not enumerate processes:

```bash
ssh -o BatchMode=yes varaksin_as@100.98.2.11 \
  'nvidia-smi --query-gpu=index,memory.used,memory.free,utilization.gpu --format=csv,noheader'
```

Reasonable moments: before starting a run; once after it has settled into steady state; if
something looks wrong. **Not on a timer, and not in a loop.**

The DataSphere job monitors this session runs are a different thing entirely — they poll the
DataSphere API from *this* laptop, not the host, and touch `cds2` not at all.
