### NOTE
This review is generated by a weaker model; take its suggestions critically, but don't downplay the review in whole. 

### Executive Stance & Audit Restraint
  In accordance with your instructions:

  • Strict Read-Only Enforcement: Zero files inside many-gens-rl-vigen or flow were modified.
  • Zero Pollution: No scripts that mutate state, write into results/data directories, or overwrite logs/checkpoints were executed.
  • No Slow/Full Suite Runs: Slow, full test suites were deliberately avoided; targeted static analysis, AST traversal, direct log forensics, and fast unit checks (<9s) were used.
  Below is the deep audit across both repositories, surfacing critical bugs that escaped both Claude and Codex, false-passing production gates, hidden learning-dynamics distortions, and the 10 unfrozen owner
  decisions.
  ──────
  ### 1. Critical Finding: The Undetected Remote Blocker (bt1muhrpsvpdn8kio2tm)
  In Turn 63, Codex diagnosed why bt112flugk04gtrirgld failed (the IDAAC vector wrapper dropped _vigen_regime), patched IDAAC and eval_grid.py, built payload-v99.tgz, submitted bt1muhrpsvpdn8kio2tm, and exited
  with task_complete while the job was still running.

  Neither Claude nor Codex ever saw the result. Forensic diagnosis of the remote DataSphere logs shows:
    === bt1muhrpsvpdn8kio2tm (2860 lines) ===
    budget ok: 100000 frames
    === NATIVE_RLVIGEN_FROM_INPUT commit=90d8b8c40acb63af6f938c1f4cc79a0cfee7d7ec ===
    === NATIVE_IMPORT_GATE idaac ===
    === NATIVE_IMPORT_GATE_PASSED idaac ===
    --- last non-boilerplate lines:
        EVALUATOR_REVISION = evaluator_revision(ROOT)
      File "/tmp/native-work/scripts/eval_provenance.py", line 35, in evaluator_revision
        raise RuntimeError(f"cannot stamp evaluator revision: missing {relative}")
    RuntimeError: cannot stamp evaluator revision: missing rlgen/protocol.py
    === NATIVE_OFFLINE_EVAL_FAILED device=cuda rc=1 ===
    0 records from /tmp/native-out
  #### The Mechanism: A Payload Contract Asymmetry
  • In eval_provenance.py:25-35, Codex added:
    members = (
        "scripts/eval_grid.py", "scripts/eval_across_scenes.py",
        "scripts/eval_provenance.py", "datasphere/native/normalize_curves.py",
        "setup/apply_patches.py", "rlgen/protocol.py"  # <--- REQUIRED HERE
    )

  • In contract.py:19-45, "rlgen/protocol.py" was never added.
  • Result: contract.py build-payload filtered out rlgen/protocol.py. When eval_grid.py executed remotely on the DataSphere VM, it imported evaluator_revision(ROOT), which crashed trying to read rlgen/protocol.
  py.
  • Immediate Fix (for next active turn): Add "rlgen/protocol.py" to BASE_ALLOWED in contract.py, build payload-v100.tgz, and resubmit.
  ──────
  ### 2. Live Algorithm Defect: CTRL JAX PRNG Leak & False Gate PASS

  Review 5 caught a critical training-isolation defect in CTRL that remains completely live and unfixed in code:

  #### Code Evidence in train_ppo.py:263-315:

    _eval_numpy_state = np.random.get_state() if os.environ.get("NATIVE_ISOLATE_ONLINE_EVAL") == "1" else None
    try:
        action_id, _, _, key = select_action(train_state.params, train_state.apply_fn,
                                             model.ac, state_id.astype(jnp.float32) / 255.,
                                             key, sample=True)
        ...
        action_ood, _, _, key = select_action(train_state.params, train_state.apply_fn,
                                              model.ac, state_ood.astype(jnp.float32) / 255.,
                                              key, sample=True)
    finally:
        if _eval_numpy_state is not None:
            np.random.set_state(_eval_numpy_state)
    
    # Training loop continues immediately afterwards:
    metric_dict, train_state, key = update_ppo(..., key)
    cluster_metric_dict, train_state, key = update_cluster(..., key)

  • The Bug: Restoring np.random only restores CPU NumPy state. JAX PRNG state is functional: select_action mutates and returns an advanced key. That advanced key is subsequently passed to update_ppo and
  update_cluster.
  • Consequence: Online evaluation directly perturbs the training trajectory's randomness. Two identical training seeds with different evaluation cadences will follow different training paths.
  • The False-PASS Mechanism: production_gates.py:88-95 reports PASS because it only checks:
    name == "ctrl" and "np.random.get_state()" not in _read("runnable/ctrl/train_ppo.py")
  It performs a naive string search for np.random.get_state(), completely blind to JAX's functional PRNG mutation.
  • Required Fix: Split the PRNG key: eval_key, key = jax.random.split(key) and pass eval_key to select_action, keeping key untouched for the training steps.
  ──────
  ### 3. The Transplanted Constant Defect: PPG’s 32× Rescaled Auxiliary Phase

  As established in faithfulness-reconciliation.md and SYNTHESIS.md:

  • Upstream PPG Configuration: 4 MPI processes × 64 environments × 256 steps = 65,536 frames per global update.
  • Our Native Port: 1 MPI process × 8 environments × 256 steps = 2,048 frames per global update (a 32× smaller effective batch).
  • The Literal Transplant Trap: In upstream, n_pi=32 means an auxiliary phase runs every 32 × 65,536 ≈ 𝟐.𝟏 M frames. Upstream in a 600k-frame experiment would execute zero auxiliary phases.
  • Our In-Tree Behavior: Because n_pi=32 was preserved literally without rescaling, the auxiliary phase runs every 32 × 2,048 = 𝟔𝟓,𝟓𝟑𝟔 frames. In a 600k run, our PPG executes nine full auxiliary optimization
  phases.
  • Audit Verdict: The claim "PPG's budget of 6e5 is sufficient because it gets nine auxiliary phases" is an artifact of the 32× down-scaling, not algorithmic fidelity. Our in-tree PPG is running in an
  auxiliary-phase-dominated regime that upstream never experienced at this frame budget.
  ──────
  ### 4. The Hardware Horizon Dilemma: 9 of 12 Baselines Stunted by DataSphere

  Every one of the following adaptations was forced by DataSphere’s 4–8 core / 27 GiB ceiling:

  1. PPG: 32× smaller effective batch (1×8 vs 4×64).
  2. IDAAC: 16× fewer parallel envs (4 vs 64).
  3. IBAC-SNI: 16× fewer worker processes (1 vs 16).
  4. CTRL: 4× fewer parallel envs (16 vs 64).
  5. RL-ViGen Baselines (DrQ-v2, RAD, SVEA, SGQN, CURL): Replay capacity capped at 300,000 frames in families.json:90 for a 600,000-frame budget. It acts as a recency ring that discards the oldest 300k frames
  of experience (uncapped requires 38.8 GiB, which DataSphere rejected).

  The Operational Risk: Production is planned for a dedicated V100 machine with 16 cores and 113 GB RAM. If the fleet is launched using DataSphere-tuned configs, it will execute nine artificially crippled
  baselines that silently diverge from the published literature despite ample hardware headroom.
  ──────
  ### 5. Production Gate Status & The 10 Owner Gates

  Executing production_gates.py recomputes:

  • 19 PASS
  • 1 FAIL: gate_source_tree_frozen (79 uncommitted files with interleaved Claude and Codex edits).
  • 10 WAITING ON OWNER: Gated on human supervisor decisions in DECISION-SHEET.md:
   Gate Name                                     | Core Question / Blocker                                 | Current State
  -----------------------------------------------|---------------------------------------------------------|------------------------------------------------------------------------------------------------------
   ibac_sni competence                           | Does IBAC learn on Door with entropy_coef: 0.0?         | Stability proven (no runaway), but competence unproven; cfg-c61-entropy-v80.yaml is ready to submit.
   shared evaluator validated                    | Has IDAAC evaluator been validated on CUDA?             | Suspended pending the v100 payload contract fix.
   estimands frozen                              | Time-limit split handling & Door reward offset.         | 3 bootstrap vs 9 terminal. Door shaped reward floor (~1.82) must be subtracted from return ratio.
   seed policy frozen                            | Sample allocation rule.                                 | Fixed minimum (n=3) across all rows vs adaptive concentrating.
   production canary                             | Has the full 600k pipeline run end-to-end?              | Longest completed cell is 5.1h; full 600k SODA cell projected at 45h.
   external RL-ViGen anchor                      | C48: Has any published RL-ViGen number been reproduced? | Open. Reviews recommend reproducing an external anchor before launching the fleet.
   statistical protocol frozen                   | Outer unit of analysis.                                 | Training seed (n=3) as outer unit, not episode count (n=600).
   checkpoint rule frozen                        | What constitutes the headline metric?                   | Endpoint recommended, but best-in-trajectory vs endpoint needs formal freeze.
   production renderer verified                  | Does production environment match evaluated renderer?   | Pin Docker digest (MUJOCO_GL=egl) and reproduce DrQ-v2 100k return (~480.6).
   production scope frozen                       | Task scope.                                             | Door only vs Door + Lift (Lift requires re-deriving floor and anchor numbers).
  ──────
  ### 6. Sibling Project State (projects/flow)
  • Audited RUNS.md: No jobs are live or in-flight.
  • E318, E316, E317 are completed and recorded.
  • In projects/flow, the invariance mechanism was established, but distilled renderer fidelity remains the limiting factor. No background tasks or compute resources are active in flow.
  ──────
  ### Recommended Next Steps When Agents Resume

  1. Contract Fix: In contract.py, add "rlgen/protocol.py" to BASE_ALLOWED. Rebuild payload-v100.tgz and resubmit IDAAC revalidation.
  2. CTRL RNG Fix: In train_ppo.py, split eval_key off key before select_action to prevent evaluation from altering the training PRNG sequence.
  3. Submit IBAC Pilot: Launch cfg-c61-entropy-v80.yaml to gather empirical competence evidence for entropy_coef: 0.0.
  4. Git Checkpoint: Clean and commit the 79 uncommitted files to establish an immutable source hash and clear gate_source_tree_frozen.
