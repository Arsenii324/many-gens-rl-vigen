# Codex handoff — 2026-09-05 08:58 MSK

Read `notes/START-HERE.md`, then this file.  Verify tree state before acting; Claude is a concurrent
writer.  No commit, merge, production fleet, or DataSphere V100 use is authorised.

## Live bounded T4 jobs

| job | purpose | config | payload |
|---|---|---|---|
| `bt1s4q60d8nsvd4771l5` | train RAD 10k, then exercise current `dmc_gb` endpoint evaluator (2 regimes x 1 scene x 5 episodes) | `cfg-dmc-gb-functional-v108.yaml` | `payload-v108.tgz` |
| `bt1ag9egnoj6n8qvsdkj` | current PPG evaluator on retained 10,240-frame terminal model | `cfg-ppg-functional-v109.yaml` | `payload-v109.tgz` |
| `bt1jb9bpfb469s1l9602` | current IBAC-SNI evaluator on retained 100,096-frame Impala model; **not** final-beta/pilot evidence | `cfg-ibac-sni-functional-v110.yaml` | `payload-v110.tgz` |
| `bt1e78hbs4s946aje3q9` | exact repeat of deterministic-on IDAAC C70 control | `cfg-idaac-determinism-repeat-v111.yaml` | `payload-v106.tgz` |

The persistent, quiet finisher monitor is `/tmp/rlvigen-job-finish-monitor.sh`, PID `72160`, logging
only state transitions to `/tmp/rlvigen-job-finish-monitor.log` and sending a macOS notification only
when a job becomes SUCCESS/ERROR/CANCELLED.  It exits after all four terminal states.  Claude-mailbox
watcher already exists separately; only Codex writes `notes/ask-claude.md`.

When a job completes: `bash datasphere/native/job.sh diagnose <id>`, inspect its records for expected
scope/provenance/strict regime/diagnostics, then retain `records.jsonl` under `results/records/<id>__records.jsonl`
without overwriting anything.  Do not mistake one-scene functional rows for performance or burden discharge.

## Completed since prior handoff

- C70 control pair retained:
  - off `bt1e81rq286p23d3l4l9`: 120 s; `torch_deterministic_algorithms=false`
  - on `bt1baht74a35e6uq582c`: 118 s; same payload/checkpoint/revision; diagnostics complete.
  - records retained in `results/records/`.  ON/OFF differs in two of five sampled train returns;
    this is evidence to keep determinism enabled, not a fleet-throughput estimate.  The live repeat
    above tests deterministic-on reproducibility.
- V100-profile RL-ViGen replay is now **620k**, not 1M: non-evicting at the 600k, action-repeat-one,
  no-early-termination Door budget and permits two-cell RAM packing.  The DataSphere 300k default is
  unchanged.  Focused production/default tests passed.
- Current operational defaults schedule full endpoint grid (4 regimes x 10 scenes x 20 episodes) and
  commensurable intermediate grids every 50k (same grid x 5 episodes); endpoint is headline.
- Old rad/ALDA mixed job `bt1ptu2e6stuko76s6dd` is diagnosed: RAD and its curve evaluator succeeded;
  ALDA was SIGKILLed in environment construction on undersized T4.  It is not a dmc evaluator failure.

## Next critical path

1. Let the four active jobs finish; do not exceed four concurrent remote jobs.
2. Freeze the evaluator revision after any remaining shared-code/document changes that alter its hash;
   then functional-revalidate every evaluator family once.  Existing usable checkpoint map is in the
   completed Luna review in the transcript; `dmc_gb` needs the live RAD output, all other six have local
   artifacts.  Functional validation does **not** discharge the shared-evaluator burden.
3. Production-host-only work: exact final IBAC-SNI pair/pilot at `procs=16`, renderer R_A -> R_B control,
   Docker digest pin, measured V100 throughput/RAM packing, then one 600k canary.  Do not substitute a
   DataSphere V100; the owner prohibited it.
4. Run the full suite once after shared code stabilises (last known full suite before these final config
   additions was green); do not run it repeatedly.

## Genuine owner decisions remain

Defaults are implemented and await ratification, not treated as settled: A1–A20 in
`notes/DECISION-SHEET.md`; most material are final IBAC pilot/entropy policy, estimand/statistics and
seed rules, endpoint rule, Door-only scope, 620k vs 1M future-budget choice, curve depth, source-tree
commit, and final canary.  `scripts/production_gates.py` is the live status surface.

