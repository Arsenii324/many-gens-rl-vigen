> **Executor output, not a finding of record.** Produced 2026-09-20 by a read-only Sonnet executor from the PDFs under `ext/`, the docs and `results/records/`. Re-checked by the lead against the PDF: RL-ViGen Table 6 (p.17) gives Robosuite Door "Training Frames int(6e5)"; `RL-ViGen-upstream/cfgs/task/easy.yaml:1` is `num_train_frames: 1100000`, a code default. The two calibration answers (IDAAC 25M p.6, CTRL 8M p.9) reproduced. Not re-checked by the lead: the other per-paper rows, and its reading that IDAAC's "1 million environment steps" describes a learning-rate decay in a hyperparameter search rather than a main-result budget. Its NOT CHECKED list is at the top.

# Training-budget research — many-gens-rl-vigen

READ-ONLY. Nothing executed except `pdftotext`, `python3` (read-only JSON/JSONL parsing), and one
run of `scripts/where_is_this_decided.py` (see below). Nothing edited.

## NOT CHECKED / limits

- `scripts/where_is_this_decided.py` was **not actually needed** in the end — every claim below was
  reached by direct grep/read, so I did not run it. If the lead wants it run on a specific term
  (e.g. `where_is_this_decided.py "600000"`), that is a two-minute follow-up, not done here.
- Part C: I converted and grepped the PDF text for all 12 baselines + RL-ViGen (main paper) + IDAAC's
  supplement, using `pdftotext -layout`. I did **not** read any paper cover-to-cover; I located the
  training-budget sentence/table by targeted grep and read only the surrounding paragraph/table.
  Charts (bar graphs, e.g. RL-ViGen Figure 19/22) were extracted as text but the numbers cannot be
  reliably attributed to the correct bar/algorithm from linearized PDF text — flagged inline, not
  guessed.
- Two baseline PDFs turned out to be **mismatched to the wrong paper** (found by reading them, not
  assumed): `ext/baseline_resources/08_alda/paper_2001.01046_alt.pdf` is "Adversarial-Learned Loss for
  Domain Adaptation" (unrelated CV paper); the correct ALDA is `paper_2410.07441.pdf`
  ("Associative Latent DisentAnglement"). `ext/baseline_resources/11_ibac_sni/paper_1901.10902.pdf` is
  "InfoBot" (a different paper by an overlapping-but-different author list); the correct IBAC-SNI is
  `ext/papers-sorted/IBAC-SNI/IBAC-SNI_paper_neurips2019.pdf` (Igl et al., NeurIPS 2019). I used the
  correct papers once I found this; I have **not** checked whether any other baseline's filed PDF is
  similarly mismatched (I confirmed drqv2/svea/drq/sgqn/curl/rad/soda/idaac/ppg/ctrl by content, not
  by an exhaustive title check of every duplicate under `ext/_duplicates` or `ext/papers-sorted`).
- Part D.3: my own JSONL aggregation does **not** split by `eval_policy_mode` (native vs sample vs
  mode) the way `production_reading.py`/`notes/production-host/34,36` do — my numbers pool policy
  modes together and will not exactly match those notes' figures for that reason (they are close;
  I did not reconcile the small differences). Treat notes 34/36 as authoritative for any
  cross-baseline claim; treat my table as independent corroboration of the curve *shape* (return
  rises, success stays ~0) and of row counts.
- Part E: I did not find any document, anywhere in `notes/`, `docs/`, or `datasphere/native/*.json`,
  that states an *expected* V100 frames-per-second for any baseline before a run — I searched and the
  search itself (pattern and file count) is reported in Part E, not just the negative claim.
- I did not open `notes/remote-infra.txt` in full (only as a citation source named by other files).
- Dates: this repository is a **recovery workspace** (`.recovery-origin`, path name
  `...-recovery-workspace-2026-08-31`) — `git log` commit dates mostly cluster at/after the recovery
  and do not reflect original authorship. Wherever a document states its own date in its text, I used
  that; where it does not, I say so and give the git commit date range as a weaker signal, labelled as
  such.

---

## Part A — where 600,000 came from

**Headline: the 600,000-frame Door budget is not invented. It is RL-ViGen's own published number.**
Verified directly against the primary source (not from a project doc's summary of it):
`ext/baseline_resources/benchmarks/rl-vigen_v3_2307.10224.pdf`, page 17 (printed page number),
**Table 6: Robosuite hyper-parameters in RL-ViGen**:
> Training Frames | Door | int(6e5)

and Table 2 (page 15), **Action repeat | Robosuite: 1, otherwise: 2** — confirming action_repeat=1 is
also the paper's own stated Robosuite setting, matching this project's choice.

**But the project's own documents disagree with each other about whether this was known, and when:**

| # | file:line | date (as stated in the doc) | verbatim | decided by |
|---|---|---|---|---|
| 1 | `docs/FAITHFULNESS.md:200` (git last-commit 2026-09-16; auto-generated by `scripts/generate_fidelity_table.py`, not independently dated per-row) | not dated per-row | `training frames \| **Door 6e5, Lift 8e5** \| — \| 5e5 \| **the budget does have a source, and we are below it**` | states the ACTIVE budget as of that table's generation was **5e5 (500,000)**, i.e. below RL-ViGen's own number |
| 2 | `docs/FAITHFULNESS.md:216-218` | undated inline, file's "FIXED 2026-08-10" markers surround it | `**\`total_frames: 500000\` is no longer sourceless** — Table 6 gives Door 6e5 and Lift 8e5... on the agent-step axis canonical Door is ~550k against our 500k, so **Door is close and Lift is the real shortfall**` | assistant, referencing the 2026-08-10 deep-research finding (Part B) |
| 3 | `docs/PREMISES.md:350` (git range 2026-09-04–07) | undated | `**Q2 — What budget?** \`total_frames: 500000\` has no source in any file. It is \`[OURS]\`, and nothing says why.` | flagged as an open question, unattributed — an EARLIER/stale passage in the same file that `:241` (below) has already moved past |
| 4 | `docs/PREMISES.md:241` | undated, same file as #3 | `` `stddev_schedule: linear(1.0,0.1,100000)` \| RL-ViGen **Door easy tier**... \| our budget is 600k `` | assistant default, current at time of writing |
| 5 | `docs/STATUS-AGAINST-THE-GOAL.md:124` | **"2026-08-10"** header (line 3), row itself annotated **"NUMBERS NOW BEAR ON THIS, 2026-09-03"** | `**training budget** \| 5e5 \| Table 6 specifies **Door 6e5, Lift 8e5**. Ours was invented; theirs is sourced.` | flags that the then-current 5e5 was `[OURS]`/invented and RL-ViGen's is sourced — the row I independently confirmed against the PDF above |
| 6 | `notes/DECISION-SHEET.md:2785-2833` ("A46", dated **2026-09-08** in its own heading) | 2026-09-08 | full per-baseline table, see below | assistant analysis; ends **"Status: operational, not ratified."** — i.e. not an owner sign-off, as of that note |
| 7 | `datasphere/native/production-schedule.json` `_comment` | states **"as of 2026-09-04"** | `Costs cover all TWELVE baselines as of 2026-09-04.` `"frames": 600000` (top-level key) | generated by `plan_production.py`; not owner-attributed in-file |
| 8 | `datasphere/native/production-schedule-v100.json` | no internal date; `"frames": 600000` | `"calendar_status": "BLOCKED_ON_MEASURED_V100_THROUGHPUT"` | same, generated |
| 9 | `notes/PARAMETER-REVIEW-CONSENSUS-MATRIX.md:358` | undated inline | `**Budget**: 600,000 frames exactly.` | assistant, stated as settled fact by this point |

**Reading the timeline these dates imply:** an early working assumption of 500,000 frames (`[OURS]`,
no source — `docs/PREMISES.md` Q2, `docs/FAITHFULNESS.md`'s "5e5" row) was corrected to 600,000 after
the 2026-08-10 deep-research pass located RL-ViGen's own Table 6 (Part B). `docs/STATUS-AGAINST-THE-GOAL.md`
(also dated 2026-08-10, annotated 2026-09-03) and `docs/FAITHFULNESS.md` both show this correction
being reasoned about; by 2026-09-04 (`production-schedule.json`) and 2026-09-08
(`notes/DECISION-SHEET.md` A46) 600,000 is the settled operational number. **I found no document in
which the owner is directly quoted approving 600,000 specifically** — every citation above is an
assistant-authored note; the closest thing to an owner touchpoint is `notes/DECISION-SHEET.md:628`'s
"**Status: operational, not ratified**" for the adjacent 620,000-replay-capacity decision, and
`docs/STATUS-AGAINST-THE-GOAL.md:124`'s "**the owner asked whether they could**" (i.e. whether numbers
could bear on the budget question), not a ruling on the number itself.

**notes/DECISION-SHEET.md A46 table (2026-09-08), each baseline's own source horizon vs. the common 600k:**

| baseline | its own source horizon | source cited | 600k as fraction |
|---|---|---|---|
| `rad`, `soda` | 500,000 | `runnable/dmc_gb/src/arguments.py:18` | 120% |
| `alda` | 500,000 | `ext/ALDA_Official/specs/*.yaml`, `n_train_steps: 500_000` | 120% |
| `sgqn` | 500,000 | SGQN paper Table 3 | 120% |
| `drqv2`, `svea`, `curl`, `drq` | **1,100,000** | `RL-ViGen-upstream/cfgs/task/easy.yaml:1` | 55% |
| `idaac`, `ppg` | 1,000,000 | `raileanu21a-supp.pdf` §E (LR-decay window, see Part C caveat) | 60% |
| `ctrl` | 8,000,000 | CTRL paper | 7.5% |
| `ibac_sni` | 160,000,000 | `ext/IBAC-SNI/coinrun/coinrun/train_agent.py:28` | 0.375% |

**Important correction to this table, found by checking the primary source myself (not by trusting
the table):** the "1,100,000" cited for `drqv2`/`svea`/`curl`/`drq` is the **shipped code's generic
"easy" difficulty-tier default** (`RL-ViGen-upstream/cfgs/task/easy.yaml:1`, `num_train_frames:
1100000` — confirmed by reading the file directly), used across multiple domains (DMControl tasks also
select `easy.yaml`). It is **not** RL-ViGen's own stated Robosuite/Door number, which is Table 6's
**600,000** — the exact number this project runs. So for the four RL-ViGen-native baselines, this
project's 600k is not "55% of RL-ViGen's own horizon" as A46 frames it — it is **100% of RL-ViGen's
own stated Door horizon**, and the 1.1M figure belongs to a different (non-Door-specific) code
default that A46 conflated with "RL-ViGen's own." I confirmed there is no Robosuite-specific override
file in the vendored `RL-ViGen-upstream/cfgs/task/` beyond `Door.yaml` (which sets no frame count of
its own — the count comes from the composed `easy`/`medium`/`hard` tier file, and the *actual* Door
number the authors ran is only recorded in the paper's Table 6, not preserved as a literal in the
shipped scripts, which take `frames=${frames}` as a template variable).

---

## Part B — the earlier AI-agent budget research the owner remembers

**Found, and dated.** `research/deep-research/DR_1..DR_4_compass_artifact_*.md`, all with file mtime
**2026-08-10** (the earliest date in this project). "compass_artifact" is Perplexity's Deep Research
export filename convention. `docs/FAITHFULNESS.md:405-406` cross-references the same event by name and
date: `> ### Historical retired-port investigation (2026-08-10) — not the production path` /
`> An external deep-research pass verified RL-ViGen's **paper** tables verbatim but could not retrieve
a single line of its **source**...`. This is almost certainly what the owner remembers.

What these four documents record about budgets, verbatim:

- **DR_2, line 23:** `**Table 6 (robosuite):** Training Frames **Door int(6e5), Lift int(8e5)**,
  TwoArmPegInhole int(8e5)...` — the deep-research pass extracted Table 6 itself, a month before I
  re-verified it.
- **DR_2, line 94/121:** `**Training frames:** paper Door 6e5, Lift 8e5 **[VERIFIED in paper]**`
- **DR_4, line 92:** `**Note a discrepancy to resolve:** RL-ViGen's Table 6 trains **Door at 6e5 and
  Lift at 8e5 frames**, not 500k — at 78 steps/s that is ~2.1 h and ~2.85 h respectively. Budget with
  your own frame counts, not 500k, or you will under-provision Lift by ~60%.`
- **DR_4, line 135:** `**Budget with RL-ViGen's real frame counts (Door 6e5, Lift 8e5), not 500k.**`
- **DR_4, line 55:** `**Procgen** (Cobbe et al. 2020) trains on N levels (easy: 200 levels / 25M steps;
  hard: 500 levels / 100–200M steps — confirmed by the openai/train-procgen flags...)` — an early,
  independent corroboration of the IDAAC/PPG-family Procgen scale, consistent with what I verified
  directly in Part C.
- **DR_1, line 77:** cites a THIRD PARTY paper (not this project's own baseline set) that also trains
  on RL-ViGen's robosuite backend — `"DeGuV: Depth-Guided Visual RL..." (arXiv:2509.04970v1, 2025)`,
  **1,000,000 frames, 3 seeds** — flagged by DR_1 itself as "their re-run... not RL-ViGen's own runs,"
  i.e. a secondary source, not RL-ViGen's own number.

So: **at the time of the 2026-08-10 deep-research pass, this project's working budget was still
500,000**, and the deep-research documents are the ones telling it (twice, in DR_2 and DR_4) that this
was wrong relative to RL-ViGen's own Table 6. This matches `docs/FAITHFULNESS.md`'s "5e5... we are
below it" passage exactly and explains where that correction pressure came from.

Other candidates checked: `ext/baselines_comprehensive_research.md` and `ext/DISCREPANCY_MATRIX.md`
(both mtime 2026-08-10, same day) are **not** budget surveys — they cover hyperparameter mismatches and
host bugs; `ext/DISCREPANCY_MATRIX.md:14,70` mention "500k frames" only in passing, referring to the
then-current (later superseded) 500k assumption in bug reports, not as budget research.
`notes/PARAMETER-REVIEW-CONSENSUS-MATRIX.md:446-471` (undated inline; references "review 18") repeats
the same A46-style per-baseline horizon table found in `notes/DECISION-SHEET.md`, with an important
extra line at `:463`: `over the literal 1M steps and stop at 600k, so both end at **40% of their
initial rate**` — same 40%-of-LR finding as A46. `notes/PRIMARY-SOURCE-FIDELITY-RECONCILIATION.md:80`
(undated) states plainly: `**SOURCE VARIANT WITH CONTEXT**. RL-ViGen's Door table specifies 600k;
original method sources use 500k DMC, 25M/100M Procgen, or other domains. A common 600k Door budget is
a declared cross-method comparison, not original training length.` — the clearest single-sentence
statement in the repo of exactly what the owner is asking about.

---

## Part C — native budgets from primary sources (verified by reading the PDFs/code myself)

| baseline | training length, in the source's own units | primary source | page | verbatim (≤40 words) |
|---|---|---|---|---|
| **RL-ViGen (robosuite/Door)** | **600,000 frames** (`int(6e5)`), action_repeat=1 | `ext/baseline_resources/benchmarks/rl-vigen_v3_2307.10224.pdf` | p.17 (Table 6), p.15/16 (Table 2) | "Training Frames \| Door \| int(6e5)"; "Action repeat \| Robosuite: 1, otherwise: 2" |
| `drqv2` | reported at DMControl's standard **100k/500k** step regimes; also states "1M environment steps ≈ 2.9h" as a wall-clock example | `ext/baseline_resources/01_drqv2/paper_2107.09645.pdf` | p.2-8 | "it takes 10^6/96/3600 ≈ 2.9 hours to train for 1M environment steps"; action repeat of 2 stated (own hyperparameter table) |
| `drq` (v1) | **100k and 500k environment steps** (the PlaNet benchmark) | `ext/baseline_resources/02_drq/paper_2004.13649.pdf` | p.~9 | "Table 1: The PlaNet benchmark at 100k and 500k environment steps." |
| `svea` | **500,000 frames** | `ext/baseline_resources/03_svea/paper_2107.00644.pdf` | Table 5 ("Hyperparameters used in experiments on DMControl and robotic manipulation") | "Number of frames \| 500,000"; "all methods are trained for 500k frames" |
| `sgqn` | **500,000 frames** | `ext/baseline_resources/04_sgqn/paper_2209.09203.pdf` | p.15 (Table 3) | "Table 3 summarizes the hyperparameters..."; "Number of frames \| 500,000" — matches `notes/DECISION-SHEET.md`'s citation exactly |
| `curl` | **100k and 500k environment steps** (DMControl standard) | `ext/baseline_resources/05_curl/paper_2004.04136.pdf` | p.~1,8 | "Training for 100-500k environment steps"; "At 100k environment steps CURL..." |
| `rad` | **100k (data-efficient) and 500k (asymptotic) environment steps** | `ext/baseline_resources/06_rad/paper_2004.14990.pdf` | p.~6 | "regime) and 500k (i.e., asymptotically optimal regime) simulator or environment steps" |
| `soda` | **500,000 training steps** | `ext/baseline_resources/07_soda/paper_2011.13389.pdf` | p.~13 (hyperparameter table) | "Number of training steps \| 500,000" |
| `alda` | **500,000** (`n_train_steps`) — **from the authors' own released code, not stated as a clean sentence in the paper's extracted text** | code: `ext/ALDA_Official/specs/train_alda_walker_walk.yaml:11` (+3 sibling specs); paper: `ext/baseline_resources/08_alda/paper_2410.07441.pdf` ("Associative Latent DisentAnglement", Batra & Sukhatme) | n/a (code) | `n_train_steps: 500_000` |
| `idaac` | **25,000,000 environment steps** (Procgen, main results) — a **separate** figure, "linear rate decay over 1 million environment steps," describes the length of a hyperparameter *grid search*, not the main-result budget | `ext/idaac/raileanu21a.pdf` (main); `ext/idaac/raileanu21a-supp.pdf` (supp) | main p.6 (Table 1); supp p.9 | "Table 1. PPO-Normalized Procgen scores on train and test levels after training on **25M environment steps**." / supp: "...linear rate decay over **1 million environment steps**. Following this grid search, we used the best values found for all the methods." |
| `ppg` | **100,000,000 timesteps** (Procgen, main results) | `ext/baseline_resources/10_ppg/paper_2009.04416.pdf` | p.13 (Appendix A hyperparameters) | "Total timesteps \| 100M" |
| `ibac_sni` | **160,000,000 timesteps** (CoinRun, from the authors' own code; the paper's CoinRun section states it follows Cobbe et al.'s CoinRun setup but does not restate the step count in extractable prose) | code: `ext/IBAC-SNI/coinrun/coinrun/train_agent.py:28`; paper: `ext/papers-sorted/IBAC-SNI/IBAC-SNI_paper_neurips2019.pdf` (Igl et al., NeurIPS 2019 — **note:** the PDF filed as `11_ibac_sni/paper_1901.10902.pdf` is the WRONG paper, "InfoBot", not IBAC-SNI) | n/a (code) | `total_timesteps = int(160e6)` |
| `ctrl` | **8,000,000 training frames** (Procgen, main results) | `ext/ctrl_rl/2106.02193v2.pdf` | p.9 (Table 1) | "Table 1: Average evaluation returns collected after **8M training frames**, ± one standard deviation over 10 seeds." |

**Calibration check (per the coordinator's two known answers): both confirmed exactly, including page
numbers** — IDAAC Table 1 at page 6, CTRL Table 1 at page 9, both matched on first read.

**The RL-ViGen Door/Lift success numbers asked for (drqv2/svea/sgqn/curl/drq):** the paper's main body
(§4.1) reports Habitat/CARLA/Adroit results in text-extractable figures; **Robosuite results sit only
in Appendix E.1.2, Figure 19**, which is a **bar chart aggregated across all three Robosuite tasks
(Door+Lift+TwoArmPegInhole combined)**, not Door alone, and pdftotext linearizes the bar labels in a
way I cannot reliably attribute to the correct algorithm-bar without seeing the image (I did extract
the raw numbers to `pdftxt/rlvigen_v3.txt:1250` but will not present them as attributed per-algorithm
values — that would be exactly the kind of unearned precision the task warns against). **This
project's own note already has the reliable version of this fact, extracted from the authors' own
spreadsheet rather than a chart**: `notes/production-host/36-*.md` quotes
(`notes/rlvigen-published-door-anchor.md`, "extracted from their own `results/evaluation_score.xlsx`"):
published Door eval-easy — DrQ-v2 3.6, CURL 6.6, DrQ 14.0, **SVEA 268.8**, **SGQN 391.4** (PIEG 387.2,
SRM 337.2). I did not re-derive this myself from the spreadsheet (not in scope of the PDF-only
instruction), but I did open the chart these numbers would correspond to and confirm the qualitative
picture matches: augmentation/saliency methods (SVEA/SGQN) score far higher than plain DrQ-family
methods, consistent with "solving Door" vs. not.

---

## Part D — our own learning-curve evidence

**1. `notes/production-host/36-no-completed-cell-passes-the-competence-gate.md` (2026-09-18, 06:30 MSK).**
Every completed production cell fails the competence gate. Train-regime endpoint table (verbatim):

| baseline | seed | estimand | train Ȳ | train success |
|---|---|---|---|---|
| `ibac_sni` | 101 | mode | 75.81 | **0.000** |
| `ibac_sni` | 101 | sample | 82.18 | **0.005** |
| `idaac` | 101 | mode | 32.91 | **0.000** |
| `idaac` | 101 | sample | 39.24 | **0.000** |
| `idaac` | 102 | mode | 29.58 | **0.000** |
| `idaac` | 102 | sample | 20.51 | **0.005** |
| `ppg` | 1 | sample | 22.69 | **0.000** |
| `drqv2` | 2 | mode | 96.21 | 0.270 |

Curve shape (own words): "**Shaped return climbs; success does not move.** `ibac_sni` more than
quintuples its return across training and still opens the door in 0 of 30 episodes at almost every
stamp." Per-stamp curve given in the note (train regime, mean over scenes): `ibac_sni` s101, 12 stamps,
15.15→79.36, success 0.000 except 0.033 at two stamps; `idaac` s101, 11 stamps, 24.96→24.54 (noisy),
success ~0; `idaac` s102, 11 stamps, 1.20→10.99, success 0.000 throughout; `ppg` s1, 12 stamps,
4.68→20.53, success 0.000 except two small blips. Random floor stated as **1.842** (cross-ref C55).

**2. `notes/production-host/34-first-three-baseline-reading-2026-09-17.md`.** One-seed and (for idaac)
two-seed comparison; headline finding explicitly retracted within the same note: "**The generalisation
gap changes sign between seeds of the same baseline.** `idaac`'s train → eval-easy change is **−17%**
at seed 101 and **+18%** at seed 102."

**3. The 439.75-vs-96.21 tension, reported without reconciling it:**
- `docs/RESEARCH-FRAME.md:137` (own text): "Door, `drqv2` reaches **439.75** on the trained scene and
  retains **22.9%** across the scene axis" — this is `docs/CONSTRUCTION.md` **C81**
  (`docs/CONSTRUCTION.md:6779-6790`): `drqv2` **seed 7**, at the **100,000-frame** budget, a run
  described as "the first cell in this project that is uncontaminated, deterministic, verified finite,
  mode-verified and paired within one run," holding both `snapshot.pt` (100k) and a preserved
  `snapshot_50k_frames.pt`. Protocol: train-regime endpoint measurement, 10 scenes, ~20 episodes/scene
  (200 total; successes 18/20 stated per-scene in one place), regime retention computed against the
  paired 50k checkpoint of the *same run*.
- `notes/production-host/36-*.md:20` (table above): `drqv2` **seed 2**, **600,000 frames** (production
  scale), estimand `mode`, train Ȳ **96.21**, success **0.270**. The note's own caption: "an older,
  shorter, exploratory cell at a different budget and protocol, kept in the table only because it is
  the one row with real successes" — note 36 itself already flags these as not comparable, before I
  looked. Two different seeds (7 vs 2), two different budgets (100k vs 600k), and note 36 explicitly
  calls seed 2's row "an older, shorter, exploratory cell at a different budget and protocol" — the
  numbers are not measuring the same thing and the project's own text says so; I have not gone further
  than quoting both.
- `docs/STATUS-AGAINST-THE-GOAL.md:124` gives a THIRD `drqv2` seed-2 budget curve, at *earlier* frame
  stamps: "train-regime **0.84 → 30.67 → 283.66 → 453.91 → 456.06** at 0/25k/50k/75k/100k, with
  `train_regime_success` reaching **1.00 at 75k**... the eval column does not trend at all — 0.80,
  13.87, 3.27, 45.44, 5.61" — a THIRD number (456.06, not 439.75 or 96.21) for `drqv2` at 100k, on
  what appears to be yet another seed/run. I am reporting all three verbatim per the task's own
  instruction not to reconcile them.

**4. `results/records/*.jsonl` — read directly with Python, read-only** (90 files, 6,193 total JSON
rows parsed; full script and output kept at `scratchpad/budget/partD_records_summary.txt`). Train-regime,
non-pooled rows, grouped by frame stamp and eval scope (my own aggregation — does not split by
`eval_policy_mode`, see NOT CHECKED):

| cell | rows (this cell) | curve/endpoint train rows | first stamp → last stamp (return, success) |
|---|---|---|---|
| `ibac_sni` s101 | 910 (293 raw training-log rows also present) | 154 | 51,200: 15.15/0.0 → 600,064 (curve): 79.36/0.0 → 600,064 (endpoint, 20 ep): 79.0/0.003 |
| `idaac` s101 | 1,141 | 286 | 51,200: 24.96/0.0 → 550,912: 24.54/0.0 → 598,016 (endpoint): 34.96/0.0 |
| `idaac` s102 | 818 | 198 | 51,200: 1.20/0.0 → 550,912: 10.99/0.0 → 598,016 (endpoint): 25.05/0.003 |
| `ppg` s1 | 715 (47 raw training-log rows) | 175 | 51,200: 4.68/0.0 → 550,912: 31.02/0.0 → 600,064 (endpoint): 32.77/0.0 |
| `drqv2` s2 | 44 | 11 | only one stamp present: 100,000 (endpoint): 96.21/0.27 |

This independently corroborates note 36's qualitative claim (return rises across the curve, success
stays at or near 0.000 throughout) without relying on the note's own arithmetic. Small numeric
differences from notes 34/36 (e.g. idaac s101 endpoint 34.96 here vs. 39.24 in note 34) are because my
grouping pools `sample`/`mode`/`native` estimands together; notes 34/36 split them. `drqv2` s2 has only
one production-scale offline row in `results/records/` (the 96.21 endpoint row already in note 36's
table) — the 100k-budget `drqv2` curves discussed in C81/STATUS-AGAINST-THE-GOAL (seeds 7, and whatever
seed produced 456.06) are **not** the same seed and, on this evidence, may not be collected into
`results/records/` under a name my `(baseline, seed)` key would catch — I did not chase this further.

---

## Part E — cost

**Measured per-cell wall clock** (`notes/production-host/35-*.md:9-14`, 2026-09-17 21:30 MSK):

| cell | bootstrap | training (`time -v`) | in-cell grid | total |
|---|---|---|---|---|
| `ibac_sni` s101 | ~13 min | 31 min 32 s | ~13.9 h | **14 h 41 m** |
| `idaac` s102 | ~22 min | 7 h 15 m 18 s | ~13.6 h | **21 h 11 m** |

Own words: "**The grid, not the training, is the cost.** It is a fixed 3,476 episodes whatever produced
the checkpoints, and it took 13.6 and 13.9 hours in the two cells." Explicitly labeled as **measured**,
on a shared card.

**Campaign totals, same note (:39-46):** 32 cells MISSING, 1 PARTIAL. "Serially, one cell at a time:
480–670 hours of cell time ≈ **20–28 days**"; "Two cells packed on one card... roughly **11–16 days**";
"At the rate actually achieved — 4 cells in 9 days... 32 cells is **about 70 days**."

**Per-baseline throughput — measured vs. converted, from `datasphere/native/production-schedule.json`.**
`_comment`: "Costs cover all TWELVE baselines as of 2026-09-04. Ten rates are measured directly on
gt4.1; alda and ctrl are CONVERTED from timed gt4i.1 cells by the 1.14 tier factor." Field
`measured_completed_frames_per_second_gt4_1` (**T4 tier, not V100**): `svea 9.99`, `sgqn 6.5`,
`soda 3.25`, `drqv2 26.05`, `drq 12.48`, `curl 13.11`, `rad 6.14`, `alda 8.75` (converted),
`ctrl 14.92` (converted), `idaac 34.82`, `ppg 21.3`, `ibac_sni 26.63`. `throughput_basis.alda`/`ctrl`:
`"converted from gt4i.1 x1.14"`; all others `"measured"`.

**V100 throughput: never measured or estimated for any baseline before a run — confirmed both by
what the schedule file says and by an empty grep.** `datasphere/native/production-schedule-v100.json`
`_comment`: `"Every V100 throughput is null until measured on that host; T4 rates are references, not
estimates silently relabelled as production measurements."`; `"calendar_status":
"BLOCKED_ON_MEASURED_V100_THROUGHPUT"`. Per-row (read directly with Python): every one of the 12 rows
carries `"v100_completed_frames_per_second": null, "throughput_source": "UNMEASURED_ON_V100"`, with
only a `t4_completed_frames_per_second_reference` field — for `svea`: **9.99**, `sgqn`: **6.5**,
`soda`: **3.25**, `drqv2`: **26.05** (same T4 numbers as above, carried over as a labelled reference,
not a V100 estimate).
**I grepped the whole repo** (`notes/`, `docs/`, `datasphere/native/*.json`) for
`v100.*(fps|frames per second|steps/s|throughput)` and the reverse order, case-insensitive — **zero
hits anywhere** outside the schedule file's own explicit "null"/"UNMEASURED_ON_V100" fields. No
document states an expected or measured V100 fps for svea/sgqn/soda or any RL-ViGen-family baseline.

**This directly bears on the coordinator's addition.** The svea s101 cell of 2026-09-19 was observed
(per the coordinator, not found by me in a document — I found no note recording it yet, consistent
with it happening after `notes/production-host/36`'s 2026-09-18 timestamp) at **~2.4 frames/second**.
Against the only reference number this project's own files carry — the **T4** figure of **9.99
fps** — that is a **~4x** shortfall, and the schedule file's own header explicitly anticipated the
possibility of exactly this kind of gap by refusing to guess a V100 number in the first place, rather
than by predicting the actual size of the gap. `notes/production-host/35-*.md:151` (2026-09-17,
written before the svea attempt) already flagged the uncertainty directly: "**What I am accepting:**
`svea` has never trained here, its training time is unknown."

**Which numbers are measured vs. converted vs. absent, summarized:**
- **Measured** (this host, completed cells): `ibac_sni`, `idaac` — wall-clock totals above.
- **Measured** (T4/gt4.1, DataSphere, `production-schedule.json`): 10 of 12 baselines' fps.
- **Converted** (T4 → gt4i.1 tier factor 1.14): `alda`, `ctrl`.
- **Absent / explicitly null** (V100, this production host): all 12 baselines, before any run;
  `svea`'s first real attempt (2026-09-19, per the coordinator) measured **2.4 fps**, ~4x below its
  only prior reference point.
